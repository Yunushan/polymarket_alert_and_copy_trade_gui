# Goal: Multi-market prediction-market support

## Objective

Refactor the existing `MarketSentinel` app into a market-adapter based application and add configurable support for these markets:

- Polymarket
- Kalshi
- PredictIt
- Robinhood Prediction Markets
- Fanatics Markets
- DraftKings Predictions
- Interactive Brokers ForecastTrader / IBKR Prediction Markets
- ForecastEx
- CME Group Prediction Markets
- Nadex
- Crypto.com Predict / CDNA
- Hyperliquid
- Myriad Markets
- Context V2
- Frenzy Finance
- XO Market
- Manifold Markets
- Metaculus
- SciCast
- Good Judgment Open
- Hypermind
- Iowa Electronic Markets
- INFER / INFER-pub
- Fact Machine
- Opinion Labs
- Gemini Titan / Gemini Predictions
- Augur
- BetMGM
- PrizePicks
- Underdog Sports
- Drift BET
- Thales Market
- Hedgehog Markets
- Omen
- Zeitgeist
- Azuro
- SX Bet / SX Network
- Limitless Exchange
- Predict.fun
- Smarkets
- Betfair Exchange
- Probo

## Critical constraints

- Do not break existing working features.
- Keep Python support as >=3.10 with no artificial upper cap; Python 3.10 through 3.14 are required CI lanes and the moving latest stable 3.x lane covers future stable releases when runners provide them.
- Do not hardcode API keys, cookies, credentials, private URLs, IPs, domains, tokens, or user data.
- Use official APIs/SDKs/documented endpoints only.
- If a platform has no reliable/official API support, create a graceful stub adapter with clear "unsupported" messages.
- Do not bypass authentication, scrape private data, or implement risky live trading by default.
- Live trading/copy trading must be disabled by default.
- Paper/dry-run mode must be the default.

## Implementation requirements

Create a market adapter architecture with:

- Common adapter interface
- Market discovery
- Event/contract listing
- Price/orderbook/odds reading where supported
- Alert support
- Paper trading support where possible
- Live trading capability flag
- Copy-trade capability flag
- Per-market config enable/disable
- Clear unsupported-feature errors

Add or update:

- GUI market selector
- Config files/examples
- README capability matrix
- Tests for adapter interface
- Offline fixture tests
- verify.py checks
- docs/BLOCKERS.md for platforms that cannot be fully implemented without official API access, KYC, credentials, or regional access

## Done when

- Existing Polymarket features still work.
- App starts successfully.
- All listed markets appear in config and GUI.
- Each market has either a working adapter or a documented stub adapter.
- README includes a capability matrix showing: alerts, read-only data, paper trading, live trading, copy trading, API required, credentials required, region/KYC limitation.
- `python verify.py` passes.
- `pytest` passes if tests exist.
- Any remaining blocker is documented in `docs/BLOCKERS.md` with exact reason.

## Full implementation policy

The next implementation pass must move beyond "cataloged stub" support.

- "100% implemented" means the adapter uses official APIs, SDKs, documented endpoints, or documented protocol contracts for every feature it marks supported.
- If a market has no official/documented API, or requires unavailable KYC, account permissions, credentials, paid data entitlements, regional access, or private consumer-app endpoints, the app must not fake support. The correct implementation is a verified unsupported adapter with exact blocker documentation.
- The README matrix must not use `TBD`. Every cell must be `Yes`, `No`, `Required`, `Not required`, `Blocked`, `N/A`, or a similarly concrete value.
- Live trading and copy trading remain disabled by default for every market.
- Paper/dry-run mode remains the default for every market.
- Every adapter implementation must include offline fixture tests and `verify.py` coverage.

## Current implementation status

This project is not 100% feature-complete across every listed market. The completed baseline means every listed market is represented in the catalog/config/GUI/docs and each market has either a working adapter or a verified unsupported adapter with blocker documentation.

Current catalog snapshot:

- Total markets: 68
- Implemented adapters: 57
- Verified-blocked adapters: 11
- Market/event discovery supported: 57 yes, 11 blocked
- Alerts supported: 55 yes, 2 unsupported, 11 blocked
- Read-only price data supported: 55 yes, 2 unsupported, 11 blocked
- Orderbook reading supported: 26 yes, 31 unsupported, 11 blocked
- Trade history supported: 30 yes, 27 unsupported, 11 blocked
- Candle history supported: 34 yes, 23 unsupported, 11 blocked
- Paper trading supported: 54 yes, 3 unsupported, 11 blocked
- Live trading supported: 36 guarded/off by default, 21 unsupported, 11 blocked
- Copy trading supported: 18 guarded/off by default, 39 unsupported, 11 blocked (Polymarket, Opinion Labs, Manifold, Myriad, Hyperliquid HIP-4 wallet fills, Predict.fun authenticated account activity, Probable public wallet activity, Azuro single-selection bettor activity, XO authenticated account trades, Matchbook authenticated matched bets, Limitless authenticated portfolio history, Kalshi authenticated portfolio fills, Betfair authenticated matched orders, IBKR ForecastTrader/ForecastEx/CME authenticated account executions, Gemini authenticated filled orders, and SX Bet authenticated fills; all simulation-first and guarded)

These totals are generated from the canonical support matrix (`market_adapters.support_matrix`)
and intentionally distinguish supported, guarded, unsupported, and verified-blocked
states.  They should be refreshed whenever the catalog or an adapter capability changes.

Important status rules:

- `100%` on an article means the article's scoped objective is complete, tested, and documented.
- `100%` does not mean every feature is available on every market.
- Polymarket official API coverage must be reported by tier: `wrapper_available`, `app_workflow_available`, `offline_tested`, `public_live_verified`, `credential_live_verified`, and `funded_live_verified`.
- A Polymarket category must not be described as fully production-verified unless every applicable tier is `yes`.
- Unsupported markets must remain unsupported unless official APIs, documented protocol contracts, credentials, entitlements, account permissions, regional access, and legal/KYC requirements make implementation safe.
- The app must never fake market support, scrape private consumer apps, bypass authentication, or enable risky live/copy trading by default.

## Completed baseline articles

These articles completed the catalog, adapter architecture, verification, docs, and implemented support where official/documented access was available:

- Article 1 Adapter Foundation: scope complete
- Article 2 Polymarket Adapter Migration: scope complete
- Article 3 Market Stub Adapters: scope complete
- Article 4 GUI Market Selector: scope complete
- Article 5 Config Examples: scope complete
- Article 6 README Capability Matrix: scope complete
- Article 7 Blockers Documentation: scope complete
- Article 8 Verification Expansion: scope complete
- Article 9 Final Integration Pass: scope complete
- Article 10 Capability De-TBD Pass: scope complete
- Article 11 Adapter Runtime Infrastructure: scope complete
- Article 12 Polymarket Production Hardening: scope complete
- Article 13 Kalshi Adapter: scope complete
- Article 14 Manifold Markets Adapter: scope complete
- Article 15 Metaculus Adapter: scope complete
- Article 16 Public Forecasting Adapters: scope complete
- Article 17 PredictIt Adapter: scope complete
- Article 18 Limitless Exchange Adapter: scope complete
- Article 19 SX Bet / SX Network Adapter: scope complete
- Article 20 Azuro Adapter: scope complete
- Article 21 Legacy Web3 Protocol Adapters: scope complete
- Article 22 Web3 Sports/DeFi Protocol Adapters: scope complete
- Article 23 Broker/Exchange Regulated Adapters: scope complete
- Article 24 Consumer-App / Sportsbook Entrants: scope complete
- Article 25 Global Betting/Opinion Exchanges: scope complete
- Article 26 GUI Full-Market Workflow: scope complete
- Article 27 All-Market Catalog and Blocker Verification: scope complete

## Continuation article status

- Article 28 React GUI Foundation and API Parity: Python/API scope complete; React source wired to the state API; frontend build verification completed in Article 51.
- Article 29 React Market Operations Parity: Python/API scope complete; React source wired for market selector, enable/disable, health/capability display, credential diagnostics, and safety setting persistence; frontend build verification completed in Article 51.
- Article 30 React Paper Trading Parity: Python/API scope complete; React source wired for quote preview, side-aware quote limit fill, paper order submission, impact preview, history refill, position refill, exposure summary, mark refresh, selected mark refresh, selected/full mark clearing, and history clearing; frontend build verification completed in Article 51.
- Article 31 React Alert Workflow Parity: Python/API scope complete; React source wired for create/edit/delete alerts, enable/disable toggles, market-scoped source selection, adapter-backed alert status, and current price state display; frontend build verification completed in Article 51.
- Article 32 React Wallet and Copy-Trading Parity: Python/API scope complete; React source wired for wallet watch add/edit/delete/toggle, manual activity polling status, recent activity display, simulation-first copy settings, and guarded live-copy preflight preview; frontend build verification completed in Article 51.
- Article 33 React Live Safety Parity: Python/API scope complete; React source wired for selected-market live gate controls, kill switch, max size/notional caps, live acknowledgement, non-ordering live preflight, and redacted audit display; frontend build verification completed in Article 51.
- Article 34 Local API Hardening: scope complete; local API now returns structured error envelopes, validates JSON body size/content shape, avoids raw internal exception leakage, recursively redacts sensitive settings/audit details, and writes config atomically with endpoint and storage tests.
- Article 35 Adapter Capability Expansion Re-Audit: scope complete; verified-blocked markets were re-checked against current official sources, no market was safely promotable without production APIs/entitlements/wallet safeguards/fixtures, and blocker notes were tightened for Context, Hyperliquid, Thales, Smarkets, CME, and consumer-app products.
- Article 36 Packaging and Launch UX: scope complete; Windows smart/dev/prod/build launch scripts now document and enforce React setup paths, `web_api.py` exposes React build/dev/prod launch metadata and serves built `frontend/dist` assets with SPA fallback, and Tkinter remains the explicit fallback through `run_gui.bat` or `python app.py`.
- Article 37 Final Parity Verification: Python/Tkinter/API scope complete; `python app.py --smoke-test`, `python -m pytest`, and `python verify.py` pass with explicit Tkinter fallback, React workflow API parity, launch UX, and frontend build-readiness checks. Strict React build/browser verification completed in Article 51.
- Article 38 Polymarket User Analytics: Python/API/React source scope complete; public profile search and Data API leaderboard scanning are wired with computed ROI %, PnL/volume/ROI min-max filters, docs, and focused unit tests. Strict React build/browser verification completed in Article 51.
- Article 39 Polymarket Real MDD Analytics: Python/API/React source scope complete; user and leaderboard analytics compute MDD USD/% from public closed positions plus current open-position snapshots, support MDD sorting/filtering, expose the MDD percentage basis, update docs, and include focused unit tests. Strict React build/browser verification completed in Article 51.
- Article 40 Polymarket Official API Coverage: Python/API scope complete; official Gamma, Data, CLOB, Bridge, Relayer, and WebSocket surfaces are represented by public or guarded modules, authenticated endpoints require explicit credentials/headers, `GET /api/polymarket/coverage` reports coverage and live-validation requirements, README is updated, and focused unit tests cover request construction and guardrails. Strict live workflow validation remains pending real Polymarket credentials, eligible region/KYC status, funded wallets, and explicit live-mode opt-in.
- Article 41 Polymarket Truthful Coverage Model: scope complete; `/api/polymarket/coverage`, README, and GOAL now distinguish wrapper availability, app workflow availability, offline tests, public live verification, credentialed live verification, and funded live verification so the project cannot overclaim 100% production support before credentials and live checks are actually complete.
- Article 42 Polymarket Official Endpoint Contract Hardening: scope complete; official REST wrappers now share central endpoint metadata and HTTP behavior, use typed Polymarket errors, validate documented batch caps instead of silently truncating, retry safe transient public reads, normalize response contracts through shared helpers, document the contract layer, and include focused regression tests.
- Article 43 Polymarket Authenticated CLOB v2 Readiness: scope complete; authenticated CLOB setup now has redacted readiness validation for private keys, signature types, funder/deposit wallets, Polygon chain id, official CLOB host, L1/L2 header presence, adapter health, local API payloads, and safe live-verification reporting without deriving credentials or placing funded orders by default.
- Article 44 Polymarket Safe Live Order/Cancel Verification Harness: scope complete; funded verification now defaults to a dry-run transcript and actual execution requires explicit live flags, exact confirmation text, token allow-listing, GTC order type, hard size/notional caps, credential readiness, public maker-side orderbook preflight, immediate cancel, post-cancel order fetch verification, and redacted audit output.
- Article 45 Polymarket True Historical MDD v2: scope complete; MDD analytics now use a versioned public-data equity-curve builder from closed positions, current open positions, activity, and trades, expose USD/% drawdown with trade/activity capital-basis context, pagination controls, process-memory public-data cache boundaries, assumptions/limitations, React filter controls, coverage metadata, docs, and focused tests.
- Article 46 Polymarket Historical Mark Replay MDD: scope complete; optional `mark_replay` MDD mode now replays trade-derived token inventory against public CLOB batch price history, enforces the documented 20-token batch cap, reports missing/clipped/unreconstructable token rows and negative inventory, falls back to MDD v2 when replay is unavailable, exposes React controls, updates coverage/docs, and includes focused tests.
- Article 47 Polymarket Accounting Snapshot Reconciliation: scope complete; optional accounting snapshot parsing now reads the public ZIP CSV payload, extracts equity/position/cash-flow evidence, uses max statement equity as the strongest available MDD percentage base, reports deposits, withdrawals, cash-flow gaps, current-value and realized-PnL reconciliation deltas, updates API/React controls, docs, coverage metadata, and includes local ZIP fixture tests.
- Article 48 Polymarket Analytics Cache, Rate-Limit, and Export Hardening: scope complete; optional persistent bounded analytics cache metadata now stores per-wallet MDD audit artifacts for expensive leaderboard/MDD/accounting scans, API payloads expose Polymarket rate-limit/backoff state, JSON/CSV export endpoints serve cached MDD artifacts without rerunning public API calls, React exposes cache toggles and download links, docs/coverage are updated, and focused tests cover cache, exports, and rate-limit behavior.
- Article 49 Polymarket Analytics Detail UX and Browser Verification: scope complete; React now has direct wallet MDD lookup/export controls, profile-search wallet handoff, cached MDD audit detail loading from JSON artifacts, point previews, audit metadata, docs, regression tests, and Article 51 browser smoke coverage.
- Article 50 Polymarket Analytics Cache Management: scope complete; cache health/listing/purge helpers now expose retention metadata for stored Polymarket MDD audit artifacts, local API routes list cache contents, report health, purge expired/all/selected artifacts, React Analytics adds cache management controls, README documents the endpoints, regression tests cover list/health/purge behavior, and Article 51 browser smoke coverage verifies the cache view.
- Article 51 Frontend Dependency and Browser Verification Unblocker: scope complete; frontend dependencies were installed, `npm run build` now performs type-check-only TypeScript validation without generating shadow Vite config files, `python verify.py --frontend-build` passes with 291 tests, `verify.py` resolves `npm.cmd` on Windows, React tabs can be opened by URL for deterministic smoke routes, and local headless Edge smoke verified overview, markets, analytics, live safety, alerts, wallets/copy, paper trading, settings, `/api/state`, and Polymarket MDD cache fetches.
- Article 52 Polymarket Live Credential Validation Gate: local scope complete; `scripts/verify_polymarket_live.py` now emits explicit stage gates for public probes, redacted credential readiness, non-destructive authenticated reads, user WebSocket checks, bridge address checks, and funded order/cancel status; strict `--require-authenticated-read-ok` fails closed until a real authenticated read/stream succeeds; optional `--include-user-websocket-connect` probes the authenticated user WebSocket without returning secrets; `--report-file` writes durable JSON audit output; docs and tests cover the workflow. Real credentialed/funded verification remains blocked until user-provided credentials, eligible region/account state, funded wallet status, and explicit live-action approval are available.
- Article 53 Polymarket Live Validation API and React Surface: scope complete; `/api/polymarket/live-validation` now exposes a local no-funded-actions stage-gate report with generated timestamp, redacted credential presence, CLOB readiness, public/authenticated/bridge/funded check statuses, operator CLI audit commands, and the next blocked step; `/api/state` includes the report; React Live Safety renders and refreshes it without funded execution controls; README and tests cover the route and UI source.
- Article 54 Polymarket Live Validation Report Persistence: scope complete; `polymarket.live_reports` now stores bounded redacted JSON snapshots, `/api/state` includes report inventory, `GET/POST/DELETE /api/polymarket/live-validation/reports` list/import/store/delete GUI and CLI validation reports, React Live Safety can store the current snapshot, import CLI JSON, list reports, delete reports, and compare the latest two stage-gate summaries, and tests/docs verify secret redaction and no funded GUI execution.
- Article 55 Polymarket Live Validation Report Export and HTTP Smoke: scope complete; stored reports can now be opened by key through `GET /api/polymarket/live-validation/reports/{key}` and downloaded through `GET /api/polymarket/live-validation/reports/{key}/export.json`; React Live Safety adds Open and JSON controls plus opened-report details; HTTP tests exercise report POST/list/open/export/delete and 404 behavior; docs and source-parity tests cover the UI/API contract; `python verify.py --frontend-build` passes with 297 tests; and a temporary local Edge headless smoke verified the built Live Safety report-history controls render with a seeded report and export route.
- Article 56 Polymarket Live Validation Browser Smoke Automation: scope complete; `scripts/verify_live_validation_report_smoke.py` now starts a temporary local React API/server with temporary config/report storage, seeds a deterministic redacted report, verifies report open/export APIs, drives local Chromium/Edge headless through CDP until the built Live Safety report-history controls render, checks that seeded secrets are not exposed, and `verify.py` exposes `--frontend-live-smoke`; docs and source-parity tests cover the reusable no-credentials/no-funded-actions workflow.
- Article 57 Polymarket Credentialed Read Runbook and Secret Hygiene: scope complete; `polymarket.credential_runbook` and `scripts/verify_polymarket_credentials.py` now provide a local no-network/no-funded-actions credential inventory for SDK trading credentials, CLOB L2/L1 headers, user WebSocket auth, relayer headers, and builder headers; reports redact secrets, expose exact operator commands, support strict local readiness gates, feed the live-validation API/CLI reports, update coverage/docs, and are covered by verifier and regression tests.
- Article 58 Polymarket Credentialed Report Promotion Guard: scope complete; stored/imported live-validation reports now include a `verification_promotion` assessment that only allows `credential_live_verified=yes` with concrete authenticated-read or user-WebSocket evidence, only allows `funded_live_verified=yes` with a real funded order/cancel audit containing `live_action=true`, order id, placed/cancel/post-cancel sections, and post-cancel verification, blocks local-only runbook/browser-smoke/readiness reports from promotion, exposes guarded evidence candidates on `/api/polymarket/coverage` without mutating static coverage tiers, and shows credential/funded tier status in the React report UI with tests/docs.
- Article 59 Polymarket Live Validation Report Schema and Import Validation: scope complete; `polymarket.live_report_schema` now defines accepted report modes and shape validation, stored/imported reports are validated before redaction/disk write and carry compact `schema_validation` metadata, malformed HTTP imports return `live_validation_report_schema_error` with structured errors/warnings, deterministic valid/invalid fixtures cover credentialed-read, funded-audit, dry-run, runbook, and browser-smoke cases, `verify.py` validates the fixture contract, docs formalize the accepted shape, and the Live Safety browser smoke is more robust against blank-shell headless startup races.
- Article 60 Polymarket Live Validation Import UX Schema Details: scope complete; React Live Safety now shows accepted live-report schema mode guidance, carries structured schema diagnostics through typed API errors, surfaces validation errors/warnings from rejected imports without storing malformed reports, displays schema status for stored/opened reports, preserves schema metadata in opened/exported report details, and the browser smoke now checks schema UI fragments plus a malformed import rejection/no-storage path.
- Article 61 Polymarket Live Validation Report Replay CLI: scope complete; `polymarket.live_report_replay` and `scripts/replay_polymarket_live_reports.py` now validate one or more local report JSON files without network or funded actions, print schema diagnostics plus guarded credential/funded promotion summaries, support `--json`, `--fail-on-warning`, and optional redacted `--import` into the local report store, skip invalid files from import, preserve no raw payloads in replay output, and are covered by fixture replay tests, CLI tests, docs, and `verify.py`.
- Article 62 Polymarket Live Validation Report Provenance and Deduplication: scope complete; stored and replayed reports now carry stable SHA-256 hashes of redacted canonical payloads plus source-file provenance, duplicate imports are skipped by default while recording duplicate audit events, explicit API/UI/CLI allow-duplicate paths retain separate audit entries with `duplicate_of` provenance, React surfaces hash/source/duplicate state, docs describe the policy, and focused tests plus `python verify.py --frontend-build --frontend-live-smoke` cover the workflow.
- Article 63 Polymarket Live Validation Report Promotion Review Package: scope complete; stored reports now export sanitized JSON and Markdown review bundles containing schema status, redacted payload hash/provenance, duplicate history, guarded promotion evidence/blockers, source CLI commands, and coverage-tier mapping while keeping `static_coverage_mutated=false` and excluding raw payloads/secrets; API routes, React download links, docs, focused tests, verifier checks, and browser smoke cover the workflow.
- Article 64 Polymarket Live Validation Promotion Decision Ledger: scope complete; promotion decisions now live in a separate no-secrets ledger requiring report key, redacted payload hash, target tier, accepted/rejected decision, reviewer note, and deterministic review-bundle hash; mismatched payload/review hashes fail closed, blocked credential/funded tiers cannot be accepted without qualifying evidence, API/React/CLI-style JSON and Markdown exports are wired, docs and browser smoke cover the workflow, and `python verify.py --frontend-build --frontend-live-smoke` passes with 323 tests.
- Article 65 Polymarket Live Validation Coverage Promotion Proposal Export: scope complete; accepted promotion-decision ledger entries now produce a guarded no-automerge JSON/Markdown coverage/docs proposal with current review-bundle revalidation, stale-decision detection, human-review gates, CLI/API/React download surfaces, docs, focused tests, verifier checks, and no static coverage mutation.
- Article 66 Polymarket Live Validation Promotion Proposal React Preview: scope complete; the React Live Safety tab now has a read-only Promotion Proposal Preview with target-tier filtering, accepted/stale/ignored counts, review-gate display, accepted/stale/change tables, filtered JSON/Markdown downloads, no apply/automerge action, docs, browser-smoke fragments, source parity tests, and frontend build coverage.
- Article 67 Polymarket Live Validation Promotion Proposal Snapshot Archive: scope complete; promotion proposals can now be stored as bounded no-secrets snapshots with proposal hashes, provenance, current/stale hash checks, list/open/delete/export API routes, React archive controls, docs, smoke coverage, verifier checks, and regression tests while keeping static coverage unmutated.
- Article 68 Polymarket Live Validation Promotion Proposal Snapshot Diff Review: scope complete; opened snapshots now include a deterministic no-secrets current-versus-snapshot diff for proposal hashes, count deltas, accepted/stale decision identities, proposed files, and review gates; snapshot/diff JSON and Markdown routes, React review controls, docs, API/browser smoke, verifier checks, and regression tests preserve read-only no-automerge behavior.
- Article 69 Polymarket Unlimited Leaderboard Pagination Guard: scope complete; unlimited leaderboard scans now stop at the first repeated full upstream page instead of accumulating duplicate rows indefinitely, disk-backed scans persist the `repeated_page` completion reason for safe `--resume` behavior, CSV/JSON exports expose the completion reason, docs explain the public-pagination boundary, and focused state/API tests plus strict frontend verification cover the safeguard.
- Article 70 Polymarket Leaderboard Enumeration Scope Contract: scope complete; API, disk-backed CLI JSON/CSV metadata, and React analytics now surface deterministic scan completion reasons and source-enumeration status, clearly distinguish public-leaderboard exhaustion from incomplete/repeated/limited scans, and state that no endpoint result establishes coverage of every Polymarket account; docs, source-parity checks, targeted API/CLI tests, frontend build, and strict verification cover the contract.
- Article 71 Polymarket Durable Scan Automatic Recovery: scope complete; disk-backed public leaderboard scans now offer opt-in `--resume-on-failure` recovery for transient Polymarket HTTP/SSL/API errors, preserve local-error fail-fast behavior, resume SQLite state after bounded exponential backoff, provide restart limits and timestamped stderr diagnostics, document the VPS/nohup command, and include CLI/API regression coverage plus strict frontend verification.
- Article 72 Polymarket Durable Scan Status CLI: scope complete; the cross-platform read-only `polymarket-leaderboard-status` command now reports durable SQLite scan rows/pages, MDD done/error/pending counts, next offset, completion reason, timestamps, database size, and non-sensitive scan signature without starting or changing a job; legacy state files derive timestamps from stored pages, and docs, source-parity coverage, targeted state/CLI tests, and strict verification cover the workflow.
- Article 73 Polymarket Durable Scan Partial Export CLI: scope complete; the cross-platform read-only `polymarket-leaderboard-export` command now streams sorted CSV/JSON snapshots directly from an active durable SQLite scan without API/MDD reruns or all-RAM reconstruction, supports ROI/PnL/volume/MDD filters including `--require-mdd --max-mdd-pct 20`, reports partial/completion metadata in JSON, and is covered by CLI/source-parity tests, VPS docs, and strict verification.
- Article 74 Polymarket Durable Scan PID-File Liveness Status: scope complete; the read-only status command now optionally reads the existing `nohup` PID file and reports `running`, `not_running`, `missing`, `invalid`, or `unknown` without changing the job, using `OpenProcess` on Windows and `kill(pid, 0)` only on POSIX; VPS docs, cross-platform regression coverage, and strict frontend verification cover the workflow.
- Article 75 Goal Completion Evidence Audit: scope complete; `docs/GOAL_COMPLETION_AUDIT.md` now maps every local multi-market requirement to its implementation and verifier evidence, separates local proof from Polymarket credential/funded and platform-runner gates, and is required by `verify.py` so the audit remains present and explicit.
- Article 76 Polymarket Public-Live Evidence: scope complete; the no-credential, no-user-WebSocket, no-funded-action verifier successfully confirmed public Gamma, Data, CLOB, and Bridge endpoint responses on 2026-07-15; the completion audit records the exact command and retains the distinction between public evidence and still-unverified credentialed/funded tiers.
- Article 77 Implemented Adapter Fixture Coverage Gate: scope complete; `verify.py` now requires an exact mapping from every implemented catalog adapter to a non-empty offline fixture directory and its discovered adapter test file, with regression tests that reject an incomplete mapping; the completion audit records this direct proof for the fixture requirement.
- Article 78 Secret and Private-Endpoint Hygiene Gate: scope complete; `verify.py` now rejects common hardcoded token/private-key formats, credentialed URLs, non-loopback private-network addresses, and literal authorization/cookie values across application sources while allowing explicit localhost development endpoints; regression tests and the completion audit cover the security constraint.
- Article 79 Full Local Live-Safety CLI Parity: scope complete; Linux/Windows CLI now exposes local redacted live-validation report import/list/open/export/review/delete, guarded decision-ledger list/record/export, and read-only promotion-proposal/snapshot list/store/open/diff/export/delete workflows without credential derivation, network calls, or funded actions; runtime artifact files are ignored, docs and CLI regression tests cover the commands.
- Article 80 Durable CLI Paper-Mark Parity: scope complete; Linux/Windows CLI now exposes paper mark refresh, selected refresh, clear, and selected clear commands backed by an atomic local sidecar so marks persist across CLI processes and feed `paper show`; the sidecar contains no credentials, is Git-ignored, and has parser/persistence regression coverage and documentation.
- Article 81 Enforced Branch-Coverage Readiness Gate: scope complete; `python verify.py` now measures branch coverage for every test run and fails below 65% across the complete Python application or 74% across the headless/backend surface, with the coverage runtime declared in package requirements and policy regression tests and CI documentation.
- Article 82 Crypto.com Predictions Market Data Adapter: scope complete; the stale verified blocker was removed after Crypto.com published its official API, and anonymous event/search/contract/price reads, alerts, dry-run paper orders, optional licensed-key handling, rate/license diagnostics, offline fixtures, registry/config integration, and explicit market-data-only safety errors are covered by tests and docs.
- Article 83 Release Version Lineage Gate: scope complete; the project is prepared as version `1.0.11`, and `verify.py` now rejects invalid/local-only versions, shallow tag histories, reuse of an existing release tag on an older commit, and untagged versions that are not newer than the latest semver tag; CI/release checkouts fetch full history and focused regression tests and release documentation enforce the contract.
- Article 84 Modern Package License Metadata: scope complete; package metadata now uses the SPDX `0BSD` expression and explicit `LICENSE` file declaration required by current setuptools, removing the deprecated license-table warning and locking the BSD Zero Clause text and metadata into project, distribution, and release verification.
- Article 85 Installed Distribution Smoke Gate: scope complete; CI and release workflows now install the exact built wheel, run its CLI from outside the source tree, verify package version and PEP 639 license metadata, construct the 68-market registry, and import the newly promoted Crypto.com adapter before artifacts can be uploaded.
- Article 86 Strict Workflow Lint Configuration: scope complete; Actionlint now recognizes only the intentional custom `windows-10` self-hosted runner label through `.github/actionlint.yaml`, allowing all workflows to lint cleanly while retaining unknown-runner detection for other labels.
- Article 87 Reproducible Python Distribution Contents: scope complete; `MANIFEST.in` now includes the source, fixtures, config, docs, frontend source, scripts, workflows, and assets required to reproduce repository verification while pruning generated directories, and CI/release run `scripts/verify_python_dist_artifacts.py` to enforce wheel metadata/entry points and wheel/sdist content before installation or upload.
- Article 88 Real Tkinter GUI Lifecycle Gate: scope complete; `App.shutdown()` now cancels queued UI callbacks and stops market, wallet, and adapter workers before Tk destruction; `python app.py --gui-smoke-test` constructs the full widget tree without network workers, validates all desktop tabs, and closes deterministically. Ubuntu CI and the release package gate run this command through Xvfb, with regression coverage and operator documentation.
- Article 89 Production Readiness Scorer: scope complete; `scripts/check_product_readiness.py` now computes a conservative 100-point score, runs the local verification and optional public-only Polymarket probe, requires reviewed JSON evidence before awarding deployment, platform, repository-settings, release, credentialed, or funded points, and documents the scoring contract in `docs/PRODUCTION_READINESS.md` and the README.
- Article 90 Probable Official API Adapter: scope complete for repository-controlled integration; the Probable market/CLOB endpoints are represented by a fixture-backed adapter with event/market discovery, token prices, orderbooks, alerts, dry-run paper orders, simulation-first copy previews from the public wallet activity feed, guarded HMAC-authenticated signed-order submission, registry/config/GUI integration, and explicit live-copy safety gates. Real credentialed and funded verification remains external and is not claimed here.
- Article 91 Matchbook Official API Adapter: scope complete for repository-controlled integration; the Matchbook event/market, decimal-odds, runner-offer, session-login, `/v2/offers`, and authenticated `/v2/matched-bets/aggregated` contracts are represented by a fixture-backed adapter with probability-normalized prices/orderbooks/matched trades, local runner/time filtering, alerts, dry-run paper orders, guarded session-authenticated live offers, registry/config/GUI integration, and explicit copy-trading limitations. Account, regional, fee/limit, and funded execution evidence remains external and is not claimed here.
- Article 145 Matchbook Matched-Bet History: scope complete for this pass; the adapter now calls the documented authenticated aggregated matched-bets endpoint with event/market/runner filters and normalizes decimal odds, sides, stake, identifiers, and optional timestamps into `MarketTrade` records. Caller bounds are validated and applied locally because the endpoint does not publish a timestamp filter; live account access and settlement evidence remain external.
- Article 161 Matchbook Account-Recovery Parity: scope complete for this pass; the adapter now exposes documented authenticated settled-bet, current-bet, current-offer, balance, and account reads through the shared CLI/API/React allow-list with bounded numeric identifiers, epoch/ISO date bounds, odds types, offer filters, fixture coverage, and explicit session requirements. Cancel mutations, regional eligibility, and funded execution remain separate guarded or external evidence gates.
- Article 146 SX Bet Public Trade History: scope complete for this pass; the adapter now calls the documented public `/trades-v3/public` tape with a market filter and normalizes outcome-specific weighted-average odds, base-unit stake, trade identifiers, and ISO timestamps into `MarketTrade` records. Caller time bounds are validated and applied locally because the public endpoint exposes no timestamp filter. The adapter also exposes fixture-backed SX Bet v3 API-key account reads for balance, active/history orders, order detail, fills, and positions, plus guarded order/event/all cancellation; authenticated settlement evidence and copy trading remain separate external or unsupported surfaces.
- Article 92 DFlow Official Metadata/Trade API Adapter: scope complete for repository-controlled integration; nested event/market discovery, settlement-account outcome-mint normalization, probability prices, public outcome-mint trade history, complementary YES/NO orderbook reconstruction, dry-run Trade API requests, and guarded submission of a user-signed Solana transaction through an explicitly configured RPC are represented by fixture-backed tests. API-key, Proof/KYC, jurisdiction, wallet, funded-balance, RPC, redemption, and asynchronous order-status evidence remains external and is not claimed here.
- Article 93 Context V2 Official API Adapter: scope complete for repository-controlled integration; current v2 market discovery, outcome-token contracts, price summaries, outcome-specific orderbooks, dry-run order payloads, and guarded submission of externally signed orders using a Context API key are represented by fixture-backed tests. API-key, wallet setup/deposit, chain eligibility, settlement, and funded execution evidence remains external and is not claimed here.
- Article 94 Smarkets Official REST Adapter: scope complete for repository-controlled integration; current v3 event/market/contract discovery, quote-derived prices/orderbooks, dry-run orders, and guarded session-authenticated order submission are represented by fixture-backed tests. API application approval, written data-use permission, account eligibility, regional terms, and funded execution evidence remains external and is not claimed here.
- Article 95 Thales Official REST/AMM Adapter: scope complete for repository-controlled integration; official grouped/ungrouped market discovery, positional contracts, public AMM prices, buy-quote fallback, strict network/address validation, dry-run paper orders, and guarded externally signed AMM transaction submission are represented by fixture-backed tests across documented network IDs. Wallet signing, collateral approval, settlement, and funded execution remain external operator gates.
- Article 96 MetaDAO Official Futarchy DEX API Adapter: scope complete for repository-controlled integration; the official public `/api/tickers` contract, DAO/token-pair discovery, bid/ask/last-price reads, alerts, and dry-run paper orders are represented by fixture-backed tests. MetaDAO orderbook depth, Solana wallet signing, settlement, and live execution remain external and are not claimed here.
- Article 97 Seer Official Serverless API Adapter: scope complete for repository-controlled integration; the official `markets-search` and `get-market` contracts, chain-qualified market discovery, outcome prices, alerts, and dry-run paper orders are represented by fixture-backed tests. Seer orderbook depth, wallet signing, settlement, and live execution remain external and are not claimed here.
- Article 98 Hyperliquid HIP-4 Outcome Adapter: scope complete for repository-controlled integration; the official `outcomeMeta`, encoded outcome-asset, `l2Book`, and public `userFills` contracts, binary side mapping, prices, orderbooks, normalized wallet activity, simulation-first copy previews, dry-run orders, and guarded externally signed exchange payloads are represented by fixture-backed tests. Hyperliquid private-key handling, wallet signing, settlement, and funded execution remain external and are not claimed here.
- Article 99 Trueo Official Base Contract Adapter: scope complete for repository-controlled integration; the official Base `TruthMarketManager`/`TruthMarket` contract reads, immutable market fields, documented Uniswap V3 pool prices, dry-run orders, and guarded externally signed transaction submission are represented by fixture-backed tests. Live forwarding decodes the transaction and binds its chain, allow-listed target, calldata, value, and reviewed market/order metadata before broadcast. Trueo CLOB depth, wallet signing, settlement, and funded execution remain external and are not claimed here.
- Article 100 Zeitgeist SDK / Markets Adapter: scope complete for repository-controlled integration; the official SDK/indexer market and asset GraphQL schema is represented by an explicit alias adapter, separate configuration key, independent fixtures, discovery, outcome prices, alerts, dry-run paper orders, and a guarded externally signed HybridRouter extrinsic boundary. Wallet signing, approvals, CLOB depth, settlement, and funded execution remain external and are not claimed here.
- Article 101 IBKR Event-Contract Adapter Family: scope complete for repository-controlled integration; the official IBKR Client Portal category/search, strikes, instrument-info, accounts, marketdata/snapshot, and account/orders contracts are represented by shared ForecastTrader, ForecastEx, and CME adapters with deterministic conid/YES-NO mapping, top-of-book prices, alerts, paper orders, guarded live limit orders, and independent fixtures. Authorized sessions, account/data entitlements, broker permissions, response confirmations, and funded execution remain external and are not claimed here.
- Article 102 Gnosis Prediction Markets Adapter: scope complete for repository-controlled integration; the official Gnosis/Omen FixedProductMarketMaker subgraph schema is exposed as an explicit market-scoped alias with independent configuration, health diagnostics, discovery, outcome prices, alerts, paper orders, fixtures, and a guarded externally signed FPMM transaction boundary. CLOB depth, collateral approval, settlement, and copy trading remain external or unsupported by the underlying AMM surface.
- Article 103 Zeitgeist Prediction Pools Adapter: scope complete for repository-controlled integration; the official Zeitgeist market/pool/asset GraphQL schema is exposed as an explicit pool-scoped alias with separate configuration, pool-identifier validation, independent fixtures, discovery, pool-backed outcome prices, alerts, paper orders, and guarded externally signed HybridRouter forwarding with reviewed pool metadata. Pool accounting, settlement, wallet signing, CLOB depth, and copy trading remain external or unsupported.
- Article 104 Reality.eth Question Adapter: scope complete for repository-controlled integration; the official Reality.eth question subgraph schema is exposed through a configured GraphQL adapter with question discovery, response-option listing, lifecycle status, independent fixtures, and explicit read-only safety errors. Prices, price-triggered alerts, orderbooks, paper orders, wallet answer submission, and trading remain unsupported because Reality.eth is an oracle/question protocol.
- Article 105 Drift BET Data API Adapter: scope complete for repository-controlled integration; the official public Drift Data API BET prediction-record contract is represented by an explicit configured-symbol adapter with bounded YES/NO price derivation, alerts, dry-run orders, path-safe validation, registry/config/GUI integration, and offline fixtures. The API has no stable market-list endpoint; binary DLOB depth, wallet-signed orders, settlement, live execution, and copy trading remain external or unsupported.
- Article 106 Space REST Adapter: scope complete for repository-controlled integration; the official Space REST `/markets`, market-detail, and `/orderbook` contracts are represented by a path-safe adapter with public discovery, binary/multi-outcome contract listing, prices, orderbooks, alerts, local paper orders, config/registry/GUI integration, and offline fixtures. The docs currently say public production release timing will be announced separately; wallet-signed live execution, settlement, and copy trading remain unsupported.
- Article 107 Prophet Exchange Official API Adapter: scope complete for repository-controlled integration; the official ProphetX Market Data and Trading API contracts are represented by a path-safe adapter with tournament/event/market discovery, nested and flat selection normalization, decimal-odds probability prices, available-quantity quote books, alerts, dry-run paper orders, guarded authenticated BUY quote submission, config/registry/GUI integration, and offline fixtures. Partner approval, credentials, account/KYC/region eligibility, cancel/status/settlement, and funded execution remain external gates.
- Article 108 Blocked-Market Freshness Audit: scope complete for this pass; PRDT Finance's official legacy `Prediction` contract ABI is now represented by fixture-backed reads for an operator-supplied, independently verified contract inventory, without guessing a current factory/indexer deployment. Probo's official site states that operations are closed, and Sporttrade's official site states that all wagering ceased on 2026-05-25; those catalog entries remain fail-closed with updated blocker evidence rather than being promoted through private or stale endpoints.
- Article 109 Hedgehog Markets Contract Promotion: scope complete for this pass; the official Hedgehog HPL Parimutuel/Eclipse `MarketV1` custom-Borsh account contract is decoded through Eclipse JSON-RPC for discovery, outcome listing, pooled probability prices, alerts, and fixture-backed dry-run `DepositV1` intents. CLOB depth, wallet-signed live execution, settlement, and copy trading remain explicitly disabled.
- Article 110 Frenzy Finance Contract Promotion: scope complete for this pass; the official Base/Base Sepolia deployments and `BetSettled` event shape are represented by a fixture-backed adapter for configured grid discovery, settlement history, alerts, and dry-run EIP-712 `BetIntent` previews. Active quotes/oracle acknowledgements, orderbooks, wallet signing, live execution, settlement submission, and copy trading remain explicitly disabled.
- Article 111 Fanatics Markets/CDNA Read-only Adapter: scope complete for this pass; Fanatics Markets is represented by a fixture-backed alias over CDNA's official Predictions API for event discovery, contracts, prices, alerts, and local dry-run orders. Fanatics-specific orderbook, live execution, and copy trading remain explicitly disabled because no supported Fanatics order API is published.
- Article 112 Coinbase Prediction Markets/Kalshi Read-only Adapter: scope complete for this pass; Coinbase's official launch and help documentation identify Kalshi as the prediction-market venue and direct users to Kalshi market outcomes. A fixture-backed alias now covers public discovery, contracts, prices, orderbooks, trades, candlesticks, alerts, and local dry-run orders through Kalshi's documented market-data API. Coinbase-specific account execution and copy trading remain explicitly disabled because no supported Coinbase order API is published.
- Article 113 Thales Guarded AMM Transaction Boundary: scope complete for this pass; the official Thales REST/AMM adapter now advertises guarded live capability and accepts only externally signed raw EVM transactions with explicit network, configured AMM target, reviewed method/calldata, selected market/position, shared live-safety gates, and validated RPC transaction hashes. Paper mode remains the default; wallet signing, collateral approval, settlement, and funded execution remain operator-owned external gates.
- Article 114 Hedgehog Guarded Deposit Boundary: scope complete for this pass; the official HPL Parimutuel `DepositV1` instruction contract and Eclipse JSON-RPC path are represented by fixture-backed discovery, pooled prices, dry-run intents, and a guarded submission boundary for canonical base64 wallet-signed transactions. The adapter requires reviewed program/market/option/amount metadata and shared live-safety gates, never signs or settles, and keeps paper mode default.
- Article 115 Omen/Gnosis Guarded FPMM Boundary: scope complete for this pass; the official Omen FixedProductMarketMaker contract surface and subgraph adapter now accept only externally signed EVM buy/sell transactions with explicit FPMM target, side, outcome, calldata, shared live-safety gates, and a validated RPC hash. Paper mode remains default; collateral approval and settlement stay operator-owned.
- Article 116 Seer Guarded DEX Transaction Boundary: scope complete for this pass; the official Seer API and contract documentation are represented by fixture-backed discovery, outcome prices, alerts, paper orders, and a guarded forwarding boundary for externally signed third-party-DEX transactions. The adapter requires an explicit reviewed DEX allow-list, chain/market/outcome/method/calldata metadata, an EVM RPC, and shared live-safety gates; it never signs, approves collateral, or settles.
- Article 117 MetaDAO Guarded Router Transaction Boundary: scope complete for this pass; the official Futarchy DEX API/configuration contract is represented by fixture-backed ticker discovery, prices, alerts, paper orders, and a guarded forwarding boundary for externally signed Solana router transactions. The adapter requires an explicit reviewed router-program allow-list, ticker/pool/instruction metadata, canonical base64, a Solana RPC, and shared live-safety gates; it never signs, approves tokens, or settles.
- Article 118 Zeitgeist Guarded HybridRouter Boundary: scope complete for this pass; the official Zeitgeist HybridRouter pallet's `buy`/`sell` call contract is represented by fixture-backed GraphQL reads and a guarded `author_submitExtrinsic` boundary. The adapter requires an externally signed canonical extrinsic, reviewed pallet/call/market/asset/amount/price/order/strategy metadata, a matching runtime spec version, an explicit Substrate RPC, and shared live-safety gates; it never signs, approves, settles, or reads CLOB depth.
- Article 119 Zetarium World BSC PredictionMarket Adapter: scope complete for this pass; the published Zetarium V2 contract model and reviewed BSC `PredictionMarket` deployment are represented by fixture-backed market discovery, binary/multi-outcome pool-share prices, alerts, local paper intents, and a guarded externally signed `placeBet` boundary. The adapter validates chain, target, value, calldata, market/outcome/side/size metadata, and shared live-safety gates before forwarding; it never signs, approves, settles, reads CLOB depth, or performs copy trading.
- Article 120 Lamas Finance Solana Anchor Adapter: scope complete for this pass; the official Lamas Finance `PricePredict` and `UpOrDown` programs are represented by fixture-backed `getProgramAccounts`/`getAccountInfo` reads, pooled Up/Down prices, PricePredict reference prices, alerts, local paper intents, and a guarded externally signed Anchor `predict` boundary. The adapter validates the reviewed program, round account, outcome, amount, and instruction bytes before forwarding; it never signs, settles, reads CLOB depth, or performs copy trading.
- Article 121 Fresh Blocked-Market Audit: scope complete for this pass; Robinhood, DraftKings, Hypermind, Good Judgment Open, Blinq, Levr Bet, Dexsport, and SynStation were rechecked against current first-party surfaces on 2026-08-21. Iowa Electronic Markets is no longer in the blocked set: Article 136 promotes its official historical price-file archive through an archive-only adapter, while current quotes, orderbooks, live trading, and copy activity remain unsupported. Nadex was re-audited separately and promoted only for the documented read-only CDNA/Nadex prediction-event API surface; account trading, DCM/FIX depth, and knock-out products remain unsupported.
- Article 122 FanDuel Predicts/OG Read-only Adapter: scope complete for this pass; FanDuel's current product and June 2026 press release document contracts listed by CME Group and Crypto.com's OG Prediction Markets/CDNA. A fixture-backed alias now covers the documented OG/CDNA discovery, contracts, prices, alerts, and local dry-run orders through the official Crypto.com Predictions API. CME-listed contracts remain available through the CME/IBKR adapter; FanDuel account execution, orderbook depth, and copy trading remain explicitly unsupported.
- Article 123 Opinion Wallet Activity and Simulation Copy Adapter: scope complete for this pass; the official Opinion OpenAPI wallet-trade feed is fixture-backed and normalized into the common activity shape, Opinion copy capability now produces bounded simulation-first paper intents, and the selected-market wallet/copy API path no longer assumes Polymarket. Opinion live execution remains separately guarded by the CLOB boundary documented in Article 128.
- Article 124 Manifold Public Activity and Simulation Copy Adapter: scope complete for this pass; the official public `/v0/bets?username=...` feed is fixture-backed and normalized into the common activity shape behind a safe `manifold:<username>` identity, selected-market wallet/copy API and desktop paths accept the identity, and simulation-first copy previews preserve Manifold amount-versus-share semantics. Live MANA betting remains separately guarded, and CLOB orderbook reading remains unsupported.
- Article 125 Myriad Public Wallet Activity and Simulation Copy Adapter: scope complete for this pass; the official public `GET /users/:address/events` feed is fixture-backed and normalized into the common activity shape with EVM identity validation, buy-value versus sell-share semantics, selected-market API/desktop copy preview parity, and guarded simulation-first paper intents. Live Myriad execution remains behind signed-order and shared live-safety gates.
- Article 126 Nadex/CDNA Read-only Adapter: scope complete for this pass; Nadex's official product/rules pages and Crypto.com's documented Predictions Market Data API support a fixture-backed alias for prediction-event discovery, contracts, bid/ask/mid/probability reads, alerts, and local paper orders. Nadex account automation, DCM/FIX depth, knock-out products, live trading, and copy trading remain explicitly unsupported.
- Article 127 Blinq/Polymarket Read-only Data Alias: scope complete for this pass; Blinq's official product page says its leverage layer trades Polymarket markets, so a fixture-backed `blinq` alias now maps the documented public Polymarket discovery, contracts, prices, orderbooks, and price-history points plus authenticated read-only CLOB trades, alerts, and local paper orders. CLOB trade reads require explicit readonly/L2 headers. Blinq leverage, deposits, private account actions, live wallet execution, and copy trading remain explicitly unsupported until a separate official contract is published.
- Article 128 Opinion CLOB Guarded Order Boundary: scope complete for this pass; the official Opinion CLOB Python SDK is represented by an optional, fixture-backed limit/market order request path with BNB-chain validation, explicit API/private-key/multisig/RPC credential gates, shared live preflight controls, and approval checks disabled by default. The adapter never signs outside the SDK boundary, keeps copy mode simulation-first, and leaves funded account eligibility, collateral approvals, settlement, and production credentials as operator-owned external gates.
- Article 129 Opinion Token Price-History Adapter: scope complete for this pass; the official Opinion OpenAPI `token/price-history` contract is now exposed through the normalized candle-history API with documented interval and timestamp validation, fixture-backed price points, and explicit flat-OHLC/no-volume semantics because the upstream feed returns one price per timestamp.
- Article 130 Polymarket Public Price-History Adapter: scope complete for this pass; the official CLOB `prices-history` endpoint is now exposed through the normalized candle-history API with documented interval/range validation, fixture-backed points, and explicit flat-OHLC/no-volume semantics because the upstream feed returns one price per timestamp. Authenticated trade history remains a separate explicit-L2-header path.
- Article 131 IBKR Event-Contract Historical OHLC Adapter: scope complete for this pass; the official `/iserver/marketdata/history` endpoint is now exposed through the shared ForecastTrader, ForecastEx, and CME event-contract adapters with documented period/bar/start-time mapping, local range filtering, fixture-backed Last Trade OHLCV bars, and explicit authorized-session/data-entitlement gates.
- Article 132 Metaculus Forecast-History Normalization: scope complete for this pass; the official authenticated Metaculus post-detail aggregation history is normalized for binary, multiple-choice, and numeric questions through `list_candles` as irregular forecast-point snapshots with no fabricated volume or exchange-style resampling. Access-controlled history, account/data-use restrictions, and the absence of documented trading/orderbook surfaces remain explicit external or unsupported gates.
- Article 133 Limitless Historical Price Normalization: scope complete for this pass; the official Limitless historical-price endpoint is normalized through `list_candles` for documented lookback presets, with optional local timestamp bounds, flat OHLC snapshots, and complementary NO prices. The API's newest-first order and the absence of volume are preserved explicitly; live credentials, account readiness, and funded execution remain external gates.
- Article 134 Limitless Finalized Trade History: scope complete for this pass; the official public `/markets/{slug}/events` endpoint is normalized through `list_trades` with documented page/limit validation, YES/NO token filtering, numeric side mapping, ISO timestamp conversion, and six-decimal matched-size scaling. The endpoint is finalized public CLOB activity and has no timestamp query filters, so shared bounds are applied locally to the newest page; private account history and copy trading remain separate unsupported or credentialed paths.
- Article 135 Myriad Order-Book Trade History: scope complete for this pass; the official `GET /markets/:id/trades` endpoint is normalized through `list_trades` with documented outcome/pagination filters, buy/sell/split/merge side preservation, smallest-unit amount scaling, local timestamp bounds, and fixture-backed response coverage. Myriad AMM actions, wallet activity, order submission, settlement, and copy execution remain separately modeled and guarded.
- Article 136 Iowa Electronic Markets Historical Archive Adapter: scope complete for this pass; the official IEM historical price-file format is represented by an explicit inventory adapter with archive event/contract metadata, daily candle normalization, latest archived prices, alerts, local dry-run orders, host/path validation, and offline fixture coverage. Current quote pages, orderbooks, live trading, and copy trading remain unsupported until IEM publishes a stable automation API and account terms.
- Article 137 SciCast Historical Data Mart Adapter: scope complete for this pass; SciCast's official Data Mart guide and complete historical package are represented by a credentialed archive-only adapter with question discovery, binary/multiple-choice contracts, historical forecast snapshots, trade-history normalization, alerts, local paper previews, host validation, and offline fixtures. The service is retired, so current quotes, orderbooks, live trading, and copy trading remain explicitly unsupported.
- Article 138 Explicit History Capability Contract: scope complete for this pass; `trade_history` and `candle_history` are now first-class capability flags in the shared model, catalog, API payloads, and React types. Only adapters with documented fixture-backed history feeds advertise the corresponding flag; base and stub adapters fail closed with market-specific errors, and regression coverage verifies the catalog-to-implementation mapping.
- Article 139 Distribution Alias Coverage: scope complete for this pass; Robinhood Prediction Markets and Kalshi via Robinhood are fixture-backed read-only aliases over Kalshi's official public market-data API, while DraftKings Predictions is a fixture-backed read-only alias over the official Crypto.com/CDNA data surface. Discovery, contracts, prices, alerts, paper orders, and documented Kalshi history are covered where available; private brokerage/app endpoints, live execution, and copy trading remain explicitly unsupported.
- Article 140 Gemini Contract History and Orderbook Contract: scope complete for this pass; Gemini Prediction Markets now reads documented contract-level `contractOrderbooks`, `prices`, and irregular `priceHistory` points from the event-detail response instead of calling the legacy crypto `/v1/book` route. Price history is normalized as flat candles with no fabricated volume or resampling, fixture-backed tests cover bounds and malformed capability paths, and the live order boundary remains terms-gated and default-off.
- Article 141 Gemini Account Recovery and Volume Contract: scope complete for this pass; Gemini Prediction Markets now signs the documented authenticated REST contracts for active orders, filled/cancelled order history, current positions, settled positions, and per-event volume metrics. Pagination, event/symbol filters, sort values, timestamp conversion/order, path-safe identifiers, and credential redaction are fixture-tested; live account, region/KYC, terms, and funding verification remain external gates.
- Article 142 IBKR Event-Contract Execution History: scope complete for this pass; the shared ForecastTrader, ForecastEx, and CME event-contract adapter now consumes IBKR's documented `GET /iserver/account/trades` execution feed, maps execution IDs/sides/prices/sizes/timestamps to normalized trades, filters by event-contract conid and caller time bounds, and caps local results. The upstream feed only covers the selected account's current day plus six previous days; session authorization, account selection, data entitlements, and any older statement/Flex history remain external or separate documented surfaces.
- Article 143 Opinion Filled Trade History: scope complete for this pass; the Opinion adapter now uses the documented authenticated `/trade/user/{walletAddress}` feed with its official `marketId` filter, requires an explicit wallet address and API key, narrows rows to the requested outcome token, applies bounded time/limit validation, and normalizes filled executions as `MarketTrade` records. The endpoint is read-only and copy intents remain simulation-first.
- Article 144 Betfair Matched Order History: scope complete for this pass; the Betfair adapter now uses the documented authenticated `listCurrentOrders` operation with `orderProjection=ALL` and `orderBy=BY_MATCH_TIME`, filters matched rows to the requested market runner and time bounds, and normalizes average decimal odds to probability-priced `MarketTrade` records. The endpoint is read-only from this adapter's perspective; live placement remains guarded and settled-order/clearing history remains a separate future surface.
- Article 147 Limitless Authenticated Portfolio Reads: scope complete for this pass; the Limitless adapter now signs the documented HMAC GET surfaces for `/portfolio/positions`, `/portfolio/history`, and `/markets/:slug/user-orders`, supports an optional validated `x-on-behalf-of` profile for linked sub-accounts, preserves the official payloads losslessly, and covers credentials, delegation, path safety, and offline fixtures. The documented `delegated_signing` scope, linked profile, account eligibility, and any live/funded operation remain external gates.
- Article 148 Probable Activity and Price History: scope complete for this pass; the Probable adapter now consumes the documented public `/activity` and `/prices-history` CLOB contracts, requires a validated wallet address for wallet-scoped trade history and copy previews, filters activity to complete trade rows, and normalizes price points as flat OHLC candles without fabricating volume. Official fixtures, catalog capability flags, verifier requirements, and adapter tests cover the reads and simulation-first copy boundary; authenticated private trade records and live/funded evidence remain separate external gates.
- Article 149 Myriad Price-Chart Candle History: scope complete for this pass; the Myriad adapter now consumes the documented `outcomes[*].price_charts` buckets from `GET /markets/:id`, maps common API resolutions to the official 24h/7d/30d/all series without resampling, normalizes array/object OHLC(V) rows with timestamp bounds, and preserves the upstream point payload. Catalog flags, fixtures, focused tests, and docs now expose the candle surface; wallet activity, signed order submission, and live/funded evidence remain separately guarded.
- Article 150 Headless Market-Read Parity: scope complete for this pass; the CLI now exposes enabled-adapter `markets events`, `contracts`, `price`, `orderbook`, `trades`, and `candles` commands using the same configuration, capability gates, normalized serializers, and fail-closed unsupported errors as the web API. Time bounds, resolution, limits, and JSON/compact output are validated and fixture-tested, so documented adapter reads no longer require the GUI.
- Article 151 React Market-Read Parity: scope complete for this pass; the web API now exposes normalized event, contract, price, and orderbook routes alongside trade/candle history, and the React Markets view provides guarded controls and tables for all six read operations. Raw upstream payloads stay out of the shared response schema, disabled/unsupported feeds fail closed, frontend API types match backend serializers, and backend/frontend build coverage verifies the contract.
- Article 152 Context V2 Activity and Price History: scope complete for this pass; the Context adapter now consumes the documented `/markets/{id}/activity` trade feed and `/markets/{id}/prices` binary price history, validates limits and time bounds, preserves upstream YES/NO outcome semantics, derives the documented complement for the second outcome, and normalizes history as flat OHLC points without fabricated volume. Catalog capability flags, official endpoint references, fixtures, CLI/web parity, and focused tests cover the new reads; wallet activity mirroring, settlement, funded execution, and current public-live evidence remain separate external or unsupported surfaces.
- Article 153 Gemini Normalized Filled Trades: scope complete for this pass; the Gemini adapter now exposes its documented authenticated filled-order history through the shared `trade_history` contract, preserving BUY/SELL direction and mapping filled quantity, average price, stable order identifiers, and timestamp bounds into `MarketTrade` records. The catalog, README, blocker record, and fixture-backed adapter tests cover the capability; credentials, account terms, region/KYC, funding, and live execution remain external gates.
- Article 154 Gemini Account-Recovery Surface Parity: scope complete for this pass; Gemini's fixture-backed authenticated recovery methods are now exposed through a bounded `markets account <operation>` CLI command, `/api/markets/{market_id}/account/{operation}` API route, and React Markets control. Only the five documented operations (active orders, order history, current positions, settled positions, and volume metrics) are accepted; arbitrary authenticated paths and unsupported adapters fail closed. Credentials, account terms, region/KYC, funding, and live execution remain external gates.
- Article 155 Kalshi Account-Recovery Surface Parity: scope complete for this pass; Kalshi's signed portfolio reads now cover active/history orders, fills, positions, settlements, balance, and queue positions through documented endpoint paths. The CLI, API, and React surfaces share the explicit operation allow-list, strict ticker/status/count/timestamp/subaccount validation, and optional historical order/fill feeds; fixture-backed tests cover signed request routing and fail-closed invalid inputs. Coinbase and Robinhood distribution aliases remain intentionally read-only, and credentials, account eligibility, funding, and live execution remain external gates.
- Article 156 Limitless Account-Recovery Surface Parity: scope complete for this pass; the existing HMAC-authenticated Limitless positions, portfolio history, and market-scoped user-order reads are now exposed through the same explicit CLI, API, and React allow-list. `user_orders` requires the adapter's validated market slug, while the optional delegated profile is validated before it becomes an `x-on-behalf-of` header; fixture-backed tests cover dispatch, forwarding, HMAC routing, delegation, and path safety. Token scope, linked-profile eligibility, funding, and live execution remain external gates.
- Article 157 Hyperliquid HIP-4 Trade-History Parity: scope complete for this pass; the documented account-scoped `userFills`/`userFillsByTime` feed is normalized into the shared `trade_history` contract for a configured wallet, filters ordinary perpetual/spot fills from the requested synthetic HIP-4 contract, validates wallet/limit/time bounds, and preserves the existing simulation-first copy path. Fixture-backed adapter coverage, catalog capability parity, README/blocker documentation, and the shared CLI/API/React history surfaces now expose the capability; wallet ownership, retention limits, jurisdiction, funding, and externally signed live execution remain separate external gates.
- Article 158 Hyperliquid Account-Recovery Surface Parity: scope complete for this pass; the documented `openOrders`, `historicalOrders`, `clearinghouseState`, `spotClearinghouseState`, `portfolio`, and `subAccounts` Info requests are exposed through an explicit wallet/dex allow-list, shared CLI/API/React account controls, safe account limits, offline fixtures, and fail-closed invalid-operation/credential tests. Hyperliquid account identity, sub-account selection, retention, permissions, funding, and externally signed live execution remain separate external gates.
- Article 159 Opinion Account-Recovery Surface Parity: scope complete for this pass; Opinion's documented authenticated `/order`, `/order/{orderId}`, and `/positions/user/{walletAddress}` reads are exposed through the shared CLI/API/React account allow-list with bounded page/limit, numeric market/chain filters, status validation, explicit wallet resolution, and path-safe order ids. Fixture-backed wrapper payload tests cover successful history/detail/positions reads and fail-closed missing-credential/invalid-input behavior; API key/account eligibility, funding, and live execution remain external gates.
- Article 160 Betfair Cleared-Order Recovery Surface Parity: scope complete for this pass; Betfair's documented authenticated `listClearedOrders` feed is exposed through the shared CLI/API/React account allow-list with bounded settlement status, roll-up grouping, market/event/runner/bet identifiers, paging, item-description, and settled-date filters. Fixture-backed RPC tests cover payload routing and invalid identifiers/statuses, while app-key/session credentials, account eligibility, funding, and live execution remain external gates.
- Article 161 Betfair Order-Management Surface Parity: scope complete for this pass; Betfair's documented `cancelOrders`, `updateOrders`, and `replaceOrders` mutations are now available through a separate POST-only API route, a `markets manage-orders` CLI command, and a React Markets panel. Every instruction is normalized and bounded to the official schema, global cancellation requires exact confirmation, and the adapter-specific opt-in is layered on top of the shared live-safety gates. Offline fixtures and adapter/CLI/API/frontend checks cover routing and fail-closed validation; credentials, account eligibility, funding, and live verification remain external gates.
- Article 162 Kalshi V2 Order-Management Surface Parity: scope complete for this pass; Kalshi's documented V2 `cancel_order`, `batch_cancel_orders`, `amend_order`, and `decrease_order` mutations are now available through the shared POST-only API route, `markets manage-orders` CLI command, and React Markets panel. The adapter signs only fixed official paths, validates safe identifiers, price/count/reduction shapes, subaccount and exchange-index bounds, and a local batch cap, while requiring a separate `kalshi_order_management_enabled` opt-in, shared live-safety gates, and exact operator confirmation. Offline fixtures and adapter/CLI/API/frontend checks cover routing and fail-closed validation; credentials, account eligibility, funding, and live verification remain external gates.
- Article 163 Probable Account and Order-Management Surface Parity: scope complete for this pass; Probable's documented L2-authenticated open-order and order-detail reads plus fixed single-order DELETE cancellation are now available through the shared CLI/API/React allow-lists. Batch and global cancellation compose the documented fixed DELETE path with bounded, path-safe order/token identities, exact operator confirmation, a separate opt-in, and an exact global-cancel confirmation; query parameters are included in HMAC signatures. Offline fixtures and adapter/CLI/API/frontend checks cover the new paths and fail-closed safety behavior; BSC account eligibility, funding, and credentialed live verification remain external gates.
- Article 164 Hyperliquid Signed Order-Management Surface Parity: scope complete for this pass; Hyperliquid's documented `POST /exchange` `cancel`, `cancelByCloid`, `modify`, `batchModify`, and `scheduleCancel` actions are now available through the shared CLI/API/React allow-lists. The adapter forwards only complete externally signed envelopes, validates action types, HIP-4 synthetic assets, order references, modify shapes, 50-item caps, and future schedule times, while requiring a separate `hyperliquid_order_management_enabled` opt-in, shared live-safety gates, exact operator confirmation, and exact scheduled-cancel confirmation. Offline response fixtures, focused adapter tests, CLI/API forwarding, React controls, docs, and the full verifier cover the surface; wallet ownership, account eligibility, funding, and live verification remain external gates.
- Article 165 IBKR Event-Contract Account and Order-Management Parity: scope complete for this pass; ForecastTrader, ForecastEx, and CME event-contract adapters now expose documented open-order and order-status reads plus fixed cancel, cancel-all, and limit-order modification routes through explicit CLI/API/React allow-lists. Numeric order ids, LMT event-contract payloads, quantity/price/side/TIF bounds, CME manual-indicator/external-operator fields, separate opt-in, shared live gates, exact operator/global confirmations, offline fixture tests, and documentation cover the surface; authorized sessions, broker entitlements, account eligibility, confirmation replies, and funded execution remain external gates.
- Article 166 Manifold Account and Open-Limit Cancellation Parity: scope complete for this pass; the documented `/v0/me` and `/v0/bets` authenticated reads now expose account details, open-limit orders, and order history through the shared CLI/API/React allow-lists with bounded contract, cursor, and timestamp filters. The documented `POST /v0/bet/{id}/cancel` route is available as a single validated cancellation behind a separate opt-in, shared live-safety gates, exact operator confirmation, fixture-backed adapter/CLI/API coverage, and updated configuration/docs; MANA account eligibility, funding, and live verification remain external gates, while Manifold CLOB orderbooks and multi-answer live buys remain unsupported.
- Article 167 Prophet Exchange Wallet and Cancellation Parity: scope complete for this pass; the documented `/v4/mm/get_balance` and `/v4/mm/get_transactions` wallet feeds are exposed through the shared CLI/API/React account allow-lists with bounded cursor and page-size validation. Fixed Trading API `/mm/cancel_order` and `/mm/cancel_multiple_orders` paths now require the original `external_id` plus returned `order_id`, a separate order-management opt-in, shared live-safety gates, exact operator confirmation, fixture-backed adapter/CLI/API coverage, and updated configuration/docs; partner approval, credentials, account/KYC/region eligibility, and funded execution remain external gates.
- Article 168 Azuro Bettor Bet-History Parity: scope complete for this pass; the documented Azuro V3 client subgraph `v3Bets` and `liveBets` feeds are exposed through a bounded `bet_history` account operation with canonical wallet validation, environment-aware GraphQL endpoint mapping, explicit GraphQL error handling, offline fixtures, and shared CLI/API/React controls. Wallet ownership, account eligibility, funding, jurisdiction, private signing, and live execution remain external gates; copy trading and wallet mutation flows remain intentionally unsupported.
- Article 169 Myriad Account-Activity Surface Parity: scope complete for this pass; the documented public `GET /users/:address/events` feed is now exposed as a bounded `account_activity` operation through the shared CLI/API/React account surface, with canonical wallet validation, network scoping, normalized trade activities, and lossless raw-payload preservation. Myriad live order submission, cancellations, wallet ownership, account eligibility, funding, and jurisdiction remain guarded or external gates.
- Article 170 Myriad Portfolio and Market-Position Surface Parity: scope complete for this pass; the documented public `GET /users/:address/portfolio` and `GET /users/:address/markets` feeds are now exposed as bounded `portfolio` and `market_positions` operations through the shared CLI/API/React account surface. Pagination, trading-model/network/token filters, portfolio sorting/status/search controls, market-position state/topic/market-id controls, canonical wallet validation, offline fixtures, and lossless raw payloads are covered. Myriad's unsigned split/merge/redeem transaction-calldata intents, wallet ownership, account eligibility, funding, and jurisdiction remain guarded or external gates.
- Article 171 Myriad Position-Transaction Intent Parity: scope complete for this pass; the official `POST /positions/split`, `/merge`, `/redeem`, `/redeem-voided`, `/neg-risk/split`, and `/neg-risk/merge` endpoints are exposed through a preview-only CLI/API/React surface. Market/network ids, uint256 amounts, NegRisk bytes32 event ids, and outcome indexes are validated; only unsigned `{to, calldata, value}` intents are returned, and external wallet signing, settlement, account eligibility, and funding remain operator-owned gates.
- Article 172 Myriad Current Discovery Contract Parity: scope complete for this pass; Myriad discovery now uses the current official `/markets?group_by_event=true` and `/events/{id}` contracts, preserves standalone market rows through a deterministic `/markets/{id}` fallback, validates grouped discovery parameters, and keeps event/contract normalization fixture-backed. The legacy `/questions` route is no longer used for production discovery.
- Article 173 Myriad Best-Quote Price Parity and Catalog Snapshot Audit: scope complete for this pass; Myriad normalized price reads now preserve documented `bestBid`/`bestAsk` values and derive a midpoint when both are present, while the catalog/readiness documentation is synchronized to the current 68-market, 57-implemented, 11-blocked inventory and capability counts.
- Article 174 Azuro Single-Bet Wallet Copy Preview: scope complete for this pass; the documented Azuro client-subgraph `v3Bets`/`liveBets` bettor history is normalized into wallet activity for ordinary single-selection bets, including fixed-point stake/odds conversion, canonical game/condition/outcome contract ids, timestamps, transaction ids, and simulation-first paper-copy previews. Express/combo rows are rejected because they contain multiple selections, and wallet signing, funding, settlement, and live execution remain external gates.
- Article 175 Capability and Operation-Surface Audit: scope complete for this pass; every advertised core capability is now checked against a concrete adapter override rather than the fail-closed base implementation, `market_discovery` is required to pair with event listing, every declared authenticated account/order-management operation must have a concrete adapter dispatcher and CLI operation choice, verified-blocked rows remain capability-empty, and the same contract audit runs in `verify.py` and the adapter foundation tests. This prevents catalog or CLI/API/React operation surfaces from drifting ahead of real fixture-backed paths; unsupported wallet and live-entitlement boundaries remain explicit external or guarded gates.
- Article 176 Canonical Full-Catalog Support Matrix: scope complete for this pass; every catalog row now exposes one normalized support contract shared by `markets support`, `/api/markets/support-matrix`, `/api/markets/{market_id}/support`, the `/api/markets` payload, and the React Markets detail. Each public capability is classified as `supported`, `guarded`, `unsupported`, or `blocked`; authenticated account and order-management surfaces list their exact allow-listed operations; safety requirements, blocker evidence, adapter identity, and contract-audit results travel with the row. This makes all market/options coverage observable without falsely promoting private, unfunded, or upstream-blocked paths to production support.
- Article 177 Hypermind Archive Export Adapter: scope complete for this pass; Hypermind's official July 2026 forecasting report now links a documented trade-level CSV and winning-outcomes text export. A fixture-backed archive-only adapter normalizes event/contract discovery, source percentage prices, historical trades with explicit unknown direction, derived point candles, alerts, and local paper previews while keeping orderbooks, live trading, and copy trading unsupported. Official export hosts, safe identifiers, row limits, and read-only semantics are enforced; the catalog moves from 12 verified blockers to 11.
- Article 178 Support Snapshot Runtime Parity: scope complete for this pass; the canonical support matrix now publishes one aggregate implementation/operation summary through the Python/API support surfaces, and `verify.py` fails when the human `GOAL.md` capability totals drift from that matrix. Focused API/verifier tests and the full local verification suite cover the synchronization contract; unsupported and externally gated capabilities remain explicit.
- Article 179 XO Account-Trade Copy Preview Parity: scope complete for this pass; XO's documented authenticated `/trades` feed now powers a validated simulation-first copy preview with canonical market/outcome identifiers, BUY/SELL and probability/quantity bounds, required trade identity, fixture coverage, and explicit non-live semantics. XO live posting and cancellation remain separately guarded and disabled by default.
- Article 180 Matchbook and Limitless Copy Preview Parity: scope complete for this pass; Matchbook's documented authenticated aggregated matched-bets feed and Limitless's HMAC `/portfolio/history` feed now power validated simulation-first copy previews with canonical contract resolution, side/price/size/trade-identity bounds, fixture coverage, delegated-profile support where documented, and explicit non-live semantics. Live offer/order submission remains separately guarded and disabled by default.
- Article 181 Kalshi and Betfair Copy Preview Parity: scope complete for this pass; Kalshi's authenticated portfolio fills and Betfair's documented `listCurrentOrders` matched summaries now power validated simulation-first copy previews with canonical outcome/selection resolution, bid/ask or BACK/LAY direction, price/size bounds, fill/bet identity, fixture coverage, and explicit non-live semantics. Live order placement remains separately guarded and disabled by default.
- Article 182 IBKR Event-Contract Copy Preview Parity: scope complete for this pass; ForecastTrader, ForecastEx, and CME event-contract adapters now normalize the documented authenticated `/iserver/account/trades` execution fields (execution id, event conid, B/S side, price, whole filled size) into validated simulation-first paper previews. The shared path preserves raw execution payloads, enforces ForecastEx buy-only semantics, includes fixture-backed invalid-input coverage, and never calls an IBKR order endpoint; authorized sessions, broker entitlements, and funded execution remain external gates.
- Article 183 Gemini Filled-Order Copy Preview Parity: scope complete for this pass; Gemini's documented authenticated filled-order history now powers a validated simulation-first copy preview with event/instrument identity, BUY/SELL direction, filled quantity, execution price, timestamp, and order id. The adapter preserves the raw order, rejects non-filled or incomplete rows, and never calls the live order endpoint; Trader-role access, terms acceptance, account eligibility, funding, and live verification remain external gates.
- Article 184 SX Bet Authenticated-Fill Copy Preview Parity: scope complete for this pass; SX Bet's documented API-key `/fills-v3` feed and account fill channel now power a validated BUY-only simulation-first copy preview with market/outcome identity, backed-outcome direction, protocol-odds normalization, base-unit stake conversion, fill identity, and lifecycle-status validation. Parlay/quarter-line rows and failed fills are rejected because the shared contract model supports only a single two-outcome selection; live bets remain separately guarded and disabled by default.
- Article 185 Drift BET Public Fill-History Parity: scope complete for this pass; the official newest-first `/market/{symbol}/predictions` feed now normalizes stable fill ids, taker LONG/SHORT direction, protocol-precision base/quote amounts, timestamps, and complementary YES/NO semantics into bounded public trades. Derived OHLCV candles retain source fill ids, are explicitly marked as derived, and document the upstream 20-record page and 31-day retention limits. Explicit symbol inventory, binary DLOB depth, wallet signing, collateral, settlement, live execution, and copy trading remain separately unsupported or external gates.
- Article 186 Signed Live-Order Intent Binding and Alias Isolation: scope complete for this pass; Context V2, Probable, Predict.fun, and Myriad now reject externally signed live payloads unless their complete market/outcome token, side, price, and size agree with the request that passed shared preflight limits. Context requires a well-formed signed struct, Probable resolves the canonical market token and uses the documented 18-decimal BNB mainnet wire scale (failing closed elsewhere unless a trusted scale is configured), Predict.fun admits only fully bound LIMIT semantics, and Myriad binds the payload to an explicit trusted network. Blinq no longer exposes inherited Polymarket private-account getters, recovery dispatch, or wallet-readiness metadata; its optional L2 credentials are restricted to read-only trade history. Regression tests prove mismatched payloads and alias-private operations cannot reach transport.
- Article 187 Omen/Gnosis FPMM Trade-History Parity: scope complete for this pass; the official Omen subgraph `FpmmTrade` and `Token.scale` entities now power bounded public BUY/SELL history for Omen and its Gnosis market alias, with strict FPMM/outcome/collateral identity, timestamp, amount, probability, uint256, and per-collateral scale validation. OHLCV candles are derived from the same immutable trade tape, retain source trade ids, and fail closed when different prices share a second because the subgraph exposes no block/log ordering key; CLOB depth, wallet activity mirroring, collateral approval, settlement, and funded execution remain separately unsupported or external gates.
- Article 188 Price-Triggered Alert Capability Integrity: scope complete for this pass; the static capability audit now rejects any adapter that advertises shared alerts without a normalized price operation. Reality.eth's question-only adapter no longer claims price alerts, and its catalog, matrix, and documentation explicitly preserve lifecycle metadata while leaving prices and alerts unsupported. This closes a concrete false-positive where the support matrix claimed alerts but the alert refresh route always raised an unsupported-price error.
- Article 189 Consumer-Brand Venue-Mapping Integrity: scope complete for this pass; PrizePicks' first-party materials identify Kalshi as its event-contract provider and Underdog's legal center identifies CDNA as its exchange, but neither publishes a machine-readable mapping from the venue-wide catalog to the distributor's current board. Both brands therefore remain verified blocked rather than relabeling every upstream contract; promotion requires an official mapping/filter plus fixture-backed negative membership tests and a separately documented account/order contract for private automation.
- Article 190 Seer Generic-Market Chart-History Parity: scope complete for this pass; the official chain-qualified `market-chart` route now powers bounded flat candle history for Seer Generic markets. The adapter follows Seer's own sorted outcome/collateral pool logic with asymmetric reciprocal price-field selection, validates every pool pair before normalization, preserves the upstream mixed hourly and swap timestamps without fabricated OHLC movement or volume, and avoids decimal-unsafe `sqrtPrice` reconstruction. Futarchy chart semantics remain fail-closed; official fixtures, capability parity, malformed-pair tests, and documentation cover the new read surface.
- Article 191 MetaDAO Bounded Public Spot-History Parity: scope complete for this pass; the official Futarchy external API's public spot-swap event surface now powers bounded recent trade history and derived OHLCV candles. Every call is capped by a configurable Solana slot window, per-call scan/request window count, streamed response-byte limit, and total decoded-row count; derived candles retain source trade ids and are explicitly marked as derived. Calls without timestamp bounds intentionally return only the bounded recent slice and may return fewer rows or omit the earliest partial candle bucket; every retained candle from a truncated scan is explicitly marked partial with bounded-slot coverage. Because estimated Solana block times are not treated as monotonic, timestamp-complete requests must reach the pair's creation slot and verify its declared creation swap within the configured scan horizon or fail closed; arbitrary full-history completeness is not claimed. Orderbook depth, wallet signing, settlement, funded execution, and copy trading remain unsupported or external gates.

## Active continuation goals

The earlier repository-controlled baseline is covered by the strict local
verification set, but the active full-coverage goal continues: every catalog
market and every advertised feature still needs an official, fixture-backed
implementation before it can be called supported. The remaining evidence
gates below are external operational work, and blocked markets must not be
reinterpreted as implemented unless their official-access constraints are
solved.

- Real Polymarket credential/funded execution remains an optional external
  operation. It requires user-supplied credentials through local environment
  variables or an approved secret mechanism, an eligible region/KYC/account
  state, a safe token/price/size allow-list, and explicit approval for each
  funded live check.

## Non-negotiable remaining blockers

Some `No` cells may remain permanently correct. A market capability stays blocked when any of these is true:

- No official API, SDK, export, or documented protocol contract exists.
- Access requires unavailable paid entitlements, private account permissions, region/KYC eligibility, or broker/exchange approval.
- Only private mobile/web consumer endpoints are visible.
- Automation would require scraping authenticated sessions, bypassing controls, or storing unsafe credentials.
- Copy trading would require account activity data that the platform does not officially expose.
