# Blockers

Last reviewed: 2026-08-23

This file documents why each market is either implemented, guarded, or stubbed. The project rule is: use official APIs, SDKs, documented endpoints, or protocol contracts only. Do not scrape private data, bypass authentication, or enable live trading/copy trading by default.

## Summary

- Polymarket, Kalshi, PredictIt, Crypto.com Predict/CDNA, Nadex/CDNA, Fanatics Markets/CDNA, FanDuel Predicts/OG, Blinq/Polymarket, Manifold, Metaculus, Good Judgment Open, Hypermind archive, SciCast archive, Myriad, Opinion, Gemini, Predict.fun, XO, Betfair, Limitless Exchange, SX Bet, Azuro, Augur, Omen, Gnosis Prediction Markets, Zeitgeist, Zeitgeist SDK / Markets, Zeitgeist Prediction Pools, Reality.eth Markets, Xmarket, Probable, Matchbook, DFlow, Context V2, Smarkets, Thales Market, MetaDAO, Seer, Hyperliquid, Trueo, Frenzy Finance, Hedgehog Markets, IBKR ForecastTrader, ForecastEx, CME event contracts, Prophet Exchange, PRDT Finance, Zetarium World, Lamas Finance, and BetMGM's partner Sports API are implemented adapters today.
- All other markets are present in config and GUI through verified-blocked adapters with exact blocker reasons.
- Live trading remains disabled by default for every market.
- Credentials must stay in `.env`, shell environment variables, OS keychain tooling, or external credential files. They must not be stored in `data/config.json`.

## 2026-05-26 Re-Audit Notes

Article 35 re-checked verified-blocked markets against current official pages and docs. No verified-blocked market was promoted in that pass because every candidate still failed at least one project rule: official production API availability, account/entitlement access, market-data licensing, documented order semantics, wallet/contract safety, or offline fixture coverage. Subsequent reviews promoted Drift BET, Frenzy Finance, Hedgehog Markets, Coinbase Prediction Markets, and Nadex/CDNA after their official public/contract surfaces and offline fixture coverage were validated. Drift inventory remains explicitly configured because its public OpenAPI contract has no stable market-list endpoint; Frenzy is limited to configured grid specs, `BetSettled` history, and dry-run intents because active quotes/oracle acknowledgements are off-chain; Hedgehog now includes a guarded externally signed `DepositV1` submission boundary while keeping signing and settlement operator-owned; Coinbase is limited to the public Kalshi venue alias because Coinbase-specific account execution APIs are not published; Nadex is limited to the documented Crypto.com Predictions API prediction-event data surface because account trading, DCM/FIX depth, and knock-out products are not automated.

Notable re-audit outcomes:

- `context_v2`: current Context v2 API documentation now publishes market, price, orderbook, activity, price-history, and signed-order endpoints. The adapter also normalizes activity trades and binary price-history points from offline fixtures. Activity preserves the upstream outcome label rather than inventing a BUY/SELL direction; history uses flat OHLC points with no fabricated volume. The adapter is guarded because API keys, wallet setup, chain eligibility, and current account/product availability remain external evidence gates.
- Historical review note (2026-05-26): `hyperliquid` documentation then described outcome asset IDs and `outcomeMeta` as testnet-only. The current HIP-4 review has promoted it to implemented with fixture-backed public metadata/orderbook coverage; live settlement and funded execution remain external gates.
- `thales_market`: official Thales docs describe API endpoints and contract integration. The adapter now covers public AMM reads, quote-backed paper orders, and a guarded externally signed transaction boundary; chain-specific AMM selection, collateral approval, wallet signing, and settlement remain operator-owned evidence gates.
- Historical note (superseded by the 2026-08-17 promotion): official Smarkets API terms still require account verification, application approval, a setup fee, pre-approved markets/limits, and written approval before platform data can be shared or distributed. Those are external eligibility/data-use gates for the implemented, fixture-backed adapter; they no longer make the repository-controlled read/order contract itself a verified blocker.
- `cme_prediction_markets`: the official IBKR Client Portal event-contract workflow now provides the broker/order path used by the adapter; CME data licensing and account entitlements remain external gates.
- Robinhood Prediction Markets and DraftKings Predictions have fixture-backed read-only public venue aliases, while their private account/order automation remains unsupported. PrizePicks and Underdog remain verified blocked: their first-party materials identify Kalshi and CDNA as upstream venues, but no official machine-readable mapping limits the venue-wide catalog to either distributor's current board, and neither brand publishes an account/order automation API for this app. BetMGM's official Sports API separately publishes fixture, market, option, and price data behind partner credentials, while order/account automation remains unsupported. Fanatics Markets is represented by the documented read-only CDNA alias below. Probo's official site states that operations are closed, so it is fail-closed rather than represented as an active integration target. Sporttrade states that all wagering ceased on 2026-05-25; both Sporttrade catalog entries remain verified blocked because the production product is no longer active.

## 2026-07-15 Incremental Re-Audit

Crypto.com Predict/CDNA was promoted from verified blocked to implemented after Crypto.com published the official Predictions Market Data API. The adapter uses the documented anonymous read-only event, search, contract, and price endpoints. It supports alerts and local dry-run paper orders, accepts an optional licensed `X-API-Key`, and records the published anonymous rate limits. Full orderbook depth, account activity, live orders, and copy trading remain unsupported because the official API is market-data-only. Personal non-commercial reads are anonymous; redistribution, commercial use, or model training requires a Market Data License.

## 2026-08-17 Incremental Re-Audit

Matchbook was promoted from verified blocked after its current developer centre was rechecked. The official API publishes event/market discovery, decimal-odds prices, runner offers/orderbooks, session login, `/v2/offers` submission, authenticated settled/current-bet reports, current-offer reads, wallet balance, and account profile. The adapter keeps reads fixture-backed, converts decimal odds to probabilities, requires a Matchbook session for account/live reads, and keeps live execution disabled unless the shared safety gates are explicitly enabled. Account eligibility, regional terms, API fees/limits, and real funded execution remain external evidence gates.

DFlow was promoted from verified blocked after its official Metadata/Trade API and current prediction-market recipe were rechecked. The adapter covers nested event/market discovery, settlement-account outcome mints, probability prices, public outcome-mint trade history, complementary YES/NO orderbooks, dry-run trade requests, and guarded submission of a wallet-signed transaction through an explicitly configured Solana RPC. API-key, Proof/KYC, jurisdiction, wallet, funded-balance, redemption, and asynchronous order-status evidence remain external gates.

Frenzy Finance was promoted on 2026-08-17 after its official Base/Base Sepolia contract deployments and `BetSettled` event shape were validated with offline fixtures. The adapter supports explicitly configured price-range grid specs, settlement history, alerts, and dry-run EIP-712 `BetIntent` previews. Active quotes/oracle acknowledgements remain off-chain and undocumented for this integration; wallet signing, orderbooks, live execution, and copy trading therefore remain fail-closed.

Coinbase Prediction Markets was promoted on 2026-08-17 as a read-only alias over Kalshi's official venue API. Coinbase's launch and help documentation identify Kalshi as the market-flow/outcome venue; the adapter therefore covers public discovery, contracts, prices, orderbooks, alerts, and local paper orders while keeping Coinbase-specific live execution and copy trading disabled.

FanDuel Predicts was promoted on 2026-08-21 as a read-only alias over the official Crypto.com OG Prediction Markets/CDNA data surface. FanDuel's current product and press-release pages state that its contracts are listed by both CME Group and Crypto.com derivatives exchanges; this adapter covers the OG/CDNA portion through the documented Predictions API, while CME-listed contracts remain available through the CME/IBKR adapter. FanDuel account execution, orderbook depth, and copy trading remain unsupported.

PRDT Finance's official legacy `Prediction` and `PredictionFactory` Solidity interfaces are represented by a fixture-backed, explicit-configuration adapter. It reads the configured Prediction contracts over EVM JSON-RPC, exposes the current bull/bear round and pool-share prices, and permits local paper intents only before the round lock timestamp. PRDT still does not publish a stable current factory/indexer inventory, so operators must supply and verify each deployed Prediction address; the adapter never guesses deployments or scrapes the consumer app. CLOB depth, wallet signing, live execution, settlement, and copy trading remain unsupported.

Context V2 was promoted from verified blocked after its current API reference was rechecked. The adapter covers market discovery, outcome-token contracts, price summaries, outcome-specific orderbooks, documented activity trades, binary price history, dry-run order payloads, and guarded submission of an externally signed order using a Context API key. Activity keeps Context's outcome-side semantics (YES/NO) and price history is represented as flat OHLC snapshots without synthetic volume. API-key, wallet approval/deposit, chain eligibility, settlement, and funded execution evidence remain external gates.

Prophet Exchange was promoted from verified blocked on 2026-08-17 after its official Market Data and Trading API references were rechecked. The adapter covers tournament/event/market discovery, nested and flat selection payloads, decimal-odds probability normalization, available-quantity quote books, local paper orders, and guarded authenticated `submit_order` requests using the documented market-maker shape. Partner approval, account/KYC/region eligibility, credentials, and funded execution remain external gates; the documented order path is BUY-only in this adapter.

Zetarium World and Lamas Finance were promoted on 2026-08-21 after their documented contract surfaces were rechecked. Zetarium uses the reviewed BSC `PredictionMarket` deployment; Lamas uses the official Solana Anchor `PricePredict` and `UpOrDown` programs. Both adapters are fixture-backed for discovery, prices, alerts, and paper intents, and accept only reviewed externally signed live transactions. Production deployment inventory, signing, settlement, and funded execution remain operator-owned gates; neither adapter exposes an orderbook or copy-trading surface.

Smarkets was promoted from verified blocked after its current v3 REST documentation was rechecked. The adapter covers event/market/contract discovery, quote-based orderbooks and prices, dry-run orders, and guarded session-authenticated order submission. Smarkets API application approval, written data-use permission, account eligibility, regional terms, and funded execution remain external gates.

Thales Market was promoted from verified blocked after its current official REST integration documentation and public Postman contract were rechecked. The adapter covers grouped/ungrouped AMM market discovery, positional contracts, public prices and buy-quote fallback, and local paper orders across the documented Optimism, Polygon, Arbitrum, and Base network IDs. Thales remains AMM-based rather than CLOB-based; wallet signing, collateral approval, settlement, and live on-chain execution stay explicitly disabled until contract transaction safeguards and funded evidence are added.

MetaDAO was promoted from verified blocked after its current official Futarchy DEX API documentation and public repository contract were rechecked. The adapter covers public `/api/tickers` DAO/token-pair discovery, bid/ask/last-price normalization, alerts, and local paper orders. The API is a read-only market-data feed; orderbook depth, Solana wallet signing, settlement, and live execution remain explicitly unsupported.

Seer was promoted from verified blocked after its current official serverless API documentation was rechecked. The adapter covers chain-qualified `markets-search` discovery, `get-market` hydration, outcome prices, official `market-chart` Generic-market history, alerts, local paper orders, and guarded forwarding of externally signed DEX transactions. Chart points follow Seer's own sorted outcome/collateral pool orientation, remain flat OHLC points without fabricated volume or resampling, and fail closed for Futarchy semantics. CLOB depth and copy trading remain unsupported; wallet signing, collateral approval, settlement, funded execution, and venue eligibility remain external gates.

Hyperliquid was promoted from verified blocked after its current HIP-4 outcome-market API documentation was rechecked. The adapter covers public `outcomeMeta` discovery, encoded outcome sides, `l2Book` prices/orderbooks, local paper orders, and guarded submission of complete externally signed exchange payloads. Wallet-scoped `userFills`/`userFillsByTime` history and the documented `openOrders`, `historicalOrders`, `clearinghouseState`, `spotClearinghouseState`, `portfolio`, and `subAccounts` account reads are exposed through the shared allow-listed account surface. Private-key handling, wallet signing, settlement, and funded execution remain external gates.

Trueo was promoted from verified blocked after its official Base deployment and contract documentation were rechecked. The adapter reads `TruthMarketManager` active markets, immutable `TruthMarket` fields, and documented Uniswap V3 pool prices through JSON-RPC, and supports alerts, local paper orders, and guarded submission of an externally signed transaction. CLOB depth, wallet signing, settlement, and funded execution remain external gates.

Zeitgeist SDK / Markets was promoted from verified blocked after confirming that the official SDK market-fetching contract uses the same documented Subsquid/indexer GraphQL market and asset schema. It has an explicit adapter class, configuration key, and independent fixtures so the catalog target is not silently treated as the primary Zeitgeist entry. It now shares the primary adapter's guarded, externally signed HybridRouter extrinsic boundary; signing, approvals, settlement, and CLOB depth remain external or unsupported.

IBKR ForecastTrader, ForecastEx, and CME event contracts were promoted after validating the official IBKR Client Portal event-contract workflow. The adapters use documented search/category-tree, strikes, instrument-info, marketdata/snapshot, historical OHLC, account/trades, and account/orders routes, map conids and YES/NO rights deterministically, provide simulation-first copy previews from complete authenticated executions, and require an authorized session, account/data entitlements, and explicit live-order gates.

Gnosis Prediction Markets was promoted after confirming that the current Gnosis prediction-market surface is Omen/Presagio. The adapter is an explicit market-scoped alias over the documented Omen FixedProductMarketMaker subgraph schema, with independent configuration, health diagnostics, fixtures, discovery, prices, public FPMM trades, per-collateral indexed scaling, bounded derived candles, alerts, paper-order coverage, and a guarded externally signed FPMM transaction boundary. Candle derivation rejects same-second trades with different prices because the subgraph does not expose block/log ordering. CLOB depth, collateral approval, settlement, and copy trading remain unsupported by the underlying AMM surface.

Zeitgeist Prediction Pools was promoted after validating that the documented market-fetching GraphQL shape includes pool identifiers, pool status, weights, and asset prices. The adapter is an explicit pool-scoped alias with a separate endpoint key, pool-presence validation, independent fixtures, discovery, prices, alerts, paper-order coverage, and guarded forwarding of an externally signed HybridRouter extrinsic with reviewed pool metadata. Pool accounting, settlement, wallet signing, and CLOB depth remain unsupported.

Reality.eth Markets was promoted on 2026-08-17 after validating the official question subgraph schema. The adapter is intentionally read-only: it exposes question discovery, response-option listing, and lifecycle status through a configured GraphQL endpoint. Reality.eth does not expose traded prices, price-triggered alerts, CLOB depth, paper orders, or exchange execution; those capabilities remain unsupported by the underlying oracle/question protocol.

## 2026-08-21 Fresh blocked-market audit

The remaining blocked entries were checked again against their first-party
surfaces. Robinhood and DraftKings now expose active consumer prediction-market
pages, but neither publishes a stable third-party market-data/order API or an
automation permission contract. Nadex was separately promoted to a read-only
CDNA alias after its official Crypto.com Predictions API surface was validated;
this does not cover Nadex account automation, DCM/FIX depth, or knock-out products.
Iowa Electronic Markets now has an archive-only adapter for its official
historical price files; current quote pages, orderbooks, live trading, and copy
activity remain unsupported because no stable automation API is documented.
Hypermind was promoted to a narrow archive-only adapter on 2026-08-23 after
its official July 2026 forecasting report began linking a documented trade CSV
and winning-outcomes export. The adapter uses those files for historical
discovery, prices, trade history, derived point candles, and paper previews;
it does not claim orderbooks, live execution, or copy trading.
Good Judgment Open is now covered through the documented Cultivate Forecasts
REST contract. The instance URL and credentialed account eligibility remain
operator-supplied and must be validated before use.

Blinq's official site explicitly says its leverage layer trades Polymarket
markets. It is therefore represented by a read-only Polymarket data alias only;
Blinq leverage, deposits, private account actions, live wallet execution, and
copy trading remain unsupported. Levr Bet's public whitepaper describes its
tokenized sports model but does not publish a stable API, deployment inventory,
or reviewed contract schema for safe fixture-backed integration. Dexsport's current official site lists
prediction markets and smart-contract settlement, but its public documentation
does not provide a stable market-data API, deployment inventory, or reviewed
prediction-contract ABI. SynStation's official whitepaper is conceptual and
does not publish a stable deployment inventory, market-data endpoint, or order
contract. Fact Machine and the remaining consumer products likewise still lack
a validated official integration surface. No entry
was promoted through web scraping, private endpoints, or guessed deployments.

## 2026-08-22 Nadex/CDNA promotion note

Nadex's current rules identify the exchange group as Crypto.com Derivatives
North America (CDNA), and Crypto.com publishes a documented Predictions Market
Data API with public event, contract, and price reads. The new `nadex` adapter is
therefore a read-only alias over that official data surface, with fixture-backed
discovery, contracts, prices, alerts, and local paper orders. This promotion is
deliberately limited to prediction-event data: it does not claim Nadex account
automation, DCM/FIX market-data depth, knock-out products, live orders, or copy
trading. Anonymous reads remain subject to the published personal-use and rate
limits; licensed API access is required for commercial redistribution or model
training.

## 2026-08-22 Blinq/Polymarket data-alias promotion note

Blinq's official product page says that its leverage layer trades Polymarket
markets, and its predictions page links that market experience. Because Blinq
does not publish a separate public market-data, leverage, wallet, or execution
API, the new `blinq` adapter is a narrow alias over Polymarket's documented
public data surface. It provides fixture-backed discovery, contracts, prices,
orderbooks, public price-history points, and authenticated read-only CLOB trade
history (explicit readonly/L2 headers are required), plus alerts and local
paper orders for the underlying markets. It does not automate Blinq leverage,
deposits, private account actions, live wallet execution, or copy trading.

## 2026-08-22 BetMGM promotion note

BetMGM's official partner Sports API publishes a versioned OpenAPI contract
for fixture discovery, market/option listing, and decimal/fractional/US odds.
The new adapter keeps the partner AccessId and AccessIdToken in the normal
credential resolver, normalizes odds to implied probabilities, and provides
fixture-backed local paper orders. The documented API has no order or account
activity endpoint, so live betting and copy trading remain unsupported.

## 2026-08-23 Good Judgment Open promotion note

Good Judgment Open is now represented through the documented Cultivate
Forecasts REST contract. The adapter covers question and answer discovery,
answer probabilities, irregular prediction-set history, local paper previews,
and a guarded forecast-submission boundary. The configured instance URL,
credentials, account/region eligibility, and any live evidence remain
operator-owned gates; submission is disabled by default and is not presented
as exchange order execution.

## Market Blockers

| Market id | Current adapter | Blocker | Required before full support | Reference |
| --- | --- | --- | --- | --- |
| `polymarket` | Implemented | Official Gamma/Data/CLOB/Bridge/Relayer/WebSocket wrappers, public CLOB price history normalized as flat OHLC points, authenticated CLOB trade history through explicit operator-supplied L2 headers, guarded live trading, wallet activity, copy simulation, MDD analytics, and local safety workflows are implemented. | Keep credentialed and funded verification separate from local tests; require explicit region/KYC/account state, signed headers/credentials, allow-listed tokens, hard caps, and immediate cancel for any live audit. | [Polymarket API docs](https://docs.polymarket.com/api-reference/introduction), [Polymarket price history](https://docs.polymarket.com/api-reference/markets/get-prices-history), [Polymarket CLOB trades](https://docs.polymarket.com/api-reference/trade/get-trades), [Polymarket live runbook](POLYMARKET_CREDENTIAL_RUNBOOK.md) |
| `kalshi` | Implemented | Official market/event discovery, contracts, prices/orderbooks, public trade history, normalized OHLCV candlesticks, alerts, dry-run orders, signed account recovery, simulation-first copy previews from complete authenticated portfolio fills, guarded live placement, and V2 `cancel_order`, `batch_cancel_orders`, `amend_order`, and `decrease_order` mutations are implemented with fixed documented Kalshi REST paths. Portfolio fills are mapped from documented outcome/book sides to canonical YES/NO BUY/SELL paper intents and never submit live orders automatically. Live trading and order management remain guarded because they require an eligible account, API key ID, RSA private key material, separate `kalshi_order_management_enabled=true`, shared live-safety acknowledgement, and exact operator confirmation. | Keep live trading and order management disabled by default, keep credentials out of config, cap batch cancellation locally, keep copy previews simulation-first, and review every mutation before submission. | [Kalshi market data docs](https://docs.kalshi.com/getting_started/quick_start_market_data), [Kalshi trades](https://docs.kalshi.com/api-reference/market/get-trades), [Kalshi candlesticks](https://docs.kalshi.com/api-reference/market/get-market-candlesticks), [Kalshi authenticated requests](https://docs.kalshi.com/getting_started/quick_start_authenticated_requests), [Kalshi fills](https://docs.kalshi.com/api-reference/portfolio/get-fills), [Kalshi order direction](https://docs.kalshi.com/getting_started/order_direction), [Kalshi create order V2](https://docs.kalshi.com/api-reference/orders/create-order-v2), [Kalshi cancel order V2](https://docs.kalshi.com/api-reference/orders/cancel-order-v2), [Kalshi batch cancel V2](https://docs.kalshi.com/api-reference/orders/batch-cancel-orders-v2), [Kalshi amend order V2](https://docs.kalshi.com/api-reference/orders/amend-order-v2), [Kalshi decrease order V2](https://docs.kalshi.com/api-reference/orders/decrease-order-v2) |
| `predictit` | Implemented | Official public market-data endpoints are implemented for read-only market/event discovery, contract listing, top-of-book price reading, alerts, and dry-run paper orders. Full orderbook depth, live trading, and copy trading remain unsupported because the documented public API is read-only and no official automated trading/account activity API is published for this adapter. | Keep live trading disabled, keep paper mode dry-run only, keep using documented market-data endpoints, and add trading only if PredictIt publishes an official automation API and terms that permit it. | [PredictIt market-data API](https://predictit.freshdesk.com/support/solutions/articles/12000001878-does-predictit-make-market-data-available-via-an-api-), [PredictIt all market data](https://www.predictit.org/api/marketdata/all), [PredictIt](https://www.predictit.org) |
| `robinhood_prediction_markets` | Implemented | Read-only alias over Kalshi's official public market-data API, covering discovery, contracts, prices/orderbooks, public trades, candles, alerts, and local paper orders. Robinhood account execution and private endpoints remain unsupported. | Keep the alias read-only; require a documented Robinhood order/activity API plus account/region/KYC evidence before adding execution. | [Robinhood newsroom](https://robinhood.com/us/en/newsroom/robinhood-prediction-markets-hub/), [Kalshi market-data docs](https://docs.kalshi.com/getting_started/quick_start_market_data) |
| `fanatics_markets` | Implemented | Fanatics documents the product as an intermediary experience whose event contracts are listed and priced on CDNA. The adapter uses CDNA's official anonymous Predictions API for discovery, contracts, prices, alerts, and local dry-run orders. Fanatics-specific order automation, orderbook depth, live trading, and copy trading remain unsupported. | Keep the alias read-only, keep live/copy trading disabled, require Fanatics account/KYC/region checks for any future trading work, and obtain a supported Fanatics/CDNA order API before adding execution. | [Fanatics Markets availability](https://fanaticsmarkets.com/legal/availability), [Fanatics launch](https://crypto.com/en/company-news/fanatics-launches-fanatics-markets-the-first-fan-led-prediction-market-at-the-intersection-of-sports-finance-and-culture-through-a-strategic-partnership-with-cryptocom), [Crypto.com Predictions API](https://data.crypto.com/docs), [Crypto.com partners](https://crypto.com/en/prediction/partners) |
| `draftkings_predictions` | Implemented | Read-only alias over the official Crypto.com/CDNA Predictions API for DraftKings-listed event discovery, contracts, prices, alerts, and local paper orders. CME-listed contracts remain covered by `cme_prediction_markets`; DraftKings account execution and copy trading remain unsupported. | Keep the alias read-only; honor the CDNA data license/rate limits and require a documented DraftKings order/activity API before adding execution. | [DraftKings/CDNA announcement](https://ir.aboutdraftkings.com/files/doc_news/2026/DraftKings-Expands-Prediction-Markets-Catalog-in-Deal-With-Crypto-com.pdf), [DraftKings Predictions](https://predictions.draftkings.com/en), [Crypto.com Data API](https://data.crypto.com/docs) |
| `ibkr_forecasttrader` | Implemented | Official IBKR Client Portal event-contract workflow maps ForecastTrader/ForecastEx category discovery, conids, top-of-book snapshots, historical Last Trade OHLC bars, recent account executions from `GET /iserver/account/trades`, simulation-first copy previews, account order/status reads, guarded cancel-all/cancel/modify mutations, alerts, paper orders, and guarded limit orders. Execution history is limited by the upstream contract to the current day and six previous days and is filtered locally by conid/time bounds; copy previews require execution id, conid, B/S side, price, and whole filled size and never submit live orders. | Keep `IBKR_SESSION_COOKIE`/`IBKR_ACCESS_TOKEN` and `IBKR_ACCOUNT_ID` outside config, require account/data entitlements, keep `ibkr_submit_live_orders` and `ibkr_order_management_enabled` disabled by default, keep copy previews simulation-first, and treat response confirmations as manual gates. | [IBKR event contracts](https://www.interactivebrokers.com/campus/ibkr-api-page/event-contracts/), [IBKR historical OHLC](https://www.interactivebrokers.com/docs/web-api/api-reference/trading/trading-market-data/get-md-history), [IBKR trades](https://www.interactivebrokers.com/docs/web-api/v1/endpoints/order-monitoring/trades), [IBKR account orders](https://www.interactivebrokers.com/docs/web-api/v1/endpoints/orders/cancel-order), [IBKR modify order](https://www.interactivebrokers.com/docs/web-api/v1/endpoints/orders/modify-order) |
| `forecastex` | Implemented | ForecastEx contracts are mapped through the documented IBKR `FORECASTX` search, strikes, instrument-info, snapshot, historical OHLC, recent account-execution and simulation-first copy-preview fields, account order/status, and fixed cancel/modify routes. Execution history is limited by IBKR to the current day and six previous days; ForecastEx positions are buy-only and the adapter rejects sell orders, including copy previews. | Keep an authorized IBKR session and ForecastEx permissions required, keep paper mode default, keep copy previews simulation-first, keep `ibkr_order_management_enabled` disabled by default, and validate live confirmations manually. | [IBKR event contracts](https://www.interactivebrokers.com/campus/ibkr-api-page/event-contracts/), [IBKR historical OHLC](https://www.interactivebrokers.com/docs/web-api/api-reference/trading/trading-market-data/get-md-history), [IBKR trades](https://www.interactivebrokers.com/docs/web-api/v1/endpoints/order-monitoring/trades), [IBKR cancel order](https://www.interactivebrokers.com/docs/web-api/v1/endpoints/orders/cancel-order), [ForecastEx markets](https://forecastex.com/markets/) |
| `cme_prediction_markets` | Implemented | CME event contracts are discovered through IBKR index/EC records and `FOP` instrument-info, with documented snapshot prices, top-of-book levels, recent account executions and simulation-first copy previews, historical OHLC bars, account order/status reads, fixed cancel/modify routes, alerts, paper orders, and guarded orders. Execution history is limited by IBKR to the current day and six previous days; copy previews validate the execution id, conid, B/S side, price, and whole filled size before creating a local paper intent. | Keep CME/IBKR market-data and trading entitlements required, keep paper mode default, keep copy previews simulation-first, require explicit `ibkr_manual_indicator` and `ibkr_external_operator` values for CME mutations, and use only the documented broker route. | [IBKR event contracts](https://www.interactivebrokers.com/campus/ibkr-api-page/event-contracts/), [IBKR historical OHLC](https://www.interactivebrokers.com/docs/web-api/api-reference/trading/trading-market-data/get-md-history), [IBKR trades](https://www.interactivebrokers.com/docs/web-api/v1/endpoints/order-monitoring/trades), [IBKR cancel order](https://www.interactivebrokers.com/docs/web-api/v1/endpoints/orders/cancel-order), [CME event contracts](https://www.cmegroup.com/markets/event-contracts.html) |
| `nadex` | Implemented | Read-only Nadex/CDNA prediction-event discovery, contract listing, bid/ask/mid/probability reads, alerts, and local paper orders use the official Crypto.com Predictions Market Data API. Nadex account trading, DCM/FIX depth, knock-out products, live orders, and copy trading remain unsupported. | Keep the alias read-only, keep paper mode default, honor anonymous rate limits and Market Data License terms, and add any execution only through a documented Nadex/CDNA order contract with fixtures and explicit safety gates. | [Nadex products](https://www.nadex.com/product-market/), [Nadex rules](https://www.nadex.com/rules/), [Crypto.com Predictions API](https://data-api.crypto.com/docs), [Crypto.com quickstart](https://data.crypto.com/quickstart) |
| `crypto_com_predict` | Implemented | Official anonymous read-only event discovery/search, contract listing, and bid/ask/mid/probability reads are implemented, with alerts and local dry-run paper orders. Full orderbook depth, live orders, and copy trading remain unsupported because the official API is market-data-only. | Keep live trading disabled, honor anonymous rate limits, send an API key only for a licensed tier, and require a Market Data License for redistribution, commercial use, or model training. | [Crypto.com Predictions API](https://data.crypto.com/docs), [Crypto.com Predictions quickstart](https://data.crypto.com/quickstart) |
| `hyperliquid` | Implemented | Official Hyperliquid HIP-4 `outcomeMeta`, encoded outcome assets, `l2Book`, `candleSnapshot`, and public `userFills`/`userFillsByTime` responses are mapped to discovery, prices, orderbooks, normalized wallet-scoped trade/candle history, alerts, paper orders, simulation-first wallet-copy previews, and guarded externally signed exchange payloads with fixture-backed validation. Documented account recovery reads cover `openOrders`, `historicalOrders`, perpetual/spot account state, `portfolio`, and `subAccounts` through a bounded wallet/dex allow-list. Private-key handling, wallet signing, settlement, and funded execution remain external gates. | Keep paper mode as default, require an explicit `HYPERLIQUID_ACCOUNT_WALLET` (or the configured trade/activity wallet fallback) for account reads, filter wallet fills to synthetic `#<encoding>` HIP-4 outcome assets, validate documented candle intervals, history bounds, account limits, and DEX names, require complete externally signed payloads, enforce live gates, and add funded evidence only through a dedicated reviewed environment. | [Hyperliquid asset IDs](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/asset-ids), [Hyperliquid info endpoint](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint), [Hyperliquid perpetual account state](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals), [Hyperliquid spot account state](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/spot), [Hyperliquid exchange endpoint](https://hyperliquid.gitbook.io/Hyperliquid-docs/for-developers/api/exchange-endpoint) |
| `myriad_markets` | Implemented | Official grouped event/market discovery via `/markets?group_by_event=true` and `/events/{id}`, outcome price reads, official `price_charts` candle history, orderbooks, order-book trade history, alerts, local paper quote payloads, bounded public wallet `account_activity`, `portfolio`, and `market_positions` reads, public wallet buy/sell activity, simulation-first copy intents, guarded unsigned split/merge/redeem/NegRisk transaction-calldata intents, guarded HMAC/JWT-authenticated signed order submission, and fixed `cancel_order`, `batch_cancel_orders`, `cancel_all_orders`, and `batch_modify_orders` mutations are implemented through the official Myriad API. Signed market/outcome, side, 1e18-scaled amount, and price must match shared preflight before transport. Common candle resolutions map to Myriad's 24h/7d/30d/all buckets without local resampling. Position intents never sign or submit on-chain; live copy/order submission remains guarded and never occurs from a feed event automatically. | Keep position intents preview-only until an operator reviews the target/calldata and signs externally; keep live order posting and order management disabled by default, require user-signed EIP-712 order payloads plus Myriad HMAC/API-key or session credentials, and validate EVM activity identities and network scoping. | [Myriad API reference](https://docs.myriad.markets/builders/myriad-api-reference), [Myriad authentication](https://docs.myriad.markets/builders/myriad-api-reference#api-key--secret-hmac), [Myriad orders](https://docs.myriad.markets/builders/myriad-api-reference#orders), [Myriad positions](https://docs.myriad.markets/builders/myriad-api-reference#positions), [Myriad price charts](https://docs.myriad.markets/builders/myriad-api-reference#markets-id), [Myriad user events](https://docs.myriad.markets/builders/myriad-api-reference#users), [Myriad order-book trades](https://docs.myriad.markets/builders/myriad-api-reference#markets-id-trades), [Myriad order book](https://docs.myriad.markets/builders/myriad-order-book), [Myriad Order Book API](https://docs.myriad.markets/builders/myriad-order-book/order-book-api) |
| `context_v2` | Implemented | Current Context v2 market, price, orderbook, and order endpoints are mapped with offline fixtures. Live orders require an API key and an externally signed wallet order whose market, outcome, side, price, and size match shared preflight; copy trading is unsupported because no account-activity mirroring endpoint is used. | Keep live trading disabled by default, require `CONTEXT_API_KEY`, keep private keys outside the adapter, require wallet setup/chain eligibility, and add cancel/portfolio/settlement flows only through documented endpoints with fixtures. | [Context API keys](https://docs.context.markets/developers/guides/api-keys), [List markets](https://docs.context.markets/api-reference/markets/list-markets), [Create order](https://docs.context.markets/api-reference/orders/create-order), [Context React SDK](https://docs.context.markets/agents/react-sdk/index) |
| `frenzy_finance` | Implemented | Official Base/Base Sepolia contract deployments support configured price-range grid events, `BetSettled` history via `eth_getLogs`, alerts, and fixture-backed dry-run EIP-712 `BetIntent` previews. The adapter does not infer private web-app routes or fabricate active quotes/oracle acknowledgements. | Keep paper/dry-run mode as the default; require an explicitly reviewed oracle-acknowledgement, wallet-signing, collateral, and settlement flow before enabling live transactions. Orderbook and copy-trading paths remain unsupported. | [Frenzy docs](https://frenzy.finance/docs), [Frenzy smart contracts](https://frenzy.finance/docs/smart-contracts), [Frenzy Finance](https://frenzy.finance) |
| `xo_market` | Implemented | Authenticated market discovery, outcome listing, orderbooks, public market trades, OHLCV candles, authenticated account/position/order/trade/settlement/audit recovery, alerts, local paper orders, simulation-first copy previews from complete account trades, and guarded signed live order posting/cancellation are implemented through official XO Markets HMAC endpoints. Copy previews never submit live orders automatically. | Keep live order posting and cancellation disabled by default, require HMAC credentials, validate bounded account filters and path identifiers, and verify account/funding/settlement evidence in a reviewed environment. | [XO documentation](https://xotrade.co/documentation.html), [XO](https://xotrade.co) |
| `manifold` | Implemented | Official market discovery, binary/multiple-choice probability parsing, alerts, paper orders, bounded derived OHLCV candles from documented `/v0/bets?contractId=...` fills, and guarded authenticated MANA betting are implemented. The public `/v0/bets?username=...` feed is normalized into a `manifold:<username>` activity identity for wallet tracking and simulation-first copy previews. The documented `/v0/bets?contractId=...` feed is also normalized into per-fill trade history with timestamp filters; derived candles retain source fill ids and are explicitly marked as derived because Manifold does not publish a native candle endpoint. Authenticated `/v0/me` and `/v0/bets` account recovery now expose account, open-limit, and historical bet reads; the documented `POST /v0/bet/{id}/cancel` boundary supports one validated open-limit cancellation behind a separate opt-in, shared live gates, and exact confirmation. CLOB orderbook reading remains unsupported, and copy never places live orders automatically. | Keep live MANA betting, order management, and copy simulation disabled by default, require `MANIFOLD_API_KEY` for authenticated calls, validate safe IDs and the prefixed username identity, do not scrape, and only add multi-answer live betting where the documented `/v0/multi-bet` shape maps safely to the app order model. | [Manifold API docs](https://github.com/manifoldmarkets/manifold/blob/main/docs/docs/api.md), [Manifold bet schema](https://raw.githubusercontent.com/ManifoldMarkets/manifold/main/common/src/bet.ts) |
| `metaculus` | Implemented | Authenticated read-only post/question discovery, accessible forecast-value parsing, and access-controlled Community Prediction aggregation history are implemented through the official Metaculus API. History is normalized as irregular point snapshots through `list_candles`; no OHLCV, resampling, trading, paper trading, orderbook, or copy-trading claim is made because Metaculus is a forecasting platform, not a trading venue. Unavailable forecasts/history produce clear errors. | Keep `METACULUS_API_TOKEN` required for API calls, respect Metaculus data-use restrictions and account access tier, do not scrape, and do not add trading controls unless Metaculus documents a trading product. | [Metaculus API](https://www.metaculus.com/api/), [Metaculus OpenAPI contract](https://github.com/Metaculus/metaculus/blob/main/docs/openapi.yml), [Metaculus API changes](https://www.metaculus.com/notebooks/42554/changes-to-the-metaculus-api/) |
| `good_judgment_open` | Implemented | The adapter uses Cultivate Forecasts' documented REST contract for question discovery, answer probabilities, irregular prediction-set history, local paper previews, and guarded forecast submission. It does not scrape HTML or model forecasts as exchange fills. | Keep the instance base URL and credentials environment-only, validate account/region eligibility, keep live submission disabled by default, and treat the forecast endpoint as a credentialed update surface rather than exchange execution. | [Cultivate Forecasts API reference](https://cultivatelabs.github.io/forecasts-api-docs/), [Good Judgment Open](https://www.gjopen.com), [Good Judgment Open questions](https://www.gjopen.com/questions) |
| `hypermind` | Implemented | Archive-only support uses Hypermind's official July 2026 forecasting report exports: the documented trade-level CSV provides historical prices/quantities and the winning-outcomes text file provides resolution outcomes. The adapter normalizes events, contracts, prices, trade history, derived point candles, alerts, and local paper previews without scraping or inventing trade direction. Orderbooks, live trading, and copy trading remain unsupported because the exports are read-only. | Keep the official export hosts allow-listed, validate bounded market/outcome identifiers and row limits, keep paper mode local/dry-run, preserve `side=TRADE` because the export does not publish BUY/SELL direction, and do not add execution without a separately documented official API. | [Hypermind forecasting-accuracy report](https://www.hypermind.com/prediction-market/forecasting-accuracy), [Hypermind trade export](https://predict.hypermind.com/hypermind/media/data/All-ifps-all-prices-260714.csv), [Hypermind outcomes export](https://predict.hypermind.com/hypermind/media/data/Winning-outcomes-all-260714.txt) |
| `iowa_electronic_markets` | Implemented | Archive-only implementation: IEM publishes an official historical price archive and documented fixed-width/text price-file format. The adapter uses explicit archive inventory for daily historical candles, latest archived prices, alerts, and dry-run orders; current quote pages, orderbooks, live trading, and copy activity remain unsupported because no stable public automation API is documented. | Keep the inventory explicit, validate official IEM archive hosts, keep paper mode local/dry-run, and add current quotes or live orders only after IEM publishes a supported API and account terms for automation. | [Iowa historical archive](https://iemweb.biz.uiowa.edu/historicaldata/), [IEM price-file format](https://iemweb.biz.uiowa.edu/historicaldata/uspoliticalmarkets/1996elections/1996powellnomination_wta/1996powellnomination_wta_fileformat.txt), [Iowa Electronic Markets](https://iem.uiowa.edu/) |
| `infer` | Verified blocked | Verified 2026-05-26; re-verified 2026-08-17: INFER-pub now redirects to the RAND Forecasting Initiative permanent read-only archive. Accounts, forecasting, and comments are no longer available; the archive preserves questions and leaderboards but does not publish a supported API or export contract. | Keep this entry fail-closed unless a maintained official export/API is published; do not scrape the archive or automate private sessions. | [INFER-pub](https://www.infer-pub.com), [RAND Forecasting Initiative archive](https://www.randforecastinginitiative.org/), [RFI questions](https://www.randforecastinginitiative.org/questions) |
| `fact_machine` | Verified blocked | Verified 2026-05-26: Fact Machine does not publish a public documented API/SDK or stable protocol integration contract suitable for discovery, pricing, or order automation in this app. | Implement only if official docs/contracts are published and cover identity, market/outcome schema, wallet flow, and settlement rules. | [Fact Machine](https://factmachine.io) |
| `opinion_labs` | Implemented | Authenticated market discovery, token listing, token prices, orderbooks, documented token price history, contract-scoped filled wallet-trade history, authenticated order history/detail, wallet-scoped positions, alerts, simulation-first copy intents, a guarded limit/market order boundary, and guarded `cancel_order`, `batch_cancel_orders`, and `cancel_all_orders` mutations are implemented through the official Opinion OpenAPI plus optional CLOB SDK. The one-price-per-timestamp history is normalized as flat OHLC points with no fabricated volume. Trade and account reads require an explicit wallet (`OPINION_ACCOUNT_WALLET` or the documented trade/activity wallet fallback) plus `OPINION_API_KEY`; order ids, page/limit, status, market, chain, and wallet path components are validated locally. Live placement and cancellation are disabled by default, require BNB Chain credentials and the shared live-safety gates plus separate opt-ins and exact confirmations, and never sign or approve outside the SDK boundary. | Keep `OPINION_API_KEY`, `OPINION_ACCOUNT_WALLET`, `OPINION_WALLET_ADDRESS`, `OPINION_PRIVATE_KEY`, `OPINION_MULTI_SIG_ADDRESS`, and `OPINION_RPC_URL` environment-only; install the optional `opinion-clob-sdk`, keep `opinion_live_check_approval=false` and `opinion_order_management_enabled=false` unless separately reviewed, keep copy mode simulation-first, and validate wallet/market/token mapping from the official trade feed. | [Opinion OpenAPI overview](https://docs.opinion.trade/developer-guide/opinion-open-api/overview), [Opinion Position API](https://docs.opinion.trade/developer-guide/opinion-open-api/position), [Opinion Order API](https://docs.opinion.trade/developer-guide/opinion-open-api/order), [Opinion CLOB SDK overview](https://docs.opinion.trade/developer-guide/opinion-clob-python-sdk/overview), [Opinion CLOB SDK package](https://pypi.org/project/opinion-clob-sdk/), [Opinion market API](https://docs.opinion.trade/developer-guide/opinion-open-api/market), [Opinion token API](https://docs.opinion.trade/developer-guide/opinion-open-api/token), [Opinion trade API](https://docs.opinion.trade/developer-guide/opinion-open-api/trade) |
| `gemini_titan` | Implemented | Public prediction-market event discovery, contract listing, contract orderbooks, implied prices, irregular contract `priceHistory` points, authenticated filled account trades, simulation-first copy previews from complete filled orders, alerts, local paper orders, guarded authenticated prediction-market limit/stop-limit orders, authenticated account recovery, and guarded single/batch order cancellation are implemented through the official Gemini Prediction Markets REST contract. Recovery covers active orders, filled/cancelled order history, current positions, settled positions, and per-event volume metrics with documented pagination, sort, timestamp, and event filters. Cancellation validates positive int64 ids and a unique 1–20 item batch before signing only the fixed documented paths. The normalized trade and copy-preview routes preserve authenticated BUY/SELL order direction and re-check event/instrument identity, order id, timestamp, filled size, and price bounds locally; non-filled or incomplete rows fail closed. Live boundaries check account terms for placement, require the Gemini Trader role, use shared live safety plus a separate `gemini_order_management_enabled` opt-in for cancellation, and keep live mode disabled by default. Price history is normalized as flat candles without fabricated OHLCV resampling; copy previews never submit live orders. | Keep live trading and order management disabled by default, keep copy previews simulation-first, require a Gemini Trader key and latest Prediction Markets terms outside this app, keep credentials environment-only, and treat real account/region/KYC/funding verification as external gates. | [Gemini Prediction Markets API](https://developer.gemini.com/prediction-markets-spec), [Gemini markets](https://developer.gemini.com/prediction-markets-spec/markets), [Gemini place order](https://developer.gemini.com/prediction-markets-spec/trading), [Gemini cancel order](https://developer.gemini.com/rest-api/prediction-markets/order-management/cancel-order), [Gemini batch cancellation](https://developer.gemini.com/rest-api/prediction-markets/order-management/cancel-batch-orders), [Gemini positions and account recovery](https://developer.gemini.com/prediction-markets-spec/positions), [Gemini terms status](https://developer.gemini.com/prediction-markets-spec/terms), [Gemini WebSocket streams](https://developer.gemini.com/prediction-markets/websocket/streams) |
| `augur` | Implemented | Read-only Augur v2 market and outcome listing is implemented against the documented subgraph schema with a configured GraphQL endpoint. Current price/orderbook, paper trading, live trading, and copy trading remain unsupported because the legacy official Augur stack does not provide a maintained Python-compatible live market-data/order route for this adapter. | Configure only maintained Augur v2 subgraph endpoints, keep live trading disabled, and add wallet-signed protocol transactions only with explicit user signing and separate tests. | [Augur repository](https://github.com/AugurProject/augur), [Augur v2 subgraph schema](https://github.com/protofire/augur-v2-subgraph) |
| `betmgm` | Implemented | Official partner Sports API fixture, market, option, and price reads are implemented with implied-probability normalization and local paper orders. The partner contract publishes no order or account-activity endpoint, so live betting and copy trading remain unsupported. | Keep partner AccessId/AccessIdToken in environment variables or external credential files; honor the API's partner approval, rate-limit, data-use, region, and KYC requirements. | [BetMGM Sports API](https://sportsapi.wv.betmgm.com/offer/swagger/sportsapi/swagger.json), [BetMGM Sports API](https://sportsapi.pa.betmgm.com/), [BetMGM partner access](https://sportsapi.nj.betmgm.com/articles/getinvolved.html) |
| `prizepicks` | Verified blocked | Verified 2026-08-23: PrizePicks' official materials identify Kalshi as the provider for its listed event contracts, but they do not publish a machine-readable mapping from the venue-wide Kalshi catalog to the current PrizePicks board. Treating every Kalshi contract as PrizePicks would overstate support; PrizePicks also publishes no account/order automation API for this app. | Require an official brand-inventory mapping/filter with replayable positive and negative fixtures, plus a separately published PrizePicks account/order contract before adding private or live operations. | [PrizePicks–Kalshi launch](https://www.prizepicks.com/press-news/prizepicks-launches-prediction-markets-offering-with-kalshi), [current PrizePicks explainer](https://www.prizepicks.com/playbook-article/prediction-markets-vs-sports-betting-differences-explained), [Kalshi market-data API](https://docs.kalshi.com/getting_started/quick_start_market_data) |
| `underdog_sports` | Verified blocked | Verified 2026-08-23: Underdog's legal center identifies CDNA as its event-contract exchange, but no official machine-readable mapping from the venue-wide CDNA catalog to the current Underdog board is published. Treating every CDNA contract as Underdog would overstate support; Underdog also publishes no account/order automation API for this app. | Require an official brand-inventory mapping/filter with replayable positive and negative fixtures, plus a separately published Underdog/CDNA account or order contract before adding private or live operations. | [Underdog Legal Center](https://legal.underdogsports.com/), [Crypto.com Predictions API](https://data.crypto.com/docs), [anonymous-access quickstart](https://data.crypto.com/quickstart) |
| `drift_bet` | Implemented | Official Drift Data API BET prediction records are mapped with explicit configured-symbol inventory, bounded YES/NO price derivation, public fill history, 31-day/20-record upstream bounds, explicitly derived OHLCV candles, alerts, and local paper orders. Fill normalization preserves stable ids and taker direction, applies documented Drift base/quote precision when raw protocol amounts are returned, and retains source ids in each derived candle. The public OpenAPI contract has no stable market-list endpoint; binary DLOB depth, wallet signing, settlement, live execution, and copy trading remain disabled. | Keep the symbol inventory explicit, keep paper mode as default, surface the 31-day and page-size limits, and add separate SDK/chain fixtures plus wallet/settlement gates before considering live or depth support. | [Drift Data API OpenAPI](https://data.api.drift.trade/playground/json), [Drift Protocol SDK](https://github.com/drift-labs/protocol-v2/tree/master/sdk), [Drift market docs](https://www.mintlify.com/drift-labs/protocol-v2/api/markets/perp-markets) |
| `thales_market` | Implemented | Official Thales REST market/quote responses are mapped with grouped-market flattening, strict network/address validation, positional prices, quote fallback, alerts, local paper orders, and a guarded `eth_sendRawTransaction` boundary for externally signed AMM transactions. Thales exposes an AMM rather than a CLOB; live submission still requires an explicitly configured/reviewed AMM target, chain metadata, collateral approval, wallet signing, and settlement. | Keep paper mode as default, require `thales_submit_signed_transactions=true` plus the shared live gates, and review the signed transaction target/calldata before submission. Add fixture-backed collateral approval and settlement flows only through official contract docs. | [Thales integration](https://docs.thalesmarket.io/technical-documentation/thales-integration), [Thales digital options integration](https://docs.thales.io/thales-digital-options/digital-options-integration), [Thales sports markets integration](https://docs.thales.io/thales-sports-markets/sports-markets-integration) |
| `hedgehog_markets` | Implemented | Official Hedgehog HPL Parimutuel/Eclipse `MarketV1` accounts are decoded through the public Eclipse JSON-RPC contract for market discovery, outcome listing, pooled probability prices, alerts, local dry-run `DepositV1` intents, and guarded forwarding of an externally signed `DepositV1` transaction. The pooled program is not a CLOB; the adapter never signs or settles transactions. | Keep paper mode as default; require `hedgehog_submit_signed_transactions=true`, an explicitly reviewed wallet-signed Eclipse transaction whose program/market/option/amount metadata matches the request, and a separate settlement flow before enabling live orders. Do not infer private metadata endpoints from on-chain URIs. | [Hedgehog program library](https://github.com/Hedgehog-Markets/hedgehog-program-library), [HPL Parimutuel Eclipse](https://github.com/Hedgehog-Markets/hpl-parimutuel-eclipse), [Eclipse RPC](https://docs.eclipse.xyz/developers/rpc-and-block-explorers) |
| `omen` | Implemented | Omen/Gnosis market discovery, outcome listing, AMM marginal-price alerts, public FPMM trades, per-collateral indexed trade-size scaling, bounded derived candles, dry-run paper orders, and guarded forwarding of externally signed FPMM buy/sell transactions are implemented through documented contracts and the subgraph schema. Candle derivation fails closed when different prices share a second because the source has no block/log order. CLOB orderbook reading, collateral approval, settlement, and copy trading remain unsupported. | Configure a maintained Omen subgraph and explicit EVM RPC, keep `omen_candle_trade_limit` bounded, keep paper mode as default, require `omen_submit_signed_transactions=true` plus shared live gates, and review the FPMM target, side, outcome, and calldata before submission. | [Omen exchange contracts/subgraphs](https://github.com/protofire/omen-exchange), [Omen subgraph schema](https://github.com/protofire/omen-subgraph), [Omen docs](https://omendotag.gitbook.io/omen) |
| `zeitgeist` | Implemented | Official Subsquid/indexer GraphQL discovery, outcome listing, asset prices, alerts, dry-run paper orders, and a guarded `author_submitExtrinsic` boundary for reviewed externally signed `HybridRouter.buy`/`sell` calls are implemented. CLOB depth and copy trading remain unsupported; wallet signing, approvals, settlement, and funded execution remain external. | Keep paper mode default; require `zeitgeist_submit_signed_extrinsics=true`, explicit `zeitgeist_rpc_url`, shared live gates, reviewed market/asset/order metadata, and a matching runtime spec version before forwarding a signed extrinsic. | [Zeitgeist fetching markets](https://docs.zeitgeist.pm/docs/build/sdk/v2/fetch-markets), [Zeitgeist indexer](https://docs.zeitgeist.pm/docs/build/sdk/v2/indexer), [Zeitgeist Hybrid Router](https://github.com/zeitgeistpm/zeitgeist/tree/main/zrml/hybrid-router), [Zeitgeist releases](https://github.com/zeitgeistpm/zeitgeist/releases) |
| `zeitgeist_sdk_markets` | Implemented | Explicit alias adapter for the official Zeitgeist SDK/Markets Subsquid GraphQL market and asset schema. Discovery, outcome prices, alerts, dry-run paper orders, and the same guarded externally signed HybridRouter boundary are fixture-backed; CLOB depth and copy trading remain unsupported. | Keep the SDK endpoint and paper mode default, require explicit RPC/live gates and reviewed HybridRouter metadata, and never put wallet keys in app configuration. | [Zeitgeist fetching markets](https://docs.zeitgeist.pm/docs/build/sdk/v2/fetch-markets), [Zeitgeist indexer](https://docs.zeitgeist.pm/docs/build/sdk/v2/indexer), [Zeitgeist Hybrid Router](https://github.com/zeitgeistpm/zeitgeist/tree/main/zrml/hybrid-router) |
| `azuro` | Implemented | Official V3 backend/feed API game discovery, condition/outcome listing, odds reading, alerts, bounded bettor bet-history reads from the documented client subgraph, normalized single-selection bettor activity, simulation-first copy previews, dry-run bet-calculation payloads, WebSocket subscription metadata, and guarded pre-signed live-order posting are implemented. Express/combo bets are excluded from copy normalization because one history row can contain multiple selections that cannot be represented by one contract id. Full wallet signing, token approval, account funding, transaction confirmation, and private account eligibility remain outside the adapter because Azuro requires explicit user wallet authorization. | Keep live trading and copy previews simulation-first, require user-signed order payloads, keep wallet credentials out of config, bound subgraph pagination, reject combo-bet copy rows, and only expand signing/approval flows through documented Azuro SDK/toolkit or backend APIs. | [Azuro APIs](https://gem.azuro.org/hub/apps/APIs), [Azuro Graph API](https://gem.azuro.org/hub/apps/APIs/graph), [Azuro backend API](https://gem.azuro.org/hub/apps/APIs/backend), [Azuro betting API](https://gem.azuro.org/hub/apps/APIs/backend/betting), [Azuro get bets history](https://gem.azuro.org/hub/apps/guides/advanced/prematch/get-bets-history), [Azuro live bet history](https://gem.azuro.org/hub/apps/guides/advanced/live-tutorial/get-bets-history), [Azuro WebSocket API](https://gem.azuro.org/hub/apps/APIs/websocket), [Azuro getGamesByFilters](https://gem.azuro.org/hub/apps/toolkit/feed/getGamesByFilters), [Azuro SDK](https://gem.azuro.org/hub/apps/sdk/overview) |
| `sx_bet` | Implemented | Official REST active-market lookup, specific market lookup, active-order orderbook reconstruction, best-odds fallback, public `/trades-v3/public` trade history, bounded derived OHLCV candles from that public trade tape, v3 API-key account balance/open orders/order history/order detail/fills/positions, alerts, dry-run orders, Centrifugo WebSocket channel metadata, and guarded signed live-order posting are implemented. Derived candles retain source trade ids and an explicit derived/source marker; the public API does not document a native candle endpoint. v3 cancellation by order id/event/all is implemented behind a separate opt-in and the shared live confirmation gate. Public trades are normalized to the requested market outcome with probability odds, base-unit stake scaling, and local timestamp filtering. Authenticated `/fills-v3` rows and account fill-channel payloads are normalized into BUY-only simulation-first copy previews with fill identity, backed outcome, odds, stake, and status validation; parlay/quarter-line/failed rows are rejected because the shared model supports only one two-outcome contract. Live trading remains disabled by default and requires SX wallet/private-key credentials plus account funding/eligibility. | Keep live trading and v3 cancellation opt-in, keep API/wallet credentials in environment variables, keep paper mode as the default, use testnet before real funds, keep copy previews simulation-first, and require an API key for v3 account reads. | [SX Bet API introduction](https://docs.sx.bet/api-reference/introduction), [SX active markets](https://docs.sx.bet/api-reference/get-markets-active), [SX specific markets](https://docs.sx.bet/api-reference/get-markets-find), [SX public trades](https://docs.sx.bet/api-reference/get-trades-v3-public), [SX v3 orders](https://docs.sx.bet/api-reference/get-orders-v3), [SX v3 bets](https://docs.sx.bet/api-reference/get-trades-v3), [SX v3 fills](https://docs.sx.bet/api-reference/get-fills-v3), [SX v3 fill updates](https://docs.sx.bet/api-reference/channel-fills-v3), [SX v3 positions](https://docs.sx.bet/api-reference/get-positions-v3), [SX v3 cancel by id](https://docs.sx.bet/api-reference/delete-orders-v3), [SX v3 cancel by event](https://docs.sx.bet/api-reference/delete-orders-v3-event), [SX v3 cancel all](https://docs.sx.bet/api-reference/delete-orders-v3-all), [SX WebSocket order books](https://docs.sx.bet/api-reference/centrifugo-order-book-updates), [SX signing](https://docs.sx.bet/api-reference/eip712-signing) |
| `limitless_exchange` | Implemented | Official REST market discovery, market detail, orderbook reading, historical YES-price candles, finalized public market-event trade history, alerts, dry-run orders, simulation-first copy previews from complete authenticated portfolio-history fills, guarded HMAC live-order posting, and fixed HMAC single/batch/market-scoped cancellation are implemented. Historical prices are normalized as flat OHLC points and NO prices are derived as the complement of the documented YES series. Market events are normalized from newest-first finalized CLOB fills, including numeric side mapping and raw six-decimal size scaling. The adapter also exposes the documented HMAC-authenticated `/portfolio/positions`, `/portfolio/history`, and `/markets/:slug/user-orders` reads as lossless raw payloads, with optional validated `x-on-behalf-of` delegation. Live trading and order management remain disabled by default and require Limitless token credentials plus the separate order-management opt-in, shared live-safety acknowledgement, and exact operator/global confirmations. Copy previews never submit live orders automatically. | Keep live trading, cancellation, and delegated account reads opt-in, keep HMAC credentials in environment variables, keep dry-run mode as the default, and require the documented `delegated_signing` scope and linked profile for sub-account reads. Only expand wallet/order flows through documented Limitless APIs or SDKs. | [Limitless active markets](https://docs.limitless.exchange/api-reference/markets/browse-active), [Limitless historical prices](https://docs.limitless.exchange/api-reference/trading/historical-price), [Limitless market events](https://docs.limitless.exchange/api-reference/trading/market-events), [Limitless cancel order](https://docs.limitless.exchange/api-reference/trading/cancel-order), [Limitless cancel batch](https://docs.limitless.exchange/api-reference/trading/cancel-batch), [Limitless cancel all](https://docs.limitless.exchange/api-reference/trading/cancel-all), [Limitless Python SDK markets](https://docs.limitless.exchange/developers/sdk/python/markets), [Limitless authentication](https://docs.limitless.exchange/developers/authentication), [Limitless programmatic API](https://docs.limitless.exchange/developers/programmatic-api), [Limitless migrate from Polymarket](https://docs.limitless.exchange/developers/migrate-from-polymarket), [Limitless WebSocket](https://docs.limitless.exchange/developers/quickstart/websocket) |
| `predict_fun` | Implemented | Market discovery, outcome listing, orderbook-derived prices, public order-match trades, official point-based timeseries candles, authenticated account/order/activity/position recovery, normalized authenticated matched-fill activity, local paper orders, and guarded signed order submission are implemented through the official Predict.fun REST API. Signed outcome token, side, 1e18-scaled size, and price must match shared preflight. Account copy intents are simulation-first and restricted to the JWT-authenticated account address; they never submit a live order automatically. Relay-only removal by order id/hash is separately guarded and explicitly does not invalidate orders on-chain. | Keep `PREDICT_FUN_API_KEY` required on mainnet, require `PREDICT_FUN_JWT` for private reads/copy/removal, keep relay removal disabled by default, and require the shared live-safety gates plus exact operator confirmation before enabling `predict_fun_order_management_enabled`. | [Predict.fun REST API](https://docs.predict.fun/developers/predict-rest-api), [Predict.fun create order](https://dev.predict.fun/create-an-order-25326903e0), [Predict.fun orderbook](https://dev.predict.fun/get-the-orderbook-for-a-market-25326908e0), [Predict.fun timeseries](https://dev.predict.fun/get-market-timeseries-25326910e0), [Predict.fun match events](https://dev.predict.fun/get-order-match-events-25663812e0), [Predict.fun orders](https://dev.predict.fun/get-orders-25326902e0), [Predict.fun positions](https://dev.predict.fun/get-positions-32675933e0), [Predict.fun account activity](https://dev.predict.fun/get-account-activity-32534697e0), [Predict.fun remove orders](https://dev.predict.fun/remove-orders-from-the-orderbook-25326904e0), [Predict.fun remove by hash](https://dev.predict.fun/remove-orders-by-hash-38139973e0) |
| `smarkets` | Implemented | Current Smarkets v3 REST event/market/contract discovery, quote orderbooks, prices, dry-run orders, authenticated order/account reads, and guarded session-authenticated order placement plus single/market-scoped cancellation are implemented with offline fixtures. API application approval, written data-use permission, account eligibility, and funded execution remain external gates; copy trading and settlement history remain unsupported. | Keep live orders and cancellations disabled by default, require an approved `SMARKETS_SESSION_TOKEN`, honor pre-approved markets/rate limits and data-use terms, keep credentials out of config, and verify account/order responses against the approved Smarkets contract before enabling the separate `smarkets_order_management_enabled` flag. | [Smarkets API reference](https://docs.smarkets.com/), [Smarkets API access terms](https://help.smarkets.com/hc/en-gb/articles/34697834941085-Smarkets-API-Access-Integration-T-Cs), [Smarkets API resources](https://help.smarkets.com/hc/en-gb/articles/34720906181021-Smarkets-API-Documentation-Resources), [Smarkets official client](https://github.com/smarkets/smk_trading_bot/blob/master/client.py) |
| `betfair_exchange` | Implemented | Authenticated market discovery, runner listing, best-offer orderbooks, implied-probability prices, current/cleared account-order recovery, simulation-first copy previews from complete matched orders, bounded derived OHLCV candles from matched executions, funds/account-detail/statement/currency-rate reads, alerts, local paper orders, and guarded `placeOrders`, `cancelOrders`, `updateOrders`, and `replaceOrders` support are implemented through Betfair's official Exchange and Accounts JSON-RPC APIs. Derived candles retain source bet ids and are explicitly marked as derived because Betfair documents matched orders rather than a native candle endpoint. Matched orders are normalized as probability-priced trades with local runner/time filtering; current-order status/order-by/sort/date filters, cleared-order status/group/id/date filters, bounded paging, item descriptions, statement locale/date/wallet filters, and currency-code validation are enforced before forwarding; mutation instructions are bounded and normalized to official schemas, global cancellation requires exact confirmation, and mutations require a separate adapter opt-in plus the shared live-safety gates. Copy previews map BACK/LAY, average matched odds, matched size, and bet identity to local BUY/SELL paper intents and never submit `placeOrders`. | Keep `BETFAIR_APP_KEY` and `BETFAIR_SESSION_TOKEN` in environment variables, keep `live_trading_enabled` and `betfair_order_management_enabled` false by default, keep copy previews simulation-first, and require explicit operator review before any live mutation. | [Betfair Developer Program](https://developer.betfair.com/), [Betfair Exchange API](https://support.developer.betfair.com/hc/en-us/categories/360000245252-Exchange-API), [Betfair API getting started](https://support.developer.betfair.com/hc/en-us/articles/115003864651-How-do-I-get-started), [Betfair `listCurrentOrders`](https://betfair-developer-docs.atlassian.net/wiki/spaces/1smk3cen4v3lu3yomq5qye0ni/pages/2687504/listCurrentOrders), [Betfair `listClearedOrders`](https://betfair-developer-docs.atlassian.net/wiki/spaces/1smk3cen4v3lu3yomq5qye0ni/pages/2687679/listClearedOrders+-+Roll-up+Fields+Available), [Betfair Accounts API](https://betfair-developer-docs.atlassian.net/wiki/spaces/1smk3cen4v3lu3yomq5qye0ni/pages/2687725/Accounts+API), [Betfair `getAccountDetails`](https://betfair-developer-docs.atlassian.net/wiki/spaces/1smk3cen4v3lu3yomq5qye0ni/pages/2699900/getAccountDetails), [Betfair `cancelOrders`](https://betfair-developer-docs.atlassian.net/wiki/spaces/1smk3cen4v3lu3yomq5qye0ni/pages/2687491/cancelOrders), [Betfair `updateOrders`](https://betfair-developer-docs.atlassian.net/wiki/spaces/1smk3cen4v3lu3yomq5qye0ni/pages/2687485/updateOrders), [Betfair placeOrders example](https://support.developer.betfair.com/hc/en-us/articles/360016170431-How-do-I-place-bets-on-handicap-markets) |
| `probo` | Verified blocked | Verified 2026-08-17: Probo's official site states that operations are closed; it also does not publish a public official API, SDK, or automation permission flow suitable for this app. | Keep the adapter fail-closed unless Probo officially resumes operations and publishes integration APIs and terms. | [Probo](https://probo.in) |
| `coinbase_prediction_markets` | Implemented | Coinbase documents that prediction-market flow comes from Kalshi and directs users to Kalshi market outcomes. The adapter uses Kalshi's official public market-data API for discovery, contracts, prices, orderbooks, public trades, candlesticks, alerts, and local dry-run orders. Coinbase-specific account execution and copy-trading APIs remain unsupported. | Keep the alias read-only, require Coinbase account/KYC/region checks for any future execution work, and obtain a supported Coinbase order API before adding live trading. | [Coinbase prediction markets launch](https://www.coinbase.com/blog/system-update-the-future-of-prediction-markets), [Coinbase prediction market introduction](https://help.coinbase.com/en/coinbase/trading-and-funding/prediction-markets/intro), [Coinbase prediction market payouts](https://help.coinbase.com/en/coinbase/trading-and-funding/prediction-markets/payouts), [Kalshi market-data docs](https://docs.kalshi.com/getting_started/quick_start_market_data), [Kalshi trades](https://docs.kalshi.com/api-reference/market/get-trades), [Kalshi candlesticks](https://docs.kalshi.com/api-reference/market/batch-get-market-candlesticks) |
| `probable` | Implemented | Official market discovery, token prices, orderbooks, public wallet-scoped activity trade history, simulation-first wallet copy previews, point price history normalized as flat candles, authenticated `open_orders`/`order` recovery reads, alerts, dry-run paper orders, guarded signed-order submission, and guarded `cancel_order`, `cancel_orders`, and `cancel_all_orders` mutations are implemented through Probable's documented market/CLOB APIs. Signed token, side, maker/taker amounts, price, and size must match shared preflight using the verified BNB mainnet 18-decimal scale; other chains require an explicit trusted scale. Batch/global cancellation composes the documented fixed single-order DELETE route after bounded local identity validation. Activity reads and copy previews require a validated wallet address; live execution and cancellation require HMAC credentials, BSC account readiness, separate opt-ins, exact operator confirmations, and explicit shared safety gates. | Keep private keys outside the app config, keep paper mode as the default, validate activity/order identity and time/contract filters locally, require signed-order and live safety gates, keep `probable_order_management_enabled=false` unless separately reviewed, and add real credentialed/funded verification before promoting live evidence. | [Probable developer docs](https://developer.probable.markets/), [Probable CLOB SDK](https://www.npmjs.com/package/@prob/clob), [Probable examples](https://github.com/0xprobable/clob-examples) |
| `kalshi_via_robinhood` | Implemented | Explicit read-only Kalshi distribution alias for Robinhood's documented KalshiEX-backed Prediction Markets surface, using Kalshi public market data for discovery, prices/orderbooks, trades, candles, alerts, and local paper orders. Robinhood account execution and private endpoints remain unsupported. | Keep the alias read-only and require a separate documented Robinhood integration contract before adding account execution. | [Robinhood newsroom](https://robinhood.com/us/en/newsroom/robinhood-prediction-markets-hub/), [Kalshi market-data docs](https://docs.kalshi.com/getting_started/quick_start_market_data) |
| `fanduel_predicts` | Implemented | Read-only FanDuel Predicts/OG alias uses the official Crypto.com Predictions API for OG/CDNA event discovery, contracts, prices, alerts, and local dry-run orders. FanDuel's product includes CME-listed contracts as well, which remain available through `cme_prediction_markets`; FanDuel account execution, orderbook depth, live trading, and copy trading are not automated. | Keep the alias read-only, honor Crypto.com data-use/rate-limit terms, keep paper mode default, and require FanDuel account/KYC/region checks plus an official order API before adding execution. | [FanDuel Predicts](https://www.fanduel.com/predicts), [FanDuel/Crypto.com and OG announcement](https://www.fanduel.com/about/news/fanduel-predicts-to-expand-event-contract-offering-through-partnership-with-crypto-com-and-og-prediction-markets), [Crypto.com Predictions API](https://data.crypto.com/docs) |
| `seer` | Implemented | Official Seer `markets-search`, `get-market`, and chain-qualified `market-chart` serverless responses are mapped to events, outcome contracts, prices, Generic-market flat candle points, alerts, local paper orders, and a guarded externally signed transaction boundary. Chart normalization validates the exact sorted outcome/collateral pool pair and uses the reciprocal price field selected by Seer's official consumer; it never fabricates volume, resamples mixed hourly/swap timestamps, or derives decimal-unsafe prices from `sqrtPrice`. Futarchy history remains fail-closed. Because Seer routes outcome-token trading through a third-party DEX, live forwarding requires an explicitly reviewed DEX contract allow-list, chain/market/outcome metadata, an EVM RPC, and shared live-safety gates; the adapter never signs, approves collateral, or settles. Orderbook depth and copy trading remain unsupported. | Keep paper mode as default; preserve the fail-closed pool-orientation validation; require `seer_submit_signed_transactions=true`, an explicit `seer_rpc_url`, a reviewed `seer_trading_contract_addresses` allow-list, and a user-signed transaction whose target, side, chain, market, outcome, and calldata metadata match the request. | [Seer API](https://seer-3.gitbook.io/seer-documentation/developers/interact-with-seer/api), [Seer chart endpoint source](https://github.com/seer-pm/demo/blob/main/web/netlify/functions/market-chart.mts), [Seer chart consumer](https://github.com/seer-pm/demo/blob/main/web/src/hooks/chart/utils.ts), [Seer pool-pair helper](https://github.com/seer-pm/demo/blob/main/packages/seer-pm-sdk/src/market-pools.ts), [Seer trading](https://seer-3.gitbook.io/seer-documentation/developers/interact-with-seer/trading) |
| `dflow` | Implemented | Official DFlow Metadata/Trade API event and nested-market discovery, outcome-mint prices, public outcome-mint trade history, bounded derived OHLCV candles from that trade feed, orderbooks, paper orders, and guarded wallet-signed Solana transaction submission are implemented with offline fixtures. Derived candles retain source trade ids and are explicitly marked as derived because DFlow documents trades rather than a native candle endpoint. | Keep API keys out of logs, require Proof/KYC and jurisdiction eligibility, obtain a signed transaction from the user's wallet, submit only through an explicitly configured Solana RPC, and add redemption/status flows only through documented endpoints with fixtures. | [DFlow introduction](https://pond.dflow.net/introduction), [Market by mint](https://dflow.mintlify.app/build/metadata-api/markets/market-by-mint), [Orderbook by mint](https://dflow.mintlify.app/build/metadata-api/orderbook/orderbook-by-mint), [Trades by mint](https://dflow.mintlify.app/build/metadata-api/trades/trades-by-mint), [DFlow order flow](https://dflow.mintlify.app/build/recipes/prediction-markets/decrease-position) |
| `space` | Implemented | Official Space REST `/markets`, market-detail, `/orderbook`, `/trades`, and `/candles` contracts are mapped to discovery, binary/multi-outcome contracts, prices, orderbooks, public trade history, OHLCV candles, alerts, and local paper orders with offline fixtures. The docs currently say the public production release will be announced separately; wallet-signed live execution, settlement, and copy trading remain unsupported. | Keep reads and paper mode available only after confirming the documented public endpoint is live; add wallet/settlement flows only through an official order contract and signed-transaction fixtures. | [Space REST API](https://docs.into.space/en/api/rest), [Space developer resources](https://docs.into.space/en/build/resources), [Space OpenAPI](https://docs.into.space/api-reference/openapi.json) |
| `xmarket` | Implemented | Official market discovery, outcome listing, prices, orderbooks, local paper orders, guarded API-key order submission, authenticated positions/user-order/market-order reads, and fixed API-key batch create/cancel mutations are implemented. Copy trading is unsupported because the documented API does not publish an account-activity mirroring feed. | Keep API credentials in environment variables, paper mode as default, and live order gates enabled. Keep `xmarket_order_management_enabled=false` unless separately reviewed, require exact operator confirmation plus shared live-safety gates, bound batch payloads, and validate every outcome/order identifier. | [Xmarket quick start](https://docs.xmarket.app/developers/quick-start), [Xmarket markets](https://docs.xmarket.app/developers/markets), [Xmarket orderbook](https://docs.xmarket.app/developers/orderbook), [Xmarket orders](https://docs.xmarket.app/developers/orders), [Xmarket positions](https://docs.xmarket.app/developers/positions) |
| `trueo` | Implemented | Official Trueo Base `TruthMarketManager`/`TruthMarket` contract reads, immutable market fields, Uniswap V3 pool prices, alerts, paper orders, and guarded externally signed transactions are fixture-backed. Before broadcast, the adapter decodes the signed EVM transaction and requires its chain, allow-listed recipient, calldata, value, and reviewed market/order metadata to match exactly. CLOB depth, copy trading, wallet signing, settlement, and funded execution remain external gates. | Keep paper mode as default; configure `trueo_chain_id` and a narrowly reviewed `trueo_live_transaction_targets` allow-list, preserve the shared live gates, and add funded/settlement evidence only through a reviewed wallet environment. | [Trueo deployments](https://docs.trueo.com/deployments), [Trueo markets](https://docs.trueo.com/markets), [Trueo trading](https://docs.trueo.com/trading), [Trueo contracts](https://github.com/trueo-protocol/trueo-contracts) |
| `prdt_finance` | Implemented | Official legacy `Prediction` contract reads, current-round bull/bear pool-share prices, alerts, and open-round local paper intents are fixture-backed. Deployment discovery is intentionally unavailable because no stable official current factory/indexer inventory is published. | Configure `prdt_rpc_url` and an explicit, independently verified `prdt_prediction_contracts` inventory; keep live execution and settlement disabled. | [PRDT Finance](https://prdt.finance/en), [smart contracts](https://prdt-finance.gitbook.io/prdt-finance-gitbook/smart-contracts), [contract source](https://github.com/PRDTfinance/contract) |
| `synstation` | Verified blocked | Verified 2026-08-23: SynStation's official whitepaper describes a conceptual CLMM prediction protocol but does not publish a stable deployment inventory, market-data endpoint, or order contract. | Require an official deployment/API or reviewed contract surface plus offline response fixtures. | [SynStation](https://synstation.ai), [SynStation whitepaper](https://synstation.notion.site/SynStation-Whitepaper-12dd359ec74180789fd2cef45609fa93) |
| `gnosis_prediction_markets` | Implemented | Explicit Gnosis/Omen alias over the official FixedProductMarketMaker subgraph schema. Discovery, outcome prices, public FPMM trades, per-collateral indexed scaling, bounded derived candles with ambiguous same-second rejection, alerts, local paper orders, and guarded externally signed FPMM buy/sell forwarding are fixture-backed; CLOB depth, collateral approval, settlement, and copy trading remain unsupported. | Keep the Gnosis endpoint and EVM RPC configured explicitly, keep `gnosis_candle_trade_limit` bounded, keep paper mode as default, require `gnosis_submit_signed_transactions=true` plus shared live gates, and review target/side/outcome/calldata before submission. | [Omen](https://omen.eth.limo), [Gnosis docs](https://docs.gnosis.io/), [Omen subgraph](https://github.com/protofire/omen-subgraph) |
| `metadao` | Implemented | Official MetaDAO Futarchy DEX `/api/tickers` rows are mapped to DAO/token-pair events, prices, bid/ask summaries, alerts, local paper orders, and a guarded externally signed Solana router boundary. Because the public API does not expose an order endpoint, live forwarding requires an explicitly reviewed router-program allow-list, ticker/pool/instruction metadata, a Solana RPC, and shared live-safety gates; the adapter never signs, approves tokens, or settles. Orderbook depth and copy trading remain unsupported. | Keep paper mode as default; require `metadao_submit_signed_transactions=true`, an explicit `metadao_solana_rpc_url`, a reviewed `metadao_router_program_ids` allow-list, and a user-signed transaction whose target, ticker, pool, side, and instruction metadata match the request. | [MetaDAO Futarchy DEX API](https://api-docs.metadao.fi/introduction), [MetaDAO configuration](https://api-docs.metadao.fi/configuration), [MetaDAO ticker reference](https://api-docs.metadao.fi/api-reference/get-api-tickers), [MetaDAO external API](https://github.com/metaDAOproject/futarchy-external-api) |
| `levr_bet` | Verified blocked | Verified 2026-08-21: no stable official API, contract schema, and settlement fixtures have been validated for Levr Bet. | Require a documented API or contract/indexer surface plus paper fixtures. | [Levr Bet](https://levr.bet) |
| `dexsport` | Verified blocked | Verified 2026-08-21: Dexsport documents prediction markets and smart-contract settlement, but its public documentation does not provide a stable market-data API, deployment inventory, or reviewed prediction-contract ABI for this app. | Add official market-data and wallet-safe paper support before live capability. | [Dexsport docs](https://dexsport.io/docs-home/), [Dexsport prediction markets](https://dexsport.io/prediction-markets/all/) |
| `lamas_finance` | Implemented | The official Lamas Finance repository documents Anchor `PricePredict` and `UpOrDown` programs with devnet identifiers. The adapter reads `RoundResult` accounts through Solana JSON-RPC, derives pooled Up/Down prices, exposes the PricePredict reference price, emits alerts and local paper intents, and guards externally signed `predict` transactions. CLOB depth, wallet signing, settlement, and copy trading remain unsupported. | Keep the documented devnet deployment as the default example; independently review any production program/mint inventory before enabling live forwarding, require `lamas_finance_submit_signed_transactions=true`, and keep settlement outside this adapter. | [Lamas Finance docs](https://docs.lamas.co/1.0), [official Lamas Finance repository](https://github.com/LamasFinance/LamasFinance), [Solana `getProgramAccounts`](https://solana.com/docs/rpc/http/getprogramaccounts) |
| `zetarium_world` | Implemented | Reviewed BSC `PredictionMarket` deployment and the published V2 contract model are mapped to on-chain market discovery, binary/multi-outcome pool-share prices, alerts, local paper intents, and a guarded externally signed BUY transaction boundary. The adapter uses the reviewed verified deployment address by default and supports explicit address/inventory overrides; CLOB depth, wallet signing, settlement, and copy trading remain unsupported. | Keep paper mode as default; review the configured BSC deployment and market inventory, require `zetarium_submit_signed_transactions=true` plus a narrow `zetarium_live_transaction_targets` allow-list for live submission, and add settlement lifecycle only through documented contract methods with fixtures. | [Zetarium Prediction Market V2](https://docs.zetarium.world/docs/products/prediction), [Zetarium smart contracts](https://docs.zetarium.world/docs/overview/smart-contracts), [reviewed PredictionMarket deployment](https://bscscan.com/address/0xfc5fa5bb5f6a812600c303b4b83ee8dbdc021d99) |
| `blinq` | Implemented | Official Blinq product pages say the leverage layer trades Polymarket markets. The adapter maps the documented public Polymarket discovery, contracts, prices, orderbooks, price-history points, and authenticated read-only CLOB trades (explicit readonly/L2 headers required), plus alerts and local paper orders for that underlying surface. Its health and operation allow-list explicitly exclude inherited Polymarket private-account endpoints. Blinq leverage, deposits, private account actions, live wallet execution, and copy trading remain unsupported. | Keep this adapter read-only; use the official Polymarket data endpoints and do not infer Blinq leverage or wallet contracts until Blinq publishes them. | [Blinq](https://blinq.fi/), [Blinq Predictions](https://predictions.blinq.fi/), [Polymarket trades](https://docs.polymarket.com/api-reference/trade/get-trades), [Polymarket price history](https://docs.polymarket.com/api-reference/markets/get-prices-history) |
| `zeitgeist_prediction_pools` | Implemented | Pool-scoped Zeitgeist adapter uses the documented market/pool/asset GraphQL schema, validates pool identifiers, maps discovery, pool-backed outcome prices, alerts, and dry-run paper orders, and forwards only reviewed externally signed HybridRouter calls whose pool metadata matches. Pool accounting, settlement, wallet signing, CLOB depth, and copy trading remain unsupported. | Keep the pool endpoint and paper mode explicit/default, require `zeitgeist_submit_signed_extrinsics=true` plus reviewed `pool_id` metadata and an explicit RPC, and add settlement lifecycle only through documented SDK/RPC flows with fixtures. | [Zeitgeist fetching markets](https://docs.zeitgeist.pm/docs/build/sdk/v2/fetch-markets), [Zeitgeist indexer](https://docs.zeitgeist.pm/docs/build/sdk/v2/indexer), [Zeitgeist Hybrid Router](https://github.com/zeitgeistpm/zeitgeist/tree/main/zrml/hybrid-router) |
| `reality_eth_markets` | Implemented | Read-only Reality.eth question discovery, response-option listing, and lifecycle status are implemented through the official subgraph schema. Prices, price-triggered alerts, orderbooks, paper orders, and trading remain unsupported because Reality.eth is an oracle/question protocol rather than a traded market. | Keep the GraphQL endpoint configured explicitly, keep this adapter read-only, and add answer submission only as a separate wallet-signed oracle workflow with fixtures and user approval. | [Reality.eth contract docs](https://reality.eth.limo/app/docs/html/contracts.html), [Reality.eth subgraph](https://github.com/RealityETH/reality-eth-monorepo/tree/main/packages/graph), [Reality.eth schema](https://raw.githubusercontent.com/RealityETH/reality-eth-monorepo/master/packages/graph/schema.graphql) |
| `sportstrade` | Verified blocked | Verified 2026-08-16: Sporttrade officially ceased all wagering on 2026-05-25; no active production market or order integration is available. | Keep the adapter fail-closed unless Sporttrade officially resumes wagering and publishes a supported integration contract. | [Sporttrade](https://getsporttrade.com/) |
| `prophet_exchange` | Implemented | Official ProphetX Market Data and Trading API endpoints are mapped to tournament/event/market discovery, selection contracts, probability prices, available-quantity quote books, alerts, local paper orders, guarded authenticated market-maker order submission, wallet balance/transaction recovery, and fixed single/batch cancellation. The adapter uses the documented BUY quote shape only; partner approval, credentials, account/KYC/region eligibility, and funded execution remain external gates. | Keep partner credentials outside config, keep live trading and order management disabled by default, require explicit safety acknowledgement and caps, validate the original `external_id` plus returned `order_id` for cancellations, and add status/settlement flows only through documented endpoints with fixtures. | [ProphetX Market Data Integration](https://docs.prophetx.co/docs/market-data-integration), [ProphetX Trading API Integration](https://docs.prophetx.co/docs/integration), [ProphetX wallets](https://docs.prophetx.co/docs/wallets), [ProphetX auth](https://docs.prophetx.co/reference/post_auth-login-2), [ProphetX market search](https://docs.prophetx.co/reference/get_mm-search-markets-2) |
| `sporttrade_products` | Verified blocked | Verified 2026-08-16: Sporttrade officially ceased all wagering on 2026-05-25; its prediction/exchange products are not an active production integration target. | Keep the adapter fail-closed unless Sporttrade officially resumes wagering and publishes a supported product API. | [Sporttrade](https://getsporttrade.com/) |
| `matchbook` | Implemented | Official Matchbook event/market discovery, decimal-odds prices, runner orderbooks, authenticated aggregated matched-bet history, simulation-first copy previews from complete matched bets, bounded derived OHLCV candles from those matched bets, settled/current-bet reports, current-offer reads, balance/account reads, alerts, dry-run orders, guarded session-authenticated offer submission, and fixed cancel/edit offer mutations are implemented. Derived candles retain source matched-bet ids and are explicitly marked as derived because Matchbook documents matched bets rather than a native candle endpoint. Account filters validate numeric ids, ISO/epoch date bounds, odds types, offer sides/statuses, intervals, cancellation reasons, and aggregation types before request construction; mutation ids, scope filters, odds/stakes, duplicate batches, and global-cancel confirmation are validated before forwarding. Live execution and order management require a Matchbook account session, region/eligibility, explicit acknowledgement, separate opt-ins, and configured safety caps. Copy previews never submit live offers automatically. | Keep credentials in environment variables, keep paper mode as default, honor Matchbook API/data terms and rate limits, keep `matchbook_order_management_enabled` false by default, and add settlement mutations only through documented endpoints with fixtures. | [Matchbook developers](https://developers.matchbook.com/), [Get aggregated matched bets](https://developers.matchbook.com/reference/get-aggregated-matched-bets), [Get settled bets](https://developers.matchbook.com/reference/get-settled-bets-v2), [Get current bets](https://developers.matchbook.com/reference/get-current-bets-v2), [Get offers](https://developers.matchbook.com/reference/get-offers-v2), [Cancel offer](https://developers.matchbook.com/reference/cancel-offer-v2), [Cancel offers](https://developers.matchbook.com/reference/cancel-offers-v2), [Edit offer](https://developers.matchbook.com/reference/edit-offer-v2), [Edit offers](https://developers.matchbook.com/reference/edit-offers-v2), [Get balance](https://developers.matchbook.com/reference/get-new-wallet-balance), [Get account](https://developers.matchbook.com/reference/get-account), [Login](https://developers.matchbook.com/reference/login), [Submit offers](https://developers.matchbook.com/reference/submit-offers-v2) |
| `scicast` | Implemented | Archive-only adapter for SciCast's documented credentialed Data Mart: question discovery, binary/multiple-choice contracts, historical forecast snapshots, public trade-history normalization, alerts, and local dry-run orders are fixture-backed. SciCast later shut down as a live service, so orderbooks, live trading, and copy trading remain unsupported. | Keep `SCICAST_API_KEY` outside repository files, validate the Data Mart host or an explicitly reviewed archive mirror, keep paper mode local/dry-run, and do not treat historical snapshots as current market quotes. | [SciCast Data Mart guide](https://scicast.wordpress.com/wp-content/uploads/2014/10/scicast_datamart_guide_v1-21.pdf), [SciCast complete data package](https://scicast.org/blog_subdomain/download/complete-scicast-data-package/), [SciCast official archive note](https://scicast.org/blog_subdomain/mode_grid/) |
| `meta_arena` | Verified blocked | Verified 2026-08-16: Meta Arena is a game platform and does not expose a validated prediction-market API for this adapter model. | Keep it out of market operations unless an official prediction API is published. | [Meta Arena docs](https://docs.metaarena.world/) |

## Implementation Rules For Clearing A Blocker

1. Add or update an adapter using official/documented APIs only.
2. Add offline fixture tests for every endpoint parser and order/dry-run path.
3. Keep paper/dry-run mode as default.
4. Keep live trading disabled unless the user explicitly enables it in local config.
5. Never commit credentials, cookies, tokens, API keys, private URLs, IPs, or user data.
6. Update `README.md`, `data/config.example.json`, and this file when a blocker changes.

