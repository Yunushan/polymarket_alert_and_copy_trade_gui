# Goal Completion Audit

This document records the current evidence for the multi-market support goal in
[`GOAL.md`](../GOAL.md). It is an evidence index, not a claim that external
account, jurisdiction, or platform-runner checks have occurred.

DFlow's wallet-filtered on-chain fill feed is now exposed through the bounded
account activity surface and simulation-first copy preview path. The adapter
requires a canonical Solana identity, API key, complete outcome-mint mapping,
side, size, price, timestamp, and transaction identity; no live order is
submitted by copy processing.

Good Judgment Open's documented authenticated recovery surface is also
covered: `/api/v1/me`, `/api/v1/prediction_sets`, and `/api/v1/scores` are
exposed as bounded account operations and preserve forecast/score semantics.

Smarkets now exposes simulation-only copy previews from complete authenticated
executed-order rows; the preview path never submits live orders automatically.

## Completion Scope

All repository-controlled requirements in the goal are complete. This includes
the application, adapter catalog, supported or truthful blocked adapters,
configuration, GUI and CLI paths, documentation, local safety controls, and
repeatable verification, including the conservative readiness scorer described
in [`PRODUCTION_READINESS.md`](PRODUCTION_READINESS.md). The user explicitly excluded the external evidence
gates below from this local completion scope. They remain deliberately
unpromoted and cannot be inferred from local tests or CI configuration.

## Local Requirement Evidence

| Goal requirement | Evidence in the repository | Verification |
| --- | --- | --- |
| Common adapter architecture | `market_adapters/base.py`, `market_adapters/registry.py`, and `market_adapters/catalog.py` define the shared contract, registry, and 68-market catalog. | `tests/test_market_adapters.py` validates the full contract and registry. `verify.py` runs `run_adapter_catalog_check`. |
| Every listed market appears in config and GUI | `data/config.example.json`, `app.py`, `web_api.py`, and `frontend/src/App.tsx` consume the same market catalog. | `tests/test_config_examples.py`, `tests/test_market_adapters.py`, `tests/test_final_parity.py`, Tkinter smoke, React build, and browser smoke. |
| Working adapter or truthful stub for each market | The 57 implemented adapters live under `market_adapters/`; verified unsupported adapters use `VerifiedBlockedAdapter` and the exact reasons in [`BLOCKERS.md`](BLOCKERS.md). Good Judgment Open now has a fixture-backed Cultivate Forecasts adapter for question/answer discovery, probabilities, irregular forecast history, local paper previews, and guarded forecast submission; its instance URL, credentials, account eligibility, and live evidence remain operator-owned. BetMGM now has a fixture-backed partner Sports API adapter for fixture/market/option discovery, implied prices, and local paper orders; its partner API has no supported order or account-activity endpoint. Robinhood Prediction Markets and Kalshi via Robinhood now have fixture-backed read-only Kalshi distribution aliases, and DraftKings Predictions has a fixture-backed read-only Crypto.com/CDNA alias. Coinbase Prediction Markets now exposes the public Kalshi trades/candlesticks through the same read-only alias. Nadex now has a fixture-backed read-only CDNA prediction-event alias; Blinq now has a fixture-backed read-only Polymarket data alias including public price-history points and read-only CLOB trades; FanDuel Predicts has a fixture-backed read-only OG/CDNA alias while CME-listed contracts remain covered by the CME/IBKR adapter. SciCast now has a credentialed archive-only Data Mart adapter for historical question, forecast, and trade snapshots. Hypermind now has a fixture-backed archive-only adapter over its official trade-level CSV and winning-outcomes export for event/contract discovery, normalized historical prices/trades, derived point candles, and local paper previews; it preserves unknown trade direction as `TRADE` and keeps execution/orderbook/copy surfaces unsupported. XO now converts complete authenticated account trades into validated simulation-first copy previews while preserving non-live semantics. Zetarium World uses the reviewed BSC `PredictionMarket` deployment and Lamas Finance uses the official Solana Anchor `PricePredict`/`UpOrDown` programs; all live paths remain guarded. | `tests/test_market_adapters.py` verifies implemented adapters, 11 verified blockers, capability flags, and clear operational errors. `tests/test_hypermind_adapter.py`, `tests/test_good_judgment_open_adapter.py`, `tests/test_additional_official_adapters.py`, and the required offline fixtures cover the new adapter surfaces. `tests/test_blockers_doc.py` verifies all catalog entries have blocker coverage. |
| Official/documented integration policy | Adapter implementations cite official sources and the documented unsupported cases are preserved in [`BLOCKERS.md`](BLOCKERS.md). | Adapter fixture/unit tests and the blocker-document verifier run in `python verify.py`. |
| Advertised capability and operation contract | `market_adapters/capability_audit.py` maps every advertised core capability to its concrete adapter operation, requires `market_discovery` to pair with event listing, and checks that every declared authenticated account/order-management operation has a concrete dispatcher and CLI choice. | `tests/test_market_adapters.py` and `verify.py` fail if a capability resolves to the base fail-closed method, an authenticated operation has no concrete adapter method/CLI choice, or catalog metadata contradicts its operational discovery surface. |
| Canonical full-catalog support matrix | `market_adapters/support_matrix.py` produces one row for every catalog market, classifies each advertised operation as supported, guarded, unsupported, or verified-blocked, lists exact account/order allow-lists and safety requirements, and carries blocker/audit evidence. `support_matrix_summary` derives the aggregate implementation/operation totals from those rows; the same rows and summary are included in `/api/markets`, exposed by `/api/markets/support-matrix` and `/api/markets/{market_id}/support`, printed by `markets support`, and rendered in the React Markets detail. `verify.py` rejects stale `GOAL.md` totals instead of allowing documentation drift. | `tests/test_market_adapters.py`, `tests/test_verifier_coverage.py`, `tests/test_web_api.py`, `tests/test_cli.py`, frontend type/build checks, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Discovery, contracts, prices, history, alerts, paper, guarded live, and copy capability declarations | Capability values are declared in `market_adapters/catalog.py` and enforced by `market_adapters/base.py` plus each adapter. `trade_history` and `candle_history` are explicit capability keys: only adapters with documented, fixture-backed feeds advertise them, while generic and verified-blocked adapters fail closed with a market-specific unsupported error. Metaculus official Community Prediction aggregation history is normalized as irregular point candles only when the authenticated post response includes accessible history; its forecast snapshots deliberately carry no fabricated volume and are not resampled into exchange OHLCV. Opinion's, Polymarket's, Gemini's, and Probable's documented price histories are normalized as flat OHLC points with no fabricated volume; Myriad's documented `outcomes[*].price_charts` is normalized from official 24h/7d/30d/all buckets without resampling; Context V2 now consumes the documented market activity trade feed and binary price-history points, preserving YES/NO outcome semantics and avoiding synthetic volume; Opinion also normalizes authenticated filled wallet trades per requested outcome token when `OPINION_WALLET_ADDRESS` and `OPINION_API_KEY` are configured, while Probable normalizes public wallet-scoped `/activity` trade records after validating the wallet, condition/token, side, price, size, and time bounds. Hyperliquid now normalizes the documented account-scoped `userFills`/`userFillsByTime` feed into HIP-4 trade history only when an explicit wallet identity is configured, while filtering ordinary perpetual/spot activity from outcome contracts. Gemini also has fixture-backed authenticated recovery for active/history orders, current/settled positions, and event volume metrics. Betfair normalizes authenticated matched account orders from documented `listCurrentOrders` match-time queries into probability-priced trades, filtering by market runner and caller time bounds, and derives bounded execution candles with explicit source/bet-id metadata. Matchbook now normalizes authenticated aggregated matched bets from its documented `matched-bets/aggregated` route into probability-priced trades with local runner/time filtering. Manifold now normalizes its public `/v0/bets?contractId=...` feed into per-fill probability-priced trades and bounded derived OHLCV candles with explicit source/fill-id metadata; DFlow now normalizes its documented API-key trade feed into outcome-mint trades and bounded derived OHLCV candles with explicit source/trade-id metadata; SX Bet normalizes its public `/trades-v3/public` tape into outcome-specific probability-priced trades with base-unit stake scaling and derives bounded OHLCV candles from that tape with explicit source/trade-id metadata; all derived candle feeds are marked as such rather than treated as native OHLCV endpoints. SX Bet also exposes fixture-backed v3 API-key balance, active/history order, order-detail, fill, and position reads, simulation-first BUY-only copy previews from authenticated fills, and guarded order/event/all cancellation. The shared IBKR event-contract adapter now normalizes documented recent account executions from `/iserver/account/trades` plus historical Last Trade OHLC bars for ForecastTrader, ForecastEx, and CME event contracts; execution history stays explicitly bounded to IBKR's current-day plus six-day upstream window. Opinion's, Manifold's, Myriad's, Hyperliquid HIP-4's, Predict.fun's, and Probable's official wallet/account activity feeds support simulation-first copy intents; Predict.fun requires the JWT-authenticated account identity and complete matched-fill fields, while Probable validates the requested EVM wallet and admits only complete public `TRADE` rows. Opinion also has an optional official CLOB SDK limit/market order boundary plus guarded SDK cancellation operations with BNB-chain and credential gates. Myriad now exposes fixed authenticated cancel, batch-cancel, cancel-all, and atomic batch-modify mutation boundaries with HMAC/JWT credential requirements and separate opt-ins. Limitless now exposes fixed HMAC single, batch, and market-scoped cancellation boundaries with separate opt-in and exact confirmations. Xmarket now exposes bounded authenticated positions, user-order, and market-order reads through its API-key account contract. The desktop and web selected-market wallet paths use the selected adapter feed instead of assuming Polymarket. Guarded live boundaries include Opinion CLOB orders, Thales externally signed AMM transactions, Hedgehog externally signed `DepositV1` transactions, Omen/Gnosis externally signed FPMM transactions, Seer externally signed third-party-DEX transactions, MetaDAO externally signed Solana router transactions, Zeitgeist externally signed `HybridRouter` Substrate extrinsics, Zetarium externally signed BSC `placeBet` transactions, and Lamas externally signed Solana Anchor `predict` transactions. | `tests/test_market_adapters.py` validates capability keys and unsupported-operation behavior; adapter-specific tests cover DFlow public trade and derived-candle normalization, Manifold public trade and derived-candle normalization, Gemini contract orderbook/price-history/recovery normalization, Metaculus binary/multiple-choice/numeric forecast-history normalization and access failures, IBKR execution and historical OHLC parsing, Betfair matched-order and derived-candle normalization, Matchbook matched-order normalization, SX Bet public trade-tape, derived candle, and v3 account/cancellation/copy normalization, Polymarket public price-history and authenticated trade validation, Opinion authenticated filled-trade and price-history normalization plus CLOB order and cancellation validation, Probable activity/price-history/copy normalization and address/time validation, Myriad price-chart candle normalization and HMAC/fixed order lifecycle mutation validation, Limitless market/history/account and HMAC cancellation validation, Xmarket authenticated account endpoint validation, Context V2 activity/price-history normalization, Hyperliquid wallet trade-history/copy normalization and bounds, Predict.fun authenticated activity/copy normalization, Opinion/Manifold/Myriad/Hyperliquid/Probable wallet activity and copy simulation, and the Thales, Hedgehog, Omen, Gnosis, Seer, MetaDAO, Zeitgeist, Zetarium, and Lamas signed-transaction guardrails. |
| Derived trade-feed candle audit | MetaDAO, Manifold, DFlow, SX Bet, Betfair, Matchbook, and Drift BET expose bounded OHLCV aggregations only from their documented trade/fill/execution feeds; every result carries an explicit derived/source marker and source trade ids because these venues do not publish native candle endpoints. MetaDAO caps each call by Solana slot window, per-call scan/request window count, streamed response bytes, and total decoded-row count; calls without timestamp bounds return only a bounded recent slice, may omit the earliest partial bucket, and mark every retained candle from a truncated scan as partial with bounded-slot coverage. Timestamp-complete requests must reach pair creation and verify the declared creation swap within the configured horizon or fail closed. Full-history completeness is not claimed. Drift additionally surfaces the prediction feed's 20-record page and 31-day retention limits and applies complementary YES/NO semantics to taker LONG/SHORT fills. | Adapter-specific history tests and `tests/test_market_adapters.py` capability checks. |
| Trueo dual-generation on-chain history | Trueo validates every requested market against the official manager, detects V1/V3 versus V2/V4 from `VERSION()`, verifies V4 PoolKey hashes/token pairs/hooks and StateView/PoolManager identity, and normalizes canonical Swap logs into bounded trades and derived OHLCV candles. Log ranges, per-request block chunks, batch sizes, response bytes, result counts, block-header counts, duplicates, malformed ABI data, and chain ordering are bounded or rejected. The default head is 12 blocks confirmed; every trade reports block/timestamp coverage; explicit time ranges outside retained coverage and incomplete bounded searches fail closed; boundary candles are marked partial; and top-level or per-call rate limits use bounded retry. Router/callback senders are retained only as raw provenance and are never mislabeled as end-user wallets. | Independent `rpc.json` (V3) and `rpc_v4.json` fixtures plus Trueo cases in `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, and `tests/test_market_adapters.py`. |
| Seer native DEX-pool chart history | Seer's official chain-qualified `market-chart` response is exposed as Generic-market flat candles. The adapter reproduces the official consumer's asymmetric sorted-pair field selection (`token0` outcome uses `token1Price`; `token1` outcome uses `token0Price`), validates every pool against the hydrated outcome/collateral addresses, filters bounded timestamps, preserves mixed hourly/swap points without resampling or volume, rejects Futarchy semantics, and deliberately avoids decimal-unsafe `sqrtPrice` reconstruction. | `market_adapters/seer.py`, `tests/fixtures/seer/market_chart.json`, asymmetric and fail-closed cases in `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, and `tests/test_market_adapters.py`. |
| MetaDAO bounded public spot history | MetaDAO's official Futarchy external API public spot-swap event surface is normalized into bounded recent trades and derived OHLCV candles after pair, asset, event, price, slot, and unique transaction identity validation. The configurable `metadao_history_slot_window`, `metadao_history_max_windows`, `metadao_history_event_cap`, and `metadao_history_response_byte_cap` settings enforce a slot window, per-call scan/request cap, total decoded-row cap, and pre-decode streamed response-byte cap. Calls without timestamp bounds intentionally return only the bounded recent slice and may return fewer rows or omit the earliest partial candle bucket; every retained candle from a truncated scan is marked partial with bounded-slot coverage. Timestamp-complete requests must reach pair creation and verify its declared creation swap within the configured scan horizon or fail closed because estimated Solana block times are not assumed to be monotonic; arbitrary full-history completeness is not claimed. | `market_adapters/metadao.py`, `tests/fixtures/metadao/`, MetaDAO history cases in `tests/test_additional_official_adapters.py`, `tests/test_adapter_runtime.py`, `tests/test_verification_fixtures.py`, and `tests/test_market_adapters.py`. |
| MetaDAO wallet-activity parity | MetaDAO's official DexScreener-compatible swap rows are exposed as the explicit `activity` account operation through the CLI/API/React allow-list. A canonical Solana identity is required, maker filtering is performed locally over bounded ticker/slot scans, raw activity rows are retained, and the response is labeled `bounded_recent`; this is a public activity preview rather than complete account history, and copy remains simulation-first. | `market_adapters/metadao.py`, `tests/test_additional_official_adapters.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and the full verifier. |
| Omen/Gnosis creator-activity parity | Omen's documented `FPMMTrade.creator` subgraph relation is exposed as the explicit `activity` account operation for both Omen and the Gnosis alias through the CLI/API/React allow-list. EVM wallet identity, creator rows, side, outcome, price, size, timestamp, and transaction identity are validated; raw rows and bounded coverage metadata are preserved, and no complete-history or live-copy claim is made. | `market_adapters/legacy_web3.py`, `tests/test_legacy_web3_adapters.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and the full verifier. |
| Externally signed live-order binding and alias isolation | Context V2 requires complete signed economic fields and validates their market/outcome, side, price, size, trader, and signature shape; Probable resolves the canonical token within the requested market and binds its signed amounts; Predict.fun accepts only fully bound LIMIT semantics; and Myriad requires the signed payload network to equal an explicit trusted `myriad_network_id`. All four checks run against the same `PaperOrderRequest` that passed shared live-order preflight before any POST. Probable defaults only the verified BNB mainnet 18-decimal amount scale and requires explicit trusted configuration elsewhere. Blinq starts from generic health metadata, declares no account-recovery operations, and directly rejects inherited Polymarket private getters while retaining optional L2-authenticated read-only trade history. | `tests/test_context_probable_signed_binding.py`, `tests/test_predict_myriad_signed_binding.py`, `tests/test_blinq_adapter.py`, focused adapter tests, Ruff, and the full verifier. |
| Live and copy safety defaults | `core/models.py`, `data/config.example.json`, `web_api.py`, and adapter preflight paths keep live/copy actions disabled unless explicitly enabled. | `tests/test_config_examples.py`, `tests/test_app_logic.py`, `tests/test_polymarket_api.py`, and `tests/test_web_api.py`. |
| Hyperliquid account-recovery parity | Hyperliquid's documented `openOrders`, `historicalOrders`, `clearinghouseState`, `spotClearinghouseState`, `portfolio`, and `subAccounts` Info requests are exposed through the explicit wallet/dex account allow-list and shared CLI/API/React controls; account limits, wallet validation, offline fixtures, and invalid-operation failures are covered. | `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, and the full verifier. |
| Opinion account-recovery parity | Opinion's documented authenticated `/order`, `/order/{orderId}`, and `/positions/user/{walletAddress}` reads are exposed through the explicit CLI/API/React account allow-list. Page/limit, numeric market/chain filters, status values, wallet identity, and order-id path safety are validated; wrapper payload fixtures and missing-credential/invalid-input tests prevent accidental arbitrary authenticated paths. | `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, and the full verifier. |
| Betfair account-recovery parity | Betfair's documented authenticated `listCurrentOrders`, `listClearedOrders`, `getAccountFunds`, `getAccountDetails`, `getAccountStatement`, and `listCurrencyRates` feeds are exposed through the explicit CLI/API/React account allow-list. Current-order status/order-by/sort/date/market/runner filters, cleared-order status/roll-up/id/date filters, bounded paging, item descriptions, statement locale/date/wallet filters, and currency-code validation are enforced before forwarding; raw upstream payloads remain lossless and account calls use Betfair's separate account JSON-RPC endpoint. | `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, and the full verifier. |
| Betfair order-management parity | Betfair's documented `cancelOrders`, `updateOrders`, and `replaceOrders` mutations are exposed through a separate POST-only API route, `markets manage-orders` CLI command, and guarded React Markets panel. The adapter validates the official instruction schemas and 60-instruction cap, requires `betfair_order_management_enabled` plus the shared live-safety acknowledgement, rejects async requests except for replace, and requires exact `CANCEL ALL BETS` confirmation for global cancellation. Offline response fixtures, adapter routing tests, CLI forwarding tests, API allow-list tests, and frontend type/build checks cover the surface; credentials, account eligibility, funding, and any live verification remain external gates. | `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Kalshi order-management parity | Kalshi's documented V2 `cancel_order`, `batch_cancel_orders`, `amend_order`, and `decrease_order` mutations are exposed through the same POST-only API route, `markets manage-orders` CLI command, and guarded React Markets panel. The adapter signs only fixed official paths, validates safe order/ticker ids, bid/ask, probability price, positive counts, exact decrease shape, subaccount/exchange-index bounds, and a local 50-order batch cap; `kalshi_order_management_enabled`, shared live-safety acknowledgement, and exact `I_UNDERSTAND_THIS_CHANGES_LIVE_ORDERS` confirmation are required. Offline response fixtures, adapter routing/validation tests, API allow-list tests, CLI forwarding tests, and frontend build checks cover the surface; credentials, account eligibility, funding, and live verification remain external gates. | `tests/test_kalshi_adapter.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Polymarket CLOB account/order-management parity | Polymarket's documented L2-authenticated `GET /data/orders`, `GET /order/{orderID}`, and `GET /trades` reads are exposed as `active_orders`, `order_detail`, and `fills` through the shared CLI/API/React allow-list, including trade-id, contract, cursor, and time-bound filters. Fixed `DELETE /order`, `/orders`, `/cancel-all`, and `/cancel-market-orders` mutations are separately exposed with strict identifier validation, a 3,000-order batch cap, `polymarket_order_management_enabled`, shared live-safety gates, and exact `I_UNDERSTAND_THIS_CHANGES_LIVE_ORDERS` confirmation. Offline CLOB response fixtures, adapter endpoint tests, API forwarding tests, CLI parity, and frontend build checks cover the surface; valid L2 credentials, account eligibility, geoblock status, funding, and live verification remain external gates. | `tests/test_polymarket_adapter.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Gemini Prediction Markets order-management parity | Gemini's documented authenticated `POST /v1/prediction-markets/order/cancel` and `POST /v1/prediction-markets/order/batch/cancel` mutations are exposed through the shared `markets manage-orders` CLI command, POST-only API route, and guarded React Markets panel. The adapter validates positive int64 order ids, unique 1–20 item batches, fixed endpoint paths, `gemini_order_management_enabled`, shared live-safety gates, synchronous requests, and exact `I_UNDERSTAND_THIS_CHANGES_LIVE_ORDERS` confirmation. Offline response fixtures, adapter validation/auth tests, CLI operation allow-list coverage, and frontend build checks cover the surface; Trader role, terms acceptance, account eligibility, funding, and live verification remain external gates. | `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Opinion CLOB order-management parity | Opinion's official Python CLOB SDK `cancel_order`, `cancel_orders_batch`, and `cancel_all_orders` mutations are exposed through the shared `markets manage-orders` CLI command, POST-only API route, and guarded React Markets panel. Order ids are locally bounded and unique, cancel-all supports only positive market filters and BUY/SELL filters, and all mutations require `opinion_order_management_enabled`, shared live-safety acknowledgement, the official SDK credentials, and exact operator/global confirmations. SDK responses are JSON-normalized and no caller-provided path or HTTP method is accepted; credentials, wallet eligibility, funding, and live verification remain external gates. | `market_adapters/opinion.py`, `tests/test_additional_official_adapters.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Limitless Exchange order-management parity | Limitless's documented HMAC `DELETE /orders/:orderId`, `POST /orders/cancel-batch`, and `DELETE /orders/all/:slug` mutations are exposed through the shared `markets manage-orders` CLI command, POST-only API route, and guarded React Markets panel. Order identifiers and market slugs are path-safe, batch cancellation is bounded to 100 unique ids, global cancellation is market-scoped, and all mutations require `limitless_order_management_enabled`, shared live-safety acknowledgement, token credentials, and exact operator/global confirmations. Requests are signed against fixed paths; no caller-provided method or endpoint is accepted. | `market_adapters/limitless.py`, `tests/test_limitless_adapter.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Smarkets account and order-management parity | Smarkets' documented authenticated `GET /orders/` and `GET /accounts/` reads plus fixed single-order and market-scoped cancellation routes are exposed through the shared CLI/API/React allow-lists. Executed order rows with positive filled quantity, average executed price, and a valid execution timestamp are normalized as account-scoped trades; bounded OHLCV candles are derived from those fills and retain source order ids. Order state filters, bounded paging, path-safe identifiers, `smarkets_order_management_enabled`, shared live-safety acknowledgement, and exact `I_UNDERSTAND_THIS_CHANGES_LIVE_ORDERS` confirmation are enforced; unscoped global cancellation is intentionally unavailable. API approval, account eligibility, funding, and live verification remain external gates. | `market_adapters/smarkets.py`, `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Probable account and order-management parity | Probable's documented L2-authenticated `GET /orders/{chain}/open` and `GET /order/{chain}/{orderId}` reads plus fixed `DELETE /order/{chain}/{orderId}` cancellation are exposed through the shared CLI/API/React allow-lists. Query parameters are included in the HMAC signature, order/token identifiers are path-safe and bounded, batch/global cancellation composes the fixed single-order route with a 50-order cap, and mutations require `probable_order_management_enabled`, shared live-safety acknowledgement, HMAC credentials, exact `I_UNDERSTAND_THIS_CHANGES_LIVE_ORDERS`, and exact `CANCEL ALL PROBABLE ORDERS` for global cancellation. Offline fixtures and adapter coverage validate reads, signatures, fixed paths, and safety rejection; BSC account eligibility, funding, and credentialed live verification remain external gates. | `market_adapters/probable.py`, `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Hyperliquid signed order-management parity | Hyperliquid's documented `POST /exchange` `cancel`, `cancelByCloid`, `modify`, `batchModify`, and `scheduleCancel` actions are exposed through the shared `markets manage-orders` CLI command, POST-only API route, and guarded React Markets panel. The adapter never signs or rewrites payloads: callers must provide the complete external signature envelope, HIP-4 synthetic assets are required for order references, batches are capped at 50, scheduled cancellation requires a separate exact confirmation, and `hyperliquid_order_management_enabled` is off by default on top of shared live-safety gates. Offline response fixtures, adapter validation tests, CLI/API forwarding, frontend types, and build checks cover the surface; wallet ownership, account eligibility, funding, and live verification remain external gates. | `market_adapters/hyperliquid.py`, `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Xmarket account-recovery parity | Xmarket's documented API-key `GET /positions`, `GET /order/my-orders`, and `GET /order/market/:marketId` reads are exposed through the explicit CLI/API/React account allow-list. Position/order statuses, page/page-size bounds, and market path identifiers are validated before request construction; raw upstream payloads remain lossless and account reads use the documented API base for positions and authenticated API base for order feeds. | `market_adapters/xmarket.py`, `tests/test_additional_official_adapters.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and the full verifier. |
| Xmarket batch order-management parity | Xmarket's documented authenticated `POST /order/batch` and `POST /order/cancel-batch` mutations are exposed through the shared `markets manage-orders` CLI command, POST-only API route, and guarded React Markets panel. Batch create/cancel payloads are capped at 100 entries, outcome/order identifiers are validated, quantity/price and configured size/notional caps are enforced, synchronous fixed endpoints are used, and `xmarket_order_management_enabled`, shared live-safety acknowledgement, and exact `I_UNDERSTAND_THIS_CHANGES_LIVE_ORDERS` confirmation are required. | `market_adapters/xmarket.py`, `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| IBKR event-contract account/order-management parity | ForecastTrader, ForecastEx, and CME event-contract adapters expose documented open-order/status reads plus fixed cancel, cancel-all, and LMT modification routes through the shared CLI/API/React surfaces. Numeric order ids, event-contract payload fields, quantity/price/side/TIF bounds, CME `manualIndicator`/`extOperator` requirements, separate `ibkr_order_management_enabled` opt-in, shared live gates, exact operator/global confirmations, and offline fixture-backed tests are enforced; authorized sessions, broker entitlements, confirmation replies, and funded execution remain external gates. | `market_adapters/ibkr_event_contracts.py`, `tests/test_additional_official_adapters.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Manifold account and open-limit cancellation parity | Manifold's documented authenticated `/v0/me` and `/v0/bets` feeds now expose account details, open-limit orders, and order history through explicit CLI/API/React allow-lists with bounded contract, cursor, and timestamp filters. The documented `POST /v0/bet/{id}/cancel` route supports one validated cancellation behind `manifold_order_management_enabled`, shared live-safety gates, exact confirmation, and API-key authentication; CLOB orderbooks and multi-answer live buys remain unsupported. | `market_adapters/manifold.py`, `tests/test_manifold_adapter.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, `data/config.example.json`, and the full verifier. |
| Prophet Exchange wallet, history, copy, and cancellation parity | ProphetX's documented wallet balance, paginated transaction/order-history, order-detail, and filled-trade endpoints are exposed through explicit CLI/API/React account allow-lists with bounded opaque cursors, timestamps, filters, and limits. The v4 filled-trade feed is normalized with fixed order-detail enrichment and bounded derived candles; complete authenticated fills now also produce simulation-first copy previews after strict contract/BUY-only/price/size/trade-identity validation. Fixed `/mm/cancel_order` and `/mm/cancel_multiple_orders` Trading API paths require the original `external_id`, returned `order_id`, separate `prophet_exchange_order_management_enabled` opt-in, shared live-safety gates, and exact operator confirmation; offline fixtures cover the history, copy, and mutation responses. | `market_adapters/prophet_exchange.py`, `market_adapters/expanded_catalog.py`, `tests/test_prophet_exchange_adapter.py`, `tests/test_market_adapters.py`, `tests/fixtures/prophet_exchange/`, `data/config.example.json`, and the full verifier. |
| Azuro bettor bet-history and copy-preview parity | Azuro's documented V3 client subgraph `v3Bets` and `liveBets` feeds are exposed as a bounded `bet_history` account operation with canonical wallet validation, environment-aware endpoint mapping, explicit GraphQL error handling, and raw response preservation. Ordinary single-selection rows are normalized into wallet activity with fixed-point stake/odds conversion and simulation-first paper-copy previews; multi-selection express/combo rows are excluded. | `market_adapters/azuro.py`, `tests/test_azuro_adapter.py`, `tests/fixtures/azuro/bet_history.json`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, `data/config.example.json`, and `docs/BLOCKERS.md`. |
| Myriad account-activity parity | Myriad's documented public `GET /users/:address/events` feed is exposed as a bounded `account_activity` operation through the shared CLI/API/React account allow-list. Canonical wallet validation, network scoping, normalized BUY/SELL activities, and raw upstream payload preservation are enforced while live order/copy execution remains guarded. | `market_adapters/myriad.py`, `tests/test_additional_official_adapters.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, `data/config.example.json`, `docs/BLOCKERS.md`, and the full verifier. |
| Myriad portfolio, market-position, and position-intent parity | Myriad's documented public `GET /users/:address/portfolio` and `GET /users/:address/markets` reads are exposed as bounded `portfolio` and `market_positions` operations through the shared CLI/API/React account allow-list. The six documented `POST /positions/*` split/merge/redeem/NegRisk endpoints are now exposed through a separate preview-only CLI/API/React position-intent surface that validates ids, uint256 amounts, bytes32 event ids, and outcome indexes, returns only `{to, calldata, value}`, and never signs or submits on-chain. | `market_adapters/myriad.py`, `tests/fixtures/myriad_markets/`, `tests/test_additional_official_adapters.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, `frontend/src/api.ts`, `frontend/src/types.ts`, `README.md`, `data/config.example.json`, `docs/BLOCKERS.md`, and the full verifier. |
| Matchbook account-recovery parity | Matchbook's documented authenticated settled-bet, current-bet, current-offer, balance, and account endpoints are exposed through the explicit CLI/API/React account allow-list. Numeric id lists, epoch/ISO date bounds, odds types, offer side/status filters, intervals, cancellation reasons, and aggregation modes are validated before forwarding; raw upstream payloads remain lossless. | `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, and the full verifier. |
| Matchbook order-management parity | Matchbook's documented `DELETE /v2/offers/{offer_id}`, filtered/global `DELETE /v2/offers`, and single/batch `PUT /v2/offers` mutations are exposed through the shared `markets manage-orders` CLI command, POST-only API route, and guarded React Markets panel. Positive int64 ids, 100-item caps, scoped cancellation filters, edit odds/stake fields, duplicate batches, separate `matchbook_order_management_enabled` opt-in, shared live-safety gates, and exact operator/global-cancel confirmations are validated before forwarding fixed paths. | `market_adapters/matchbook.py`, `tests/test_additional_official_adapters.py`, `tests/test_verification_fixtures.py`, `tests/test_cli.py`, `tests/test_web_api.py`, `frontend/src/App.tsx`, and the full verifier. |
| Secret and endpoint hygiene | Application sources must not contain common access-token formats, private keys, credentialed URLs, non-loopback private-network addresses, or literal authorization/cookie values. | `tests/test_secret_hygiene.py` and the `verify.py` secret-hygiene gate. |
| GUI market selector and web parity | Tkinter integration is in `app.py`; the web UI is in `frontend/src/App.tsx`; state/API surface is in `web_api.py`. Wallet polling and simulation-first copy use the selected market's official activity adapter where one is available, while Polymarket-only analytics/geoblock flows remain explicitly labeled. The React Markets view now exposes enabled-adapter discovery, contract, price, orderbook, trade, candle, and explicitly allow-listed credentialed account reads through the normalized API surface. | `verify.py --frontend-build --frontend-live-smoke` runs Tkinter metadata smoke, React build, API checks, and a local headless browser smoke. CI and release additionally run `python app.py --gui-smoke-test` under Ubuntu Xvfb to construct the actual Tk widget tree and verify shutdown without network workers. |
| README capability matrix | [`README.md`](../README.md) contains all 68 rows and required capability columns. | `tests/test_readme_matrix.py` and `verify.py` reject missing rows, headers, and `TBD` values. |
| Offline fixtures and verifier | `tests/fixtures/` contains adapter and Polymarket fixtures, including official Polymarket price-history and authenticated CLOB trade payloads, Crypto.com Predictions/CDNA, Nadex/CDNA alias, Blinq/Polymarket history aliases, Fanatics Markets alias, FanDuel Predicts/OG alias, Coinbase Prediction Markets/Kalshi trade/candlestick aliases, Context V2 activity and price-history payloads, Opinion wallet trades and token price history, Reality.eth question-subgraph, Gnosis/Omen, IBKR event-contract discovery/snapshot/history payloads, Hedgehog HPL Parimutuel account, Frenzy `BetSettled` log, PRDT legacy `Prediction` RPC shapes, reviewed Zetarium BSC `PredictionMarket` RPC shapes, and Lamas Solana Anchor `RoundResult` RPC shapes; `verify.py` validates fixture JSON, named required fixtures, imports, metadata, docs, workflows, and the test suite. It also requires a complete fixture-directory/test-file mapping for every implemented adapter. | `tests/test_verifier_coverage.py`, `tests/test_polymarket_adapter.py`, `tests/test_blinq_adapter.py`, `tests/test_crypto_com_predict_adapter.py`, `tests/test_fanatics_markets_adapter.py`, `tests/test_fanduel_predicts_adapter.py`, `tests/test_nadex_adapter.py`, `tests/test_coinbase_prediction_adapter.py`, `tests/test_legacy_web3_adapters.py`, `tests/test_additional_official_adapters.py`, `tests/test_hedgehog_markets_adapter.py`, `tests/test_frenzy_finance_adapter.py`, `tests/test_prdt_finance_adapter.py`, `tests/test_zetarium_adapter.py`, `tests/test_lamas_finance_adapter.py`, and `python verify.py --frontend-build --frontend-live-smoke`. |
| Cross-platform Python support contract | `pyproject.toml` requires Python `>=3.10`; `.github/workflows/ci.yml` defines Python 3.10 through 3.14 plus the moving 3.x lane and documented OS matrix. | `verify.py` checks the workflow contract and `tests/test_platform_support.py` checks that support is described without a false full-support claim. |
| Release metadata, version lineage, and installed artifacts | `pyproject.toml` carries the next unreleased version and modern SPDX/license-file metadata; `MANIFEST.in` defines reproducible source contents; CI/release inspect both archives and install the exact wheel from outside the source tree. | `verify.py` rejects tag reuse, shallow/stale histories, deprecated license metadata, and missing distribution gates; `scripts/verify_python_dist_artifacts.py` checks metadata and required/forbidden archive members, then the installed-wheel smoke checks CLI startup, all 68 registry entries, and the Crypto.com adapter import before upload. |
| CLI-only operation | `market_sentinel_cli.py` exposes market discovery, contract listing, normalized prices/orderbooks/trades/candles, explicitly allow-listed authenticated account recovery, market safety/configuration, alerts, paper including durable position-mark refresh/clear, wallet, copy, analytics, durable leaderboard scan/status/export, Live Safety report/review/decision/proposal artifacts, and server commands without Tkinter. | `tests/test_cli.py` and the full verifier cover CLI parsing, normalized market-read/account serialization, local paper marks and report artifacts, state handling, exports, recovery, and status. |

### Account-recovery parity

Predict.fun's documented public order-match feed is also covered: the adapter
normalizes `GET /v1/orders/matches` into outcome-filtered BUY/SELL trades,
converts the documented ISO or millisecond execution timestamps, and applies
the shared local time bounds without inventing a private-account requirement.
The endpoint, fixture, and parser behavior are covered by the Predict.fun
adapter tests and the generic market-history API route.

Predict.fun's authenticated `GET /v1/account/activity` feed now also powers
the wallet/copy workflow for the JWT-authenticated account address.  Only
matched/fill events with a complete outcome, quote side, filled size,
execution price, timestamp, and transaction identity are normalized; the copy
boundary returns a paper intent and never submits a live order automatically.
Identity matching, field normalization, and simulation behavior are
fixture-backed in the Predict.fun adapter tests.

Gemini's documented authenticated recovery methods are also available through
the allow-listed CLI `markets account <operation>` command and the web/API
route `/api/markets/{market_id}/account/{operation}`. The React Markets view
advertises these operations only when the adapter health payload publishes
them; unsupported operations are rejected before any authenticated request.

Kalshi's signed portfolio operations use the same allow-listed CLI, API, and
React surface: active/history orders, fills, positions, settlements, balance,
and queue positions are mapped only to documented endpoint paths. Order/fill
history can opt into the documented historical feeds. The read-only Kalshi
distribution aliases intentionally publish no private account operations.

Matchbook's authenticated account parity uses the same allow-listed surface:
settled bets, current bets, current offers, wallet balance, and account profile
reads are mapped only to documented endpoint paths. Numeric id lists, date
bounds, odds types, offer side/status filters, intervals, cancellation reasons,
and aggregation modes are validated before request construction; live offers
remain separately guarded and account eligibility is external.

Limitless's documented HMAC account reads now use the same bounded surface:
positions, portfolio history, and market-scoped user orders are available only
through the adapter's explicit operation allow-list. User-order slugs and
optional delegated profiles are validated before request construction, and the
CLI/API/React controls forward only those validated values. Credentials,
delegated-signing scope, linked-account eligibility, and funding remain external
gates.

Thales' documented positional-market subgraph is also exposed through the same
allow-listed surface: bounded wallet-scoped `positions` reads cover both binary
and ranged position balances, while `transactions` reads cover the official
`optionTransactions` feed with optional market and Unix-timestamp filters. The
GraphQL document is static and all user values are sent as typed variables; raw
upstream rows are preserved without recasting AMM activity as CLOB fills. The
subgraph URL and optional default account wallet remain explicit configuration
inputs, and this read-only surface never signs or submits a transaction.

## Polymarket Evidence Tiers

Polymarket public and local safety paths are implemented and tested. The
application deliberately does not promote a tier based only on local fixtures,
a readiness check, or a browser smoke.

| Tier | Current evidence | Status |
| --- | --- | --- |
| `wrapper_available` | Official Gamma/Data/CLOB/Bridge wrappers and adapter paths. | Locally implemented. |
| `app_workflow_available` | GUI, API, CLI, alerts, paper, wallet tracking, guarded copy, MDD analytics, durable scans, and redacted validation reports. | Locally implemented. |
| `offline_tested` | Fixtures, parser tests, API tests, report-schema/replay/promotion tests, and browser smoke. | Verified locally. |
| `public_live_verified` | Safe public probes can be recorded by `scripts/verify_polymarket_live.py`. | Environment-dependent; not inferred from tests. |
| `credential_live_verified` | Requires a real authenticated CLOB/relayer read or authenticated user WebSocket report. | Requires user credentials and an eligible account. |
| `funded_live_verified` | Requires an explicitly approved, allow-listed funded order/cancel audit. | Requires user approval, a funded eligible account, and live parameters. |

The report schema, promotion guard, review bundle, decision ledger, proposal,
and snapshot archive ensure that incomplete local evidence cannot be promoted to
credentialed or funded verification. See the `POLYMARKET_LIVE_REPORT_*.md`
documents in this directory.

### Observed Public-Live Evidence

On 2026-07-15, 2026-07-18, 2026-07-19, and 2026-08-02, the following public-only command
exited successfully from this workspace:

```powershell
python scripts/verify_polymarket_live.py --skip-authenticated-read-checks --timeout 15
```

It returned `status=ok` for Gamma `/markets`, Data `/v1/leaderboard`, CLOB
`/time`, and Bridge `/supported-assets`. No authenticated read, user WebSocket,
bridge-address creation, credential derivation, funded order, or cancellation
flag was supplied. This is point-in-time public endpoint evidence only; it does
not change the credentialed or funded tier status.

A later re-run on 2026-08-17 from this Windows workspace received remote
connection resets from the public Gamma, Data, CLOB, and Bridge hosts. That
failure does not erase the historical successful observations above, but it
does mean the public-live tier must be re-established from the target network
before a current production claim is made.

### 2026-08-23 Copy Preview Parity Evidence

Matchbook and Limitless now convert complete official account fills/history into
validated simulation-first copy previews. Matchbook uses authenticated
`matched-bets/aggregated`; Limitless uses HMAC `GET /portfolio/history` and
preserves its optional delegated-profile boundary. Both paths require a
documented trade identity, validate contract/side/price/size fields, retain the
raw activity in the preview, and never submit live offers/orders.

Kalshi and Betfair now provide the same simulation-first parity from complete
authenticated execution records. Kalshi portfolio fills supply outcome side,
bid/ask direction, fixed-point price, count, and fill identity; Betfair
`listCurrentOrders` supplies market/selection identity, BACK/LAY direction,
average matched odds, matched size, and bet identity. Both adapters retain raw
activity and fail closed on incomplete or inconsistent records without calling
live mutation endpoints.

IBKR ForecastTrader, ForecastEx, and CME event contracts now provide the same
parity from the documented authenticated `/iserver/account/trades` feed. The
shared adapter validates execution id, event conid, B/S direction, probability
price, and whole filled size, preserves the execution payload, and routes only
to a local paper preview; no IBKR order endpoint is called by copy preview.

Gemini now provides the same parity from authenticated filled order history.
The adapter validates event/instrument identity, filled status, BUY/SELL
direction, positive filled quantity, probability price, timestamp, and order
identity, preserves the raw order, and never calls the live order endpoint from
copy preview.

MetaDAO now exposes its official public maker-tagged swap tape through the
same allow-listed `activity` operation. The CLI, web API, and React Markets
surface accept a canonical Solana wallet, apply bounded local maker filtering,
and preserve the normalized/raw rows while labeling the result as bounded
recent activity. Omen and Gnosis expose their official public
`FPMMTrade.creator` rows through the same operation with canonical EVM wallet
validation and simulation-first copy previews. Neither surface claims complete
account history or performs live copying.

### MetaDAO one-level top-of-book

The official MetaDAO `/api/tickers` bid/ask summaries are normalized as a
fixture-backed one-level `OrderBookSnapshot`. The response marks
`depth=top_of_book_only` and `size_available=false`, with unknown level sizes
represented as `0.0`; full depth and live execution remain external or
unsupported. The regression is covered by the MetaDAO adapter fixture tests.

## Open External Evidence Gates

These are deliberately not replaced with simulated success:

1. Polymarket authenticated read/user-WebSocket proof requires user-provided
   credentials through local environment variables and a region/KYC-eligible
   account.
2. Polymarket funded order/cancel proof requires explicit approval, a funded
   account, and an allow-listed token, price, and size.
3. Full platform-support claims require successful runs on the named physical or
   hosted runners documented in [`PLATFORM_SUPPORT.md`](PLATFORM_SUPPORT.md),
   including the optional Windows 10 self-hosted lane and the declared
   enterprise-Linux/BSD/Solaris evidence. CI configuration alone is not proof
   that those hosts have run successfully.
4. Verified-blocked markets need official APIs, permissions, entitlements, or
   documented contracts before their blockers can be cleared. The exact
   market-by-market prerequisites are maintained in [`BLOCKERS.md`](BLOCKERS.md).

## Repeatable Local Verification

Run the strict local evidence set from the repository root:

```powershell
python verify.py --frontend-build --frontend-live-smoke
```

This validates local implementation and regression coverage, but it must not be
used as evidence for any of the external gates above.

